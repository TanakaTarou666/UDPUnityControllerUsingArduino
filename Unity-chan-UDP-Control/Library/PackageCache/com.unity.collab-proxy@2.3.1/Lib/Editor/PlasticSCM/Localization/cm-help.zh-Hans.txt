== CMD_DESCRIPTION_ACL ==
?置?象?限。

== CMD_USAGE_ACL ==
用法：

    cm ^acl (--^user=<用?名> | --^group=<?名称>)
           (-^allowed|-^denied|-^overrideallowed|-^overridedenied=+|-<?限>[,...])[,...]
           <?象?格>

    --^user             用?名。
    --^group            ?名称。
    -^allowed           ?用指定的一?或多??限。使用
                       逗号?各个?限?行分隔。（使用 'cm ^showpermissions'
                       可?示所有可用?限。）
    -^denied            拒?指定的一?或多??限。使用
                       逗号?各个?限?行分隔。（使用 'cm ^showpermissions'
                       可?示所有可用?限。）
    -^overrideallowed   覆盖允?的一?或多??限。使用
                       逗号?各个?限?行分隔。（使用 'cm ^showpermissions'
                       可?示所有可用?限。）
    -^overridedenied    覆盖拒?的一?或多??限。使用
                       逗号?各个?限?行分隔。（使用 'cm ^showpermissions'
                       可?示所有可用?限。）
    ?象?格         要?置?限的?象。
                       此命令的有效?象?：
                       存??服?器、存??、分支、?更集、??、?
                       和属性。
                       （使用 'cm ^help ^objectspec' 可?一?了解?格。）

安全路径的特殊用法：
    cm ^acl [(--^user=<用?名> | --^group=<?名称>)
            (-^allowed|-^denied|-^overrideallowed|-^overridedenied=+|-<?限>[,...])[,...]]
            [--^delete] [--^branches=[+ | -]<分支>[,...]]
            <?格>

    --^delete           ?除安全路径。
                       ?参?“?注”以了解更多信息。
    --^branches         将安全路径?限?置?一个分支?。
                       使用逗号?各个分支?行分隔。
                       （可?）?个分支前面可添加 + 或 -
                       符号，从而指定在???必?在列表中添加
                       ?是?除某个分支。
                       ?参?“?注”以了解更多信息。
    ?格               要?置?限的安全路径。

== CMD_HELP_ACL ==
?了配置?限，需要了解 Unity VCS 安全机制的工作原理。
??看《安全指南》以了解?限的工作原理：
https://www.plasticscm.com/download/help/securityguide

?注：

    此命令?置用?或??于指定?象、存??、分支、??
    和/或服?器路径的?限。

    ?象?格：
        （使用 'cm ^help ^objectspec' 可了解如何指定?象。）
        '^acl' 命令使用一?特殊?型的?格：安全路径。

        - 安全路径?格：
            ^path:server_path[#tag]
            示例：^path:/src/foo.c
                      ^path:/doc/pdf
                      ^path:/doc/pdf#documents

    ?限操作：
        使用 -^allowed 和 -^denied 指定要?置的?限。
        使用 -^overrideallowed 和 -^overridedenied 参数指定
        要覆盖的?限。

        ?个操作都需要一个?限列表（以逗号分隔）。

    ?限名称：
        ?个?限名称前面都?有 + 或 - 符号。
        + 符号表示?置?限，而 - 符号表示清除?限。
        要?看?象的?限，?使用 'cm ^showacl' 命令。

    覆盖的?限：
        使用 -^overrideallowed 和 -^overridedenied
        来覆盖?限可以???承。
        ??于??在存??或服?器???置的?限
        很有?助。
        示例：
            cm ^acl --^user=vio -^allowed=+^ci -^overrideallowed=+^ci ^br:qa@test
            （允?用? 'vio' 在存?? 'test' 上的分支 'qa' 上?入，
            即使?用?的?限在存????被拒?也是如此。）

    服?器路径?限（又称“安全路径”）：
        - 允???定的服?器路径指定?限。
        - 在?入操作期?会???些?限。
        - 也可以在更新操作期????些?限，
          并可以将?些?限用作一?防止某些目?和文件
          下?到工作区的方法。
        - ?于要?入的?一?，服?器都会??将?路径
          与安全路径匹配。如果匹配，??入操作
          将??是否可以?入??。

        可?安全路径定?的?限
        如下：
            '^ci'、'^change'、'^add'、'^move'、'^rm'、'^read'

        ?于任何?及的?，如果?限??不成功，
        ??入操作将回?。

        要将安全路径?限?置?一个分支?，?使用
        --^branches ??。
        示例：
          cm ^acl --^user=jo -^denied=+^ci ^path:/src#rule0 --^branches=main,main/rel0

        要??与安全路径??的 ACL，???很有用。
        示例：
          cm ^acl --^user=jo -^denied=+^rm ^path:/src#rule0
          （如果没有??，?需要再次指定
          分支列表。）

        安全路径的分支列表是可以??的。
        示例：
          cm ^acl ^path:/src#rule0 --^branches=-main,+main/rel1
          （从列表中?除 'main' 并添加 'main/rel1'。）

        要?除安全路径，?使用 --^delete 参数。
        示例：
          cm ^acl --^user=jo --^delete ^path:/src#rule0

    ?承：
        ?承是 Plastic SCM 3.0 ?期的一???。
        ?是高???，但几乎已弃用。
        ???允??象从任何其他?象?承?限，
        从而覆盖默?的?承?系。

        使用?? -^cut 切断?承?。
        使用?? -^cutncpy 切断并?制当前?承的
        ?限。（?个??受到 Windows 文件系??限的??，
        在 Windows 文件系?中可以切断?承?系但保留???限。）

        -^inherit ??允?用?从?象?格?承。
        示例：'-^inherit=?象?格'

示例：

    cm ^acl --^user=danipen -^denied=+^ci ^rep:core
    （拒?用? 'danipen' 在存?? 'core' 上?入。）

    cm ^acl --^group=developers -^allowed=+^view,-^read -^denied=+^chgperm ^br:main
    （?命令向 'main' 分支中的 'developers' ?
    授予?看?限、清除?取?限并拒? chgperm ?限。）

安全路径示例：

    cm ^acl --^group=devs -^denied=+^ci ^path:/server#rel --^branches=main,main/2.0
    （?于在分支 'main' 和 'main/2.0' 中与 '/server' 匹配的任何路径，
    ?命令拒?向 'devs' ?授予?入?限。?建?? '#rel'
    是?了稍后能??行引用。）

    cm ^acl ^path:/server#rel --^branches=-/main,+/main/Rel2.1
    （更新具有 'rel' ??的安全路径 '/server'，
    ?除 'main' 分支，并将分支 'main/Rel2.1' 添加到
    安全路径所?用到的分支?。就前面的示例而言，
    ?在分支列表将包含 'main/Rel2.1' 和 'main/2.0'。）

    cm ^acl --^user=vsanchezm -^allowed=-^read -^overrideallowed=+^read ^path:/doc
    （?除授?? 'vsanchezm' 的 '^read' ?限，在 '/doc' 路径中?其?行覆盖。）

== CMD_DESCRIPTION_ACTIVATEUSER ==
激活?可的用?。

== CMD_USAGE_ACTIVATEUSER ==
用法：

    cm ^activateuser | ^au <用?名>[ ...][--^server=<存??服?器?格>]

    用?名   要激活的一个或多个用?名。使用双引号 (" ")
                指定包含空格的用?名。使用空格
                ?各个用?名?行分隔。

??：
    --^server=<存??服?器?格>  激活指定服?器中的用?。
                                如果未指定服?器，?在 client.conf 文件
                                中的默?服?器中?行命令。
                                （使用 'cm ^help ^objectspec' 可?一?了解
                                存??服?器?格。）

== CMD_HELP_ACTIVATEUSER ==
?注：

    要激活用?，必?已事先禁用?用?。
    默?情况下，用?第一次在 Unity VCS 中?行写操作?
    会被激活。?当未超?最大用?数?，
    才会自?激活用?。

    有?停用 Unity VCS 用?的更多信息，
    ?参? 'cm ^help ^deactivateuser' 命令。

示例：

    cm ^activateuser john
    cm ^activateuser david "mary collins"
    cm ^au peter --^server=localhost:8087

== CMD_DESCRIPTION_ADD ==
向版本控制中添加?。

== CMD_USAGE_ADD ==
用法：

    cm ^add [-^R | -^r | --^recursive] [--^silent] [--^ignorefailed]
           [--^skipcontentcheck] [--^coparent] [--^filetypes=<文件>] [--^noinfo]
           [--^format=<格式字符串>] [--^errorformat=<格式字符串>]
           <?路径>[ ...]

    ?路径   要移?的一个或多个?。使用双引号 (" ") 指定
                包含空格的路径。使用空格?各个??行分隔。
                使用 * 添加当前目?的所有内容。

??：

    -^R -^r --^recursive   以??方式添加?。
    --^silent            不?示任何?出。
    --^ignorefailed      如果无法添加某一?，?添加操作将在没有
                        ?一?的情况下???行。注意：如果无法添加
                        某个目?，?不会添加目?中的内容。
    --^skipcontentcheck  如果不足以根据?展名将文件?置?
                        文本或二?制，?会将?文件?置?二?制，
                        而不是??内容来???型。??做
                        是?了在?行大量?入?提高性能。
    --^coparent          ?要添加的?的父??行?出。
    --^filetypes         要使用的 filetypes 文件。??看以下?接以了解
                        更多信息：
                        http://blog.plasticscm.com/2008/03/custom-file-types.html
    --^noinfo            不打印?度信息。
    --^format            ?索特定格式的?出消息。??看
                        示例以了解更多信息。
    --^errorformat       ?索特定格式的??消息
                        （如果有）。??看示例以了解更多信息。

== CMD_HELP_ADD ==
?注：

    添加?的要求：
    - 要添加的?的父目?必?已事先添加。

从 stdin ?取?入：

    '^add' 命令可从 stdin ?取路径。?此，???一个破折号
    "-"。
    示例：cm ^add -

    路径将一直?取到有空行?入?止。
    ??情况下允?使用??指定要添加的文件。
    示例：
      dir /S /B *.c | cm ^add -
      （在 Windows 中，添加工作区中的所有 .c 文件。）



示例：

    cm ^add file1.txt file2.txt
    （添加 'file1.txt' 和 'file2.txt' ?。）

    cm ^add c:\workspace\file.txt
    （添加路径 'c:\workspace' 中的 'file.txt' ?。）

    cm ^add -^R c:\workspace\src
    （以??方式添加 'src'。）

    cm ^add -^R *
    （以??方式添加当前目?的所有内容。）

    cm ^add -^R * --^filetypes=filetypes.conf
    （以??方式添加当前目?的所有内容，使用
    'filetypes.conf' 根据文件的?展名?文件分配?型，
    而不是??文件内容。）

    cm ^add --^coparent c:\workspace\dir\file.txt
    （将 'file.txt' 添加到源代?管理中，并?行 'dir' 的?出。）

    cm ^add -^R * --^format="ADD {0}" --^errorformat="ERR {0}"
    （以??方式添加当前目?的所有内容，
    ?于成功添加的文件，打印 '^ADD <?>'，而?于无法添加的?，
    ?打印 '^ERR <?>'。）

== CMD_USAGE_ADDIGNOREPATTERN ==
用法：

      cm ^addignorepattern <模式>[ ...]
                          [--^workspace=<工作区路径> | --^allworkspaces] [--^remove]

== CMD_DESCRIPTION_ADMIN ==
在服?器上?行管理命令。

== CMD_USAGE_ADMIN ==
用法：

    cm ^admin <命令> [??]

可用命令：

    ^readonly

    要?取有??条命令的更多信息，??行：
    cm ^admin <命令> --^usage
    cm ^admin <命令> --^help

== CMD_HELP_ADMIN ==
?注：
    只有服?器管理?才能?行管理命令。

示例：

    cm ^admin ^readonly ^enter
    cm ^admin ^readonly ^status

== CMD_DESCRIPTION_ADMIN_READONLY ==
?用/禁用服?器只?模式。

== CMD_USAGE_ADMIN_READONLY ==
用法：

    cm ^admin ^readonly (^enter | ^leave | ^status) [<服?器>]

操作：

    ^enter   服?器?入只?模式。写操作将被拒?。
    ^leave   服?器退出只?模式。
    ^status  ?示服?器只?模式状?。

??：
    服?器  在指定服?器（服?器:端口）中?行命令。（使用
            'cm ^help ^objectspec' 可?一?了解服?器?格。）
            如果未指定服?器，??命令?用于当前工作区的
            服?器。
            如果当前路径不在工作区中，??命令?用于
            client.conf 配置文件中定?的默?服?器。

== CMD_HELP_ADMIN_READONLY ==
?注：
    只有服?器管理?才能?入服?器只?模式。

示例：

    cm ^admin ^readonly ^enter diana:8086
    cm ^admin ^readonly ^leave

== CMD_DESCRIPTION_ANNOTATE ==
?示文件?一行上次修改所在的?更集及其作者。

== CMD_USAGE_ANNOTATE ==
用法：

    cm ^annotate | ^blame <?格>[ ...]
        [--^format=<格式字符串>]
        [--^ignore=(^eol | ^whitespaces | ^"eol&whitespaces" | ^none)]
        [--^dateformat=<日期格式字符串>]
        [--^encoding=<名称>]
        [--^stats]
        [--^repository=<存???格>]

    ?格        要批注的文件的?格。
                （使用 'cm ^help ^objectspec' 可?一?了解?格。）
                使用双引号 (" ") 指定包含空格的路径。

??：

    --^format        ?索特定格式的?出消息。?参?
                    “?注”以了解更多信息。
    --^ignore        ?置指定的比?方法。
                    ?参?“?注”以了解更多信息。
    --^dateformat    ?置?出格式以打印日期。
    --^encoding      指定?出??，如：utf-8。
                    ?参?位于以下网址的 MSDN 文档：
                    http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                    以?看包含受支持??及其格式的表格
                    （位于?面末尾的“名称”列中）。
    --^stats         ?示??信息。
    --^repository    指定用于?算批注的存??
                    ?格。默?情况下，此命令使用的
                    存??存?了工作区中已加?的
                    修?存??。（使用 'cm ^help ^objectspec' 可?一?
                    了解存???格。）

== CMD_HELP_ANNOTATE ==
?注：

    无法批注二?制文件。

    --^ignore ??：
        ^none                ??行尾和空格差?。
        ^eol                 忽略行尾差?。
        ^whitespaces         忽略空格差?。
        ^"eol&whitespaces"   忽略行尾和空格差?。

    --^format ??：
        此命令的?出参数如下：
        {^owner}        上次更改行的用?。
        {^rev}          行的源修??格。
        {^content}      行内容。
        {^date}         ?入行的日期。
        {^comment}      行的源修?的注?。
        {^changeset}    行的源修?的?更集。
        {^line}         文件中的行号。
        {^id}           ? ID。
        {^parentid}     ?的父? ID。
        {^rep}          ?的存??。
        {^branch}       行的源修?的分支。
        {^ismergerev}   是否在合并中?建了行的修?。

    --^dateformat:
        指定打印日期的?出格式。
        ?参?以下网址中指明的受支持格式：
        https://docs.microsoft.com/en-us/dotnet/standard/base-types/custom-date-and-time-format-strings

    --^repository:
        从?程存???索数据。?合用于分布式
        ?景。

示例：

    cm ^blame c:\workspace\src --^ignore=^"eol&whitespaces" --^encoding=utf-8
    cm ^annotate c:\workspace\file.txt --^ignore=^eol

    cm ^annotate c:\workspace\file.txt --^format="{^owner} {^date, 10} {^content}"
    （依次写入 owner 字段、一个空格、date 字段（右??）、
    一个空格以及 content 字段。）

    cm ^blame c:\workspace\file.txt --^format="{^owner, -7} {^comment} {^date}" \
        --^dateformat=yyyyMMdd
    （依次写入 owner 字段（占用 7 个空格，左??）、
    一个空格、comment 字段、?一个空格以及?尾的
    格式化日期（例如，20170329）。）

    cm ^annotate c:\workspace\file.txt --^repository=centralRep@myserver:8084

    cm ^blame ^serverpath:/src/client/checkin/Checkin.cs#^cs:73666
    （使用服?器路径??更集 73666 中?始的文件?行批注。）

== CMD_DESCRIPTION_APPLYLOCAL ==
??是否有本地更改（本地移?、本地?除和本地更改）
并?用?些更改，以便 Unity VCS ?始跟踪?些更改。

== CMD_USAGE_APPLYLOCAL ==
用法：

    cm ^applylocal | ^al [--^dependencies] [<?路径>[ ...]]
                    [--^machinereadable [--^startlineseparator=<分隔符>]
                      [--^endlineseparator=<分隔符>] [--^fieldseparator=<分隔符>]]

??：

    --^dependencies        将本地更改依??添加到要?用
                          的?。
    ?路径             要?用的?。使用空格?各个路径?行
                          分隔。使用双引号 (" ") 指定包含空格
                          的路径。
    --^machinereadable     以易于解析的格式?出?果。
    --^startlineseparator  与 '--^machinereadable' ?志?合使用，指定
                          行?如何??。
    --^endlineseparator    与 '--^machinereadable' ?志?合使用，指定
                          行?如何?尾。
    --^fieldseparator      与 '--^machinereadable' ?志?合使用，指定
                          ?如何分隔字段。

== CMD_HELP_APPLYLOCAL ==
?注：

    如果未指定 --^dependencies 和 <?路径>，??操作?及
    工作区中的所有本地更改。
    始?以??方式从?定路径?用。

示例：

    cm ^applylocal foo.c bar.c

    cm ^applylocal .
    （?用当前目?中的所有本地更改。）

    cm ^applylocal
    （?用工作区中的所有本地更改。）

    cm ^applylocal --^machinereadable
    （?用工作区中的所有本地更改，并以易于解析的
    ?化格式打印?果。）

    cm ^applylocal --^machinereadable --^startlineseparator=">" \
      --^endlineseparator="<" --^fieldseparator=","
    （?用工作区中的所有本地更改，并以易于解析的
    ?化格式打印?果，以指定的字符串作?行的??和?尾以及
    ?各字段?行分隔。）

== CMD_DESCRIPTION_ARCHIVE ==
将数据存档在外部存?中。

== CMD_USAGE_ARCHIVE ==
用法：

    cm ^archive | ^arch <修??格>[ ...][-^c | --^comment=<注?字符串>]
                        [--^file=<基?文件>]
    （从存??中提取数据并将?些数据存?在外部存?中。）

    cm ^archive | ^arch <修??格>[ ...]--^restore
    （将以前存档的修??原到存??中。）

    修??格             一个或多个修??格。可通? "-" 修?符从 STDIN
                        ?行?取。（使用 'cm ^help ^objectspec' 可
                        ?一?了解修??格。）
    --^restore           从生成的存档文件中?原以前存档的
                        数据。

??：

    -^c | --^comment      在要?建的存档存?文件中?置注?。
    --^file              新存档数据文件的名称前?和
                        （可?）路径。

== CMD_HELP_ARCHIVE ==
?注：

    此命令从存??数据?中提取数据并将?些数据存?在
    外部存?中，从而?省数据?空?。
    此命令?可以将以前存档的修??原到
    存??数据?中 (--^restore)。

    使用 'cm ^help ^objectspec' 可了解如何指定修??格。

    ?行此命令的用?必?是 Unity VCS 服?器管理?
    （存??服?器所有者）才能完成操作。

    来自指定修?中的?个数据段都将存?在
    不同的文件中，且文件名以 --^file
    参数定?的???。此参数可以包含完整路径?，其中包括
    未来存档文件的前?，也可以?包含此前??。

    存档后可以通???方式??来自指定修?中的
    数据：

    - 从客?端：客?端将??数据是否已存档，
      并提示用??入文件的位置。
      用?可以通??建一个名? externaldata.conf 的
      文件（在?准配置文件位置，使用?用于
      client.conf 文件的相同??）来配置外部数据位置，
      其中包含已存档数据所在的路径。

    - 从服?器：通???方式，用?将不必知道数据
      是否已存档，因??求将以透明的方式由服?器
      解析。?此，管理?将在服?器目?中?建一个
      名? externaldata.conf 的文件，并在?文件中填充
      已存档的卷所在的路径。

    要取消存档（?原）一个修?（或一?修?），必?能?
    从客?端??已存档的文件。因此，无法取消存档
    服?器正在解析的数据（方法 2），原因是客?端
    无法将数据???已存档。如果使用了方法 2，
    ?了成功取消存档，管理?必?首先??
    externaldata.conf 服?器文件，以便?除?必?取消存档的
    已存档文件的???限。

    ?置 PLASTICEDITOR ?境?量可指定用于?入注?的
    ??器。

示例：

    cm ^archive bigfile.zip#^br:/main
    （在分支 'main' 中存档 'bigfile.zip' 的最后一个修?。）

    cm ^archive ^rev:myfile.pdf#^cs:2 -^c="大型 PDF 文件" --^file=c:\arch_files\arch
    （将?更集 2 中 myfile.pdf 的修?存档在 'c:\archived_files'
    文件?中。存档的文件名将以 'arch' ??（例如 arch_11_56）。）

    cm ^find "^revs ^where ^size > 26214400" --^format="{^item}#{^branch}" \
      --^nototal | cm ^archive --^comment="volume00" --^file="volume00" -
    （将所有大于 25Mb 的文件存档在名称 'volume00' ??的
    文件中。）

    cm ^find "^revs ^where ^size > 26214400 ^and ^archived='true'" \
      --^format="{^item}#{^branch}" --^nototal | cm ^archive --^restore
    （?原所有大于 25Mb 的存档文件。）

== CMD_DESCRIPTION_ATTRIBUTE ==
允?用?管理属性。

== CMD_USAGE_ATTRIBUTE ==
用法：

    cm ^attribute | ^att <命令> [??]

命令：

    ^create | ^mk
    ^delete | ^rm
    ^set
    ^unset
    ^rename
    ^edit

    要?取有??条命令的更多信息，??行：
    cm ^attribute <命令> --^usage
    cm ^attribute <命令> --^help

== CMD_HELP_ATTRIBUTE ==
示例：

    cm ^attribute ^create 状?
    cm ^attribute ^set ^att:status ^br:/main/SCM105 未完成
    cm ^attribute ^unset ^att:status ^br:/main/SCM105
    cm ^attribute ^delete ^att:status
    cm ^attribute ^rename ^att:status "?建状?"
    cm ^attribute ^edit ^att:status "CI 管道中任?的状?"

== CMD_DESCRIPTION_CHANGELIST ==
?更改列表中的待定更改?行分?。

== CMD_USAGE_CHANGELIST ==
用法：

    a) 管理更改列表?象：

       cm ^changelist | ^clist [--^symlink]
       （?示所有更改列表。）

       cm ^changelist | ^clist ^add <更改列表名称>
          [<更改列表描述>] [--^persistent | --^notpersistent] [--^symlink]
       （?建更改列表。）

       cm ^changelist | ^clist ^rm <更改列表名称> [--^symlink]
       （?除所?的更改列表。如果此更改列表包含待定更改，
       ?些更改将移至 ^default 更改列表。）

       cm ^changelist | ^clist ^edit <更改列表名称> [<操作名称> <操作?>]
                             [--^persistent | --^notpersistent] [--^symlink]
       （??所?的更改列表。）

    b) 管理?定更改列表的内容：

       cm ^changelist | ^clist <更改列表名称> (^add | ^rm) <路径名称>[ ...]
                             [--^symlink]
       （通?添加 ('^add') 或?除 ('^rm') 与?定路径名称匹配的更改
       来添加所?的更改列表。使用空格
       ?各个路径名称?行分隔。使用双引号 (" ") 指定包含空格的
       路径。路径的状?必?? '^Added' 或 '^Checked-out'。）

??：

    更改列表名称          更改列表的名称。
    更改列表描述          更改列表的描述。
    操作名称         ?? '^rename' 或 '^description' 来??
                        更改列表。
    操作?        在??更改列表??用新名称或
                        新描述。
    --^persistent        即使更改列表的内容已被?入或?原，
                        更改列表也会保留在工作区中。
    --^notpersistent     （默??）即使更改列表的内容已被?入
                        或?原，更改列表也不会保留在
                        工作区中。
    --^symlink           将操作?用于符号?接而不是
                        目?。

== CMD_HELP_CHANGELIST ==
?注：

    '^changelist' 命令会?理工作区待定更改列表以及更改列表中
    包含的更改。

示例：

    cm ^changelist
    （?示当前工作区更改列表。）

    cm ^changelist ^add 配置更改 "dotConf 文件" --^persistent
    （?建一个名? '配置更改' 和描述? 'dotConf
    文件' 的新更改列表，?更改列表在待定更改列表被?入
    或?原后将在当前工作区中持久保留。）

    cm ^changelist ^edit 配置更改 ^rename 配置文件 --^notpersistent
    （??名? '配置更改' 的更改列表，并将?更改列表重命名?
    '配置文件'。此外?会将更改列表??“非持久性”。）
        
    cm ^changelist ^edit 配置更改 --^notpersistent
    （??名? '配置更改' 的更改列表，并将?更改列表??“非持久性”。）

    cm ^changelist ^rm 配置文件
    （从当前工作区中?除待定更改列表 '配置文件'。）

    cm ^changelist 配置文件 ^add foo.conf
    （将文件 'foo.conf' 添加到 '配置文件' 更改列表中。）

    cm ^changelist 配置文件 ^rm foo.conf readme.txt
    （从 '配置文件' 更改列表中?除文件 'foo.conf' 和 'readme.txt'，
    并将?些文件移至系?默?更改列表。）

== CMD_DESCRIPTION_CHANGESET ==
??更集?行高?操作。

== CMD_USAGE_CHANGESET ==
用法：

    cm ^changeset <命令> [??]

命令：

    ^move        | ^mv
    ^delete      | ^rm
    ^editcomment | ^edit

    要?取有??条命令的更多信息，??行：
    cm ^changeset <命令> --^usage
    cm ^changeset <命令> --^help

== CMD_HELP_CHANGESET ==
示例：

    cm ^changeset ^move ^cs:15@myrepo ^br:/main/scm005@myrepo
    cm ^changeset ^delete ^cs:2b55f8aa-0b29-410f-b99c-60e573a309ca@devData

== CMD_DESCRIPTION_CHANGESET_EDITCOMMENT ==
修改?更集的注?。

== CMD_USAGE_CHANGESET_EDITCOMMENT ==
用法：

    cm ^changeset ^editcomment | ^edit <?更集?格> <新注?>

??：

    ?更集?格            要修改注?的目??更集。
                        （使用 'cm ^help ^objectspec' 可?一?了解?更集
                        ?格。）
    新注?         要添加到目??更集的
                        新注?。

== CMD_HELP_CHANGESET_EDITCOMMENT ==
?注：

    - 目??更集?格必?有效。

示例：

    cm ^changeset ^editcomment ^cs:15@myrepo "我忘了添加?入??信息"
    cm ^changeset ^edit ^cs:cb11ecdb-1aa9-4f11-8698-dcab14e5885a \
         "此注?文本将替代先前的注?文本。"

== CMD_DESCRIPTION_CHANGESET_MOVE ==
将?更集及其所有后代移?到?一个分支。

== CMD_USAGE_CHANGESET_MOVE ==
用法：

    cm ^changeset ^move | ^mv <?更集?格> <分支?格>

??：

    ?更集?格            要移?到?一个分支的第一个?更集。All
                        同一分支中的所有后代?更集也将作?
                        ?命令的目?。
                        （使用 'cm ^help ^objectspec' 可?一?了解?更集
                        ?格。）
    分支?格          存?目??更集的目?
                        分支。目?分支需要?空或不存在；
                        如果目?分支不存在，?命令将?建
                        目?分支。
                        （使用 'cm ^help ^objectspec' 可?一?了解分支
                        ?格。）

== CMD_HELP_CHANGESET_MOVE ==
?注：

    - 目??更集?格必?有效。
    - 目?分支必??空或不存在。
    - 如果目?分支不存在，?会?建目?分支。
    - 合并?接将保持不?，因?分支不会影?它?。

示例：

    cm ^changeset ^move ^cs:15@myrepo ^br:/main/scm005@myrepo
    cm ^changeset ^move ^cs:cb11ecdb-1aa9-4f11-8698-dcab14e5885a ^br:/hotfix/TL-352

== CMD_DESCRIPTION_CHANGESET_DELETE ==
从存??中?除?更集。

== CMD_USAGE_CHANGESET_DELETE ==
用法：

    cm ^changeset ^delete | ^rm <?更集?格>

??：

    ?更集?格           要?除的目??更集。必??足
                       某些特定条件。?参?“?注”以了解更多信息。
                       （使用 'cm ^help ^objectspec' 可?一?了解?更集
                        ?格。）

== CMD_HELP_CHANGESET_DELETE ==
?注：

    - 目??更集必?是其分支中的最后一个?更集。
    - 目??更集不能是任何其他?更集的父?。
    - 目??更集既不能作?合并?接的源，也不能
      在?隔合并中作?源。
    - 不得将任何???用于目??更集。
    - 目??更集不能是根?更集 ('^cs:0')

示例：

    cm ^changeset ^rm ^cs:4525@myrepo@myserver
    cm ^changeset ^delete ^cs:cb11ecdb-1aa9-4f11-8698-dcab14e5885a

== CMD_DESCRIPTION_CHANGEUSERPASSWORD ==
更改用?密? (UP)。

== CMD_USAGE_CHANGEUSERPASSWORD ==
用法：

    cm ^changepassword | ^passwd

== CMD_HELP_CHANGEUSERPASSWORD ==
?注：

    ?当安全性配置? UP（用?/密?）?，
    此命令才可用。?参?《管理指南》以了解更多信息。
    需要新旧密?。

示例：

    cm ^passwd

== CMD_DESCRIPTION_CHECKCONNECTION ==
??与服?器的?接。

== CMD_USAGE_CHECKCONNECTION ==
用法：

      cm ^checkconnection | ^cc

== CMD_HELP_CHECKCONNECTION ==
?注：

    - 此命令返回一条消息，指示是否存在与配置的
      Unity VCS 服?器之?的有效?接。
    - 此命令??配置的用?是否有效。此外
      ?会??与服?器的版本兼容性。

== CMD_DESCRIPTION_CHECKDB ==
??存??的完整性。

== CMD_USAGE_CHECKDB ==
用法：

    cm ^checkdatabase | ^chkdb [<存??服?器?格> | <存???格>]

使用 'cm ^help ^objectspec' 可?一?了解存??服?器和存???格。

== CMD_HELP_CHECKDB ==
?注：

    - 如果未指定存??服?器?格和存???格，
      ?将在 client.conf 文件内指定的服?器中?行??。

示例：

    cm ^checkdatabase ^repserver:localhost:8084
    cm ^chkdb ^rep:default@localhost:8084

== CMD_DESCRIPTION_CHECKIN ==
将更改存?在存??中。

== CMD_USAGE_CHECKIN ==
用法：

    cm ^checkin | ^ci [<?路径>[ ...]]
        [-^c=<注?字符串> | -^commentsfile=<注?文件>]
        [--^all|-^a] [--^applychanged] [--^private] [--^update] [--^symlink]
        [--^noshowchangeset]
        [--^machinereadable [--^startlineseparator=<分隔符>]
          [--^endlineseparator=<分隔符>] [--^fieldseparator=<分隔符>]]

??：

    ?路径             要?入的?。使用双引号 (" ")
                          指定包含空格的路径。使用空格
                          ?各个?路径?行分隔。
                          使用 . 将?入?用到当前目?。
    -^c                    将指定的注??用于在?入操作中?建的
                          ?更集。
    -^commentsfile         将指定文件中的注??用于在?入操作中
                          ?建的?更集。
    --^all | -^a            ?包括在?定路径上?行了本地更改、移?
                          和?除的?。
    --^applychanged        将?入操作?用于在工作区中
                          ??到的已更改?以及
                          ?出的?。
    --^private             ?包括在工作区中??到的
                          私有?。
    --^update              自??理更新合并
                          （如果最??生此行?）。
    --^symlink             将?入操作?用于符号?接而不是
                          目?。
    --^noshowchangeset     不打印?果?更集。
    --^machinereadable     以易于解析的格式?出?果。
    --^startlineseparator  与 '--^machinereadable' ?志?合使用，指定
                          行?如何??。
    --^endlineseparator    与 '--^machinereadable' ?志?合使用，指定
                          行?如何?尾。
    --^fieldseparator      与 '--^machinereadable' ?志?合使用，指定
                          ?如何分隔字段。

== CMD_HELP_CHECKIN ==
?注：

    - 如果未指定 <?路径>，??入操作?及
      工作区中的所有待定更改。
    - ?入操作始?以??方式从?定路径?用。
    - 要?入?，必??足以下条件：
      - ?必?受源代?管理。
      - 如果是私有?（不受源代?管理），?必?有 --^private
        ?志才能?入??。
      - 必??出?。
      - 如果已更改但未?出?，?除非 <?路径> 是目?
        或包含通配符 ('*')，否?不需要
        --^applychanged ?志。

    修?内容必?与以前的修?不同才能
    ?入。

    ?置 PLASTICEDITOR ?境?量可指定用于?入注?的
    ??器。

从 stdin ?取?入：

    '^checkin' 命令可从 stdin ?取路径。?此，???一个
    破折号 "-"。
    示例：cm ^checkin -

    路径将一直?取到有空行?入?止。
    ??情况下允?使用??指定要?入的文件。
    示例：
      dir /S /B *.c | cm ^checkin --^all -
      （在 Windows 中，?入工作区中的所有 .c 文件。）

示例：

    cm ^checkin file1.txt file2.txt
    （?入已?出的文件 'file1.txt' 和 'file2.txt'。）

    cm ^checkin .-^commentsfile=mycomment.txt
    （?入当前目?并在 'mycomment.txt' 文件中
    ?置注?。）

    cm ^checkin ?接 --^symlink
    （?入 '?接' 文件而不是目?；?用于 UNIX
    ?境。）

    cm ^ci file1.txt -^c="我的注?"
    （?入 'file1.txt' 并包含注?。）

    cm ^status --^short --^compact --^changelist=pending_to_review | cm ^checkin -
    （列出名? 'pending_to_review' 的更改列表中的路径，并将此列表
    重定向到 checkin 命令的?入。）

    cm ^ci .--^machinereadable
    （?入当前目?，并以易于解析的?化格式
    打印?果。）

    cm ^ci .--^machinereadable --^startlineseparator=">" --^endlineseparator="<" --^fieldseparator=","
    （?入当前目?，并以易于解析的?化格式
    打印?果，以指定的字符串作?行的??和?尾以及
    ?各字段?行分隔。）

== CMD_DESCRIPTION_CHECKOUT ==
将文件???修改就?。

== CMD_USAGE_CHECKOUT ==
用法：

    cm ^checkout | ^co [<?路径>[ ...]] [-^R | -^r | --^recursive]
                     [--^format=<格式字符串>]
                     [--^errorformat=<格式字符串>] [--^resultformat=<格式字符串>]
                     [--^silent] [--^symlink] [--^ignorefailed]
                     [--^machinereadable [--^startlineseparator=<分隔符>]
                       [--^endlineseparator=<分隔符>] [--^fieldseparator=<分隔符>]]

??：

    ?路径             要?出的?。使用双引号 (" ")
                          指定包含空格的路径。使用空格
                          ?各个?路径?行分隔。
                          使用 . 将?出?用到当前目?。
    -^R                    以??方式?出文件。
    --^format              ?索特定格式的?出?度
                          消息。??看示例以了解更多信息。
    --^errorformat         ?索特定格式的??消息
                          （如果有）。??看示例以了解更多信息。
    --^resultformat        ?索特定格式的?出?果
                          消息。??看示例以了解更多信息。
    --^silent              不?示任何?出。
    --^symlink             将?出操作?用于符号?接而不是
                          目?。
    --^ignorefailed        如果无法?定某?（无法?行独占?出），
                          ??出操作将在没有?一?的情况下
                          ???行。
    --^machinereadable     以易于解析的格式?出?果。
    --^startlineseparator  与 '--^machinereadable' ?志?合使用，指定
                          行?如何??。
    --^endlineseparator    与 '--^machinereadable' ?志?合使用，指定
                          行?如何?尾。
    --^fieldseparator      与 '--^machinereadable' ?志?合使用，指定
                          ?如何分隔字段。

== CMD_HELP_CHECKOUT ==
?注：

    要?出?，必??足以下条件：
    - ?必?受源代?管理。
    - 必??入?。

    如果在服?器上配置了?（存在 lock.conf），??次
    在某条路径上?行?出?，Unity VCS 都会??是否符合??，
    如果符合，?路径将?于独占?出（?定）状?，以便任何其他人
    都无法同??行?出。
    可使用 'cm ^lock ^list' ?取服?器中的所有?。
    ?参?《管理?指南》以了解更多信息：
    https://www.plasticscm.com/download/help/adminguide

    格式字符串将占位符 '{0}' 替??要?出的
    ?的路径。??看示例以了解具体用法。

从 stdin ?取?入：

    '^checkout' 命令可从 stdin ?取路径。?此，???一个
    破折号 "-"。
    示例：cm ^checkout -

    路径将一直?取到有空行?入?止。
    ??情况下允?使用??指定要?出的文件。
    示例：
      dir /S /B *.c | cm ^checkout -
      （在 Windows 中，?出工作区中的所有 .c 文件。）

示例：

    cm ^checkout file1.txt file2.txt
    （?出 'file1.txt' 和 'file2.txt' 文件。）

    cm ^co *.txt
    （?出所有文本文件。）

    cm ^checkout .
    （?出当前目?。）

    cm ^checkout -^R c:\workspace\src
    （以??方式?出 'src' 文件?。）

    cm ^co file.txt --^format="正在?出? {0}"
        --^errorformat="?出? {0} ?出?" /
        --^resultformat="? {0} 已?出"
    （?出 'file.txt'，并使用指定的格式字符串
    ?示操作的?度、?果和??。）

    cm ^checkout ?接 --^symlink
    （?出 '?接' 文件而不是?出到目?；?用于 UNIX
    ?境。）

    cm ^checkout .-^R --^ignorefailed
    （以??方式?出当前文件?，忽略无法?出的
    文件。）

    cm ^co .--^machinereadable --^startlineseparator=">"
    （?出当前目?，并以易于解析的?化格式打印?果，
    以指定的字符串作?行的??。）

== CMD_DESCRIPTION_CHECKSELECTORSYNTAX ==
????器的?法。

== CMD_USAGE_CHECKSELECTORSYNTAX ==
用法：

    cm ^checkselectorsyntax | ^css --^file=<??器文件>
    （????器文件?法。）

    ^cat <??器文件> | cm ^checkselectorsyntax | ^css -
    （?用于 Unix。在?准?入中????器文件。）

    ^type <??器文件> | cm ^checkselectorsyntax | ^css -
    （?用于 Windows。在?准?入中????器文件。）


    --^file     要从中?取??器的文件。

== CMD_HELP_CHECKSELECTORSYNTAX ==
?注：

    此命令在文件或?准?入中?取??器，并??
    ??器是否?有效?法。如果?法???果?失?，?会将原因
    打印在?准?出中。

示例：

    cm ^checkselectorsyntax --^file=myselector.txt
    （?? 'myselector.txt' 文件的?法。）

    ^cat myselector.txt | cm ^checkselectorsyntax
    （在?准?入中?? 'myselector.txt' 的?法。）

== CMD_DESCRIPTION_CHANGEREVISIONTYPE ==
更改某个?的修??型（二?制或文本）。

== CMD_USAGE_CHANGEREVISIONTYPE ==
用法：

    cm ^changerevisiontype | ^chgrevtype | ^crt <?路径>[ ...]--^type=(^bin | ^txt)

    ?路径           要更改修??型的?。使用双引号 (" ")
                        指定包含空格的路径。使用空格
                        ?各个?路径?行分隔。
    --^type              目?修??型。?? '^bin' 或 '^txt'。

== CMD_HELP_CHANGEREVISIONTYPE ==
?注：

    此命令只能?用于文件，而不能?用于目?。
    指定的?型必?是系?支持的?型：'^bin' 或 '^txt'（二?制
    或文本）。

示例：

    cm ^changerevisiontype c:\workspace\file.txt --^type=^txt
    （将 'file.txt' 修??型更改?文本。）

    cm ^chgrevtype comp.zip "image file.jpg" --^type=^bin
    （将 'comp.zip' 和 "image file.jpg" 修??型更改?二?制。）

    cm ^crt *.* --^type=^txt
    （将所有文件的修??型更改?文本。）

== CMD_DESCRIPTION_TRIGGER_EDIT ==
??触?器。

== CMD_USAGE_TRIGGER_EDIT ==
用法：

    cm ^trigger | ^tr ^edit <子?型_?型> <位置?号>
                         [--^position=<新位置>]
                         [--^name=<新名称>] [--^script=<脚本路径>]
                         [--^filter=<??器字符串>] [--^server=<存??服?器?格>]

    子?型_?型        触?器?行和触?器操作。
                        ?入 'cm ^showtriggertypes' 可?看触?器?型
                        列表。
    位置?号     要修改的触?器占用位置。

??：

    --^position          指定触?器的新位置。
                        此位置不能正在由相同?型的?一个
                        触?器使用。
    --^name              指定触?器的新名称。
    --^script            指定触?器脚本的新?行路径。
                        如果脚本以 "^webtrigger " ??，???
                        ?是 Web 触?器。?参?“?注”以了解
                        更多??信息。
    --^filter            ???与指定??条件匹配的?。
    --^server            修改指定服?器上的触?器。
                        如果未指定服?器，?在客?端上配置的
                        服?器上?行命令。
                        （使用 'cm ^help ^objectspec' 可?一?了解服?器
                        ?格。）

== CMD_HELP_TRIGGER_EDIT ==
?注：

    Web 触?器：?建 Web 触?器的方法是?入 "^webtrigger <目?-URI>"
    作?触?器命令。在??情况下，触?器将??指定的 URI
    ?行 POST ??，其中，?求主体包含?有触?器
    ?境?量的 JSON 字典，?有一个指向字符串数?的
    固定 INPUT ?。

示例：

    cm ^trigger ^edit ^after-setselector 6 --^name="Backup2 管理器" --^script="/new/path/al/script"
    cm ^tr ^edit ^before-mklabel 7 --^position=4 --^server=myserver:8084
    cm ^trigger ^edit ^after-add 2 --^script="^webtrigger http://myserver.org/api"

== CMD_DESCRIPTION_CODEREVIEW ==
?建、??或?除代???。

== CMD_USAGE_CODEREVIEW ==
用法：

    cm ^codereview <?格> <??> [--^status=<状?名称>]
                [--^assignee=<用?名>] [--^format=<格式字符串>]
                [--^repository=<存???格>]
    （?建代???。）

    cm ^codereview -^e <ID> [--^status=<状?名称>] [--^assignee=<用?名>]
                [--^repository=<存???格>]
    （??代???。）

    cm ^codereview -^d <ID> [ ...][--^repository=<存???格>]
    （?除一个或多个代???。）


    ?格                可以是?更集?格或分支?格。此?格
                        将作?新代???的目?。（使用 
                        'cm ^help ^objectspec' 可?一?了解?更集?格
                        或分支?格。）
    ??               用作新代???的??的
                        文本字符串。
    ID                  代?????号。也可以使用
                        GUID。

??：

    -^e                  ???有代???的参数。
    -^d                  ?除一个或多个?有代???。使用
                        空格?各个代??? ID ?行分隔。
    --^status            ?置代???的新状?。?参?“?注”
                        以了解更多信息。
    --^assignee          ?置代???的新被分派人。
    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。
    --^repository        ?置要用作默?存??的存??。（使用
                        'cm ^help ^objectspec' 可?一?了解存??
                        ?格。）

== CMD_HELP_CODEREVIEW ==
?注：

    此命令允?用?管理代???：??更集或分支
    ?建、??和?除代???。

    要?建新的代???，必?提供?更集/分支?格和
    ??。也可以?置初始状?和被分派人。?果将返回
    一个 ID（如果?求返回 GUID，?返回 GUID）。

    要??或?除?有代???，必?提供目?代?
    ?? ID（或 GUID）。如果没有??，?不会?示任何消息。

    状?参数只能是以下?之一：^"Under review"
    （默??）、^"Reviewed" 或 ^"Rework required"。

    repository 参数可用于?置默?的工作
    存??。当用?想要在其他服?器上而不是在与当前工作区
    ??的服?器上管理???，或者根本没有
    当前工作区?，此参数很有用。

    ?出格式自定?：

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：
        {0}             ID
        {1}             GUID

    ?注意，'--^format' 参数?在?建新的代?
    ???生效。

示例：

    cm ^codereview ^cs:1856@myrepo@myserver:8084 "我的代???" --^assignee=dummy
    cm ^codereview ^br:/main/task001@myrepo@myserver:8084 "我的代???" \
    --^status=^"Rework required" --^assignee=newbie --^format="{^id} -> {^guid}"

    cm ^codereview 1367 -^e --^assignee=新的被分派人
    cm ^codereview -^e 27658884-5dcc-49b7-b0ef-a5760ae740a3 --^status=已??

    cm ^codereview -^d 1367 --^repository=myremoterepo@myremoteserver:18084
    cm ^codereview 27658884-5dcc-49b7-b0ef-a5760ae740a3 -^d

== CMD_DESCRIPTION_CRYPT ==
?密??行加密。

== CMD_USAGE_CRYPT ==
用法：

    cm ^crypt <我的密?>

    我的密?          要加密的密?。

== CMD_HELP_CRYPT ==
?注：

    此命令用于加密作?参数??的?定密?。
    旨在?配置文件中的密??行加密并提高
    安全性。

示例：

    cm ^crypt dbconfpassword -> ENCRYPTED: encrypteddbconfpassword
    （?数据?配置文件 'db.conf' 中的密??行加密。）

== CMD_DESCRIPTION_DEACTIVATEUSER ==
停用?可的用?。

== CMD_USAGE_DEACTIVATEUSER ==
用法：

    cm ^deactivateuser | ^du <用?名>[ ...][--^server=<名称:端口>]
                           [--^nosolveuser]

    用?名            要停用的一个或多个用?名。使用空格
                        ?各个用?名?行分隔。
                        如果是 SID，?需要 '--^nosolveuser'。

??：

    --^server            停用指定服?器上的用?。
                        如果未指定服?器，?在客?端上配置的
                        服?器上?行命令。
    --^nosolveuser       使用此???，?命令将不会??身???系?上
                        是否存在相?的用?名。而且
                        <用?名> 必?是用? SID。

== CMD_HELP_DEACTIVATEUSER ==
?注：

    此命令将用??置?非活?状?，从而禁止?用?使用
    Unity VCS。

    有?激活 Unity VCS 用?的更多信息，?参? 'cm ^activateuser'
    命令。

    此命令??用?是否在基?身???系?（例如 ActiveDirectory、
    LDAP、用?/密?...）上存在。
    要?制停用在身???系?上不再存在的
    用?，可以使用 '--^nosolveuser' ??。

示例：

    cm ^deactivateuser john
    cm ^du peter "mary collins"
    cm ^deactivateuser john --^server=myserver:8084
    cm ^deactivateuser S-1-5-21-3631250224-3045023395-1892523819-1107 --^nosolveuser

== CMD_DESCRIPTION_DIFF ==
?示文件、?更集和??之?的差?。

== CMD_USAGE_DIFF ==
用法：

    cm ^diff <?更集?格> | <???格> | <?置?格> [<?更集?格> | <???格> | <?置?格>]
            [<路径>]
            [--^added] [--^changed] [--^moved] [--^deleted]
            [--^repositorypaths] [--^download=<下?路径>]
            [--^encoding=<名称>]
            [--^ignore=(^eol | ^whitespaces | ^"eol&whitespaces" | ^none)]
            [--^clean]
            [--^format=<格式字符串>] [--^dateformat=<格式字符串>]

        ?示“源”?更集或?置集与“目?”?更集或?置集
        之?的差?。可使用?更集或???格
        来指定?更集。
        如果?出了?个?格，?第一个是差?比?的“源”，
        第二个是“目?”。
        如果??出一个?格，?“源”将是指定“目?”的
        父?更集。
        如果指定了可?路径，?将??“差?比?窗口”以?示
        ?文件的?个修?之?的差?。

    cm ^diff <修??格1> <修??格2>

        ?示一?修?之?的差?。?些差?
        ?示在“差?比?窗口”中。指定的第一个修?将
        ?示在左?。

    cm ^diff <分支?格> [--^added] [--^changed] [--^moved] [--^deleted]
            [--^repositorypaths] [--^download=<下?路径>]
            [--^encoding=<名称>]
            [--^ignore=(^eol | ^whitespaces | ^"eol&whitespaces" | ^none)]
            [--^clean]
            [--^format=<格式字符串>] [--^dateformat=<格式字符串>]
            [--^fullpaths | --^fp]

        ?示分支差?。

    （使用 'cm ^help ^objectspec' 可?一?了解?格。）

??：

    --^added             ?打印包含已添加到存??的?
                        的差?。
    --^changed           ?打印包含已更改的?
                        的差?。
    --^moved             ?打印包含已移?或已重命名的?
                        的差?。
    --^deleted           ?打印包含已?除的?
                        的差?。

                        如果未指定 '--^added'、'--^changed'、'--^moved' 或 '--^deleted'，
                        ??命令将打印所有差?。
                            '^A' 表示已添加的?。
                            '^C' 表示已更改的?。
                            '^D' 表示已?除的?。
                            '^M' 表示已移?的?。左?的?是原始?，
                              右?是目??。

    --^repositorypaths   打印存??路径而不是工作区路径。
                        （此??将覆盖 '--^fullpaths' ??。）
    --^download          将差?内容存?在指定的?出
                        路径中。
    --^encoding          指定?出??，如：utf-8。
                        ?参?位于以下网址的 MSDN 文档：
                        http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                        以?看包含受支持??及其格式的表格
                        （位于?面末尾的“名称”列中）。
    --^ignore            ?置指定的比?方法。
                        ?参?“?注”以了解更多信息。
    --^clean             不考?由于合并而?生的
                        差?，?考????入所?建的
                        差?。
    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。
    --^dateformat        用于?出日期的格式。
    --^fullpaths, --^fp   如果可能，?制打印文件和目?的完整
                        工作区路径。

== CMD_HELP_DIFF ==
?注：

    比?方法：
        ^eol                 忽略行尾差?。
        ^whitespaces         忽略空格差?。
        ^"eol&whitespaces"   忽略行尾和空格差?。
        ^none                ??行尾和空格差?。

    此命令接受格式字符串以?示?出。
    此命令的参数如下：
        {^path}              ?路径。
        {^date}              更改日期/??。
        {^owner}             更改作者。
        {^revid}             在差?比?中被??目?的修?的
                            修? ID。
        {^parentrevid}       在差?比?中被??目?的修?的
                            父?修? ID。
        {^baserevid}         在差?比?中被??源的修?的
                            修? ID。
        {^srccmpath}         移??（移?操作）之前的服?器路径。
        {^dstcmpath}         移??（移?操作）之后的服?器路径。
        {^type}              ??型：
            ^D   目?，
            ^B   二?制文件，
            ^F   文本文件，
            ^S   符号?接，
            ^X   Xlink。
        {^repository}        ?的存??。
        {^status}            ?状?：
            ^A   已添加，
            ^D   已?除，
            ^M   已移?，
            ^C   已更改。
        {^fsprotection}      ?示??限 (Linux/Mac chmod)。
        {^srcfsprotection}   ?示父修???限。
        {^newline}           插入一个新行。

有? '^revid' 的注意事?：
    ?于已添加的?，'^baserevid' 和 '^parentrevid' 将? -1，因?
    在??情况下不存在以前的修?。
    ?于已?除的?，'^revid' 是源修?的 ID，而
    '^baserevid' 将? -1，因?没有目?修?。
    ?于 Xlink，'^baserevid' 和 '^parentrevid' 都始?? -1。

示例：

  比?分支：

    cm ^diff ^br:/main/task001
    cm ^diff ^br:/main/task001 \doc\readme.txt

  比??更集?：

    cm ^diff 19
    cm ^diff 19 25
    cm ^diff ^cs:19 ^cs:25 --^format="{^path} {^parentrevid}"
    cm ^diff ^cs:19 ^cs:23 --^format="{^date} {^path}" --^dateformat="yy/dd/MM HH:mm:ss"
    cm ^diff ^cs:19 ^cs:23 --^changed
    cm ^diff ^cs:19 ^cs:23 --^repositorypaths
    cm ^diff ^cs:19 ^cs:23 --^download="D:\temp"
    cm ^diff ^cs:19 ^cs:23 --^clean
    cm ^diff ^cs:19 ^cs:23 \doc\readme.txt

  比????：

    cm ^diff ^lb:FirstReleaseLabel ^lb:SecondReleaseLabel
    cm ^diff ^lb:tag_193.2 ^cs:34214
    cm ^diff ^cs:31492 ^lb:tag_193.2

  比??置?：

    cm ^diff ^sh:2
    cm ^diff ^sh:2 ^sh:4

  比?修??格：
    cm ^diff ^rev:readme.txt#^cs:19 ^rev:readme.txt#^cs:20
    cm ^diff ^serverpath:/doc/readme.txt#^cs:19@myrepo \
        ^serverpath:/doc/readme.txt#^br:/main@myrepo@localhost:8084

== CMD_DESCRIPTION_DIFFMETRICS ==
?示?个修?之?的差?比?指?。

== CMD_USAGE_DIFFMETRICS ==
用法：

    cm ^diffmetrics | ^dm <修??格 1> <修??格 2> [--^format=<格式字符串>]
                        [--^encoding=<名称>]
                        [--^ignore=(^eol | ^whitespaces | ^"eol&whitespaces" | ^none)]

    修??格           用于比?的修?。
                      （使用 'cm ^help ^objectspec' 可?一?了解修??格。）

??：

    --^format          ?索特定格式的?出消息。?参?
                      “?注”以了解更多信息。
    --^encoding        指定?出??，如：utf-8。
                        ?参?位于以下网址的 MSDN 文档：
                        http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                        以?看包含受支持??及其格式的表格
                        （位于?面末尾的“名称”列中）。
    --^ignore          ?置指定的比?方法。
                      ?参?“?注”以了解更多信息。

== CMD_HELP_DIFFMETRICS ==
?注：

    指??：已更改、添加和?除的行数。

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：
        {0}             已更改的行数。
        {1}             已添加的行数。
        {2}             已?除的行数。

示例：

    cm ^diffmetrics file.txt#^cs:2 file.txt#^br:/main/scm0211 \
    --^format="已更改 {0} 行，已添加 {1} 行，已?除 {2} 行。"
    （?索已格式化的差?比?指??果。）

    cm ^dm file.txt#^cs:2 file.txt#^cs:3 --^encoding=utf-8 --^ignore=^whitespaces

== CMD_DESCRIPTION_FASTEXPORT ==
以快速?出格式?出存??。

== CMD_USAGE_FASTEXPORT ==
用法：

    cm ^fast-export | ^fe <存???格> <快速?出文件>
                        [--^import-marks=<??文件>]
                        [--^export-marks=<??文件>]
                        [--^branchseparator=<分隔符>]
                        [--^nodata] [--^from=<?更集ID>] [--^to=<?更集ID>]

??：

    存???格             要从中?出数据的存??。
                        （使用 'cm ^help ^objectspec' 可?一?了解存???格。）
    快速?出文件    具有存??数据并采用 Git 快速?出格式的
                        文件。
    --^import-marks      用于增量?入的??文件。此文件
                        已在先前由 '--^export-marks' ?出。此
                        文件中描述的?更集将不会被?入，
                        因??些?更集已在先前被?入。
    --^export-marks      所?入的?更集将保存到的文件。
                        此文件在以后的快速?入中用于表示
                        已?入的?更集。
    --^branchseparator   Unity VCS 使用 "/" 作?分支?次??中的默?
                        分隔符。此??允?将字符用作?次??
                        分隔符，因此 main-task-sub 将在 Unity VCS 中
                        映射? /main/task/sub。
    --^nodata            ?出存??，但不包括数据。??于
                        ???出功能是否可正??行很有用。
    --^from              从特定?更集?出。
    --^to                ?出到特定?更集。

== CMD_HELP_FASTEXPORT ==
?注：

    - ?了将 Unity VCS 存???入到 Git，?使用?如以下命令的命令：
      ^cat repo.fe.00 | ^git ^fast-import --^export-marks=marks.git  --^import-marks=marks.git

    - 支持使用一个包含先前?入的?更集的
      ??文件（'--^import-marks' 和 '--^export-marks' 文件）?行增量
      ?出。
      ?意味着将??出在先前快速?出中未?出的
      新?更集。

示例：

    cm ^fast-export repo@localhost:8087 repo.fe.00 --^import-marks=marks.cm \
      --^export-marks=marks.cm
    （将本地服?器中的存?? 'repo' 以 Git 快速?出格式
    ?出到 'repo.fe.00' 文件中，并?建??文件以稍后
    ?行增量?出。）

    cm ^fast-export repo@localhost:8087 repo.fe.00 --^from=20
    （将本地服?器中的存?? 'repo' 以 Git 快速?出格式
    从?更集 '20' ?出到 'repo.fe.00' 文件中。）

== CMD_DESCRIPTION_FASTIMPORT ==
将 Git 快速?出数据?入到存??中。

== CMD_USAGE_FASTIMPORT ==
用法：

    cm ^fast-import | ^fi <存???格> <快速?出文件>
                        [--^import-marks=<??文件>]
                        [--^export-marks=<??文件>]
                        [--^stats] [--^branchseparator=<分隔符>]
                        [--^nodata] [--^ignoremissingchangesets] [--^mastertomain]

??：

    存???格                     要将数据?入到的
                                存??。如果事先不存在存??，
                                ?会?建?存??。（使用 'cm ^help ^objectspec' 可?一?
                                了解存???格。）
    快速?出文件            具有存??数据并采用 Git 快速
                                ?出格式的文件。
    --^import-marks              用于增量?入的??文件。
                                此文件已在先前由
                                '--^export-marks' ?出。此文件中描述的
                                ?更集将不会被?入，因??些?更集
                                已在先前被?入。
    --^export-marks              所?入的?更集将保存到的
                                文件。此文件在以后的
                                快速?入中用于表示
                                已?入的?更集。
    --^stats                     打印一些有??入?程的??信息。
    --^branchseparator           Unity VCS 使用 "/" 作?分支?次??
                                中的默?分隔符。此??允?
                                将字符用作?次??分隔符，因此 main-task-sub
                                将在 Unity VCS 中映射? /main/task/sub。
    --^nodata                    ?入 Git 快速?出?果，但不包括
                                数据。??于???入功能是否可正?
                                ?行很有用。
    --^ignoremissingchangesets   无法?入的任何?更集都将被?弃，
                                然后在没有?些?更集的情况下??
                                ?行快速?入操作。
    --^mastertomain              使用 "^main" 而不是 "^master" ?行?入。

== CMD_HELP_FASTIMPORT ==
?注：

    - ?了?出 Git 存??，?使用?如以下命令的命令：
      ^git ^fast-export --^all -^M --^signed-tags=^strip --^tag-of-filtered-object=^drop> ..\git-fast-export.dat
      -^M ???于??移?的?很重要。

    - 如果指定的存??不存在，?会?建?存??。

    - 支持使用一个包含先前?入的?更集的
      ??文件（'--^import-marks' 和 '--^export-marks' 文件）?行增量
      ?入。
      ?意味着将??入在先前快速?入中未?入的
      新?更集。

示例：

    cm ^fast-import mynewrepo@atenea:8084  repo.fast-export
    （将?出到 'repo.fast-export' 文件中的内容?入到
    服?器 'atenea:8084' 上的 'mynewrepo' 存??中。）

    cm ^fast-import repo@atenea:8084  repo.fast-export --^export-marks=rep.marks
    （将?出到 'repo.fast-export' 文件中的内容?入到
    服?器 'atenea:8084' 上的 'repo' 存??中，并?建一个??文件
    以稍后?行增量?入。）

    cm ^fast-import repo@server:8084  repo.fast-export --^import-marks=repo.marks \
      --^export-marks=repo.marks
    （?入 'repo.fast-export' 文件的内容。??入
    不在??文件中的新?更集。同?的?个
    ??文件?用于再次保存?更集列表，以便?行
    下一次增量?入。）

== CMD_DESCRIPTION_FILEINFO ==
?索有?工作区中的?的??信息。

== CMD_USAGE_FILEINFO ==
用法：

    cm ^fileinfo <?路径>[ ...][--^fields=<字段?>[,...]]
                [[--^xml | -^x [=<?出文件>]] | [--^format=<格式字符串>]]
                [--^symlink] [--^encoding=<名称>]

    ?路径           要?示的?。使用空格?各个??行
                        分隔。
                        使用双引号 (" ") 指定包含空格的
                        路径。

??：

    --^fields            一串用逗号分隔的?。此字符串用于
                        ??要???打印的字段。?参?“?注”
                        以了解更多信息。
    --^xml | -^x          以 XML 格式将?出打印到?准?出。
                        可以指定?出文件。此??
                        不能与 '--^format' ?合使用。
    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。此??不能与 '--^xml'
                        ?合使用。
                        此 '--^format' ???先于 '--^fields'（如果同?
                        指定了??者）。
    --^symlink           将 fileinfo 操作?用于符号?接而不是
                        目?。
    --^encoding          指定?出??，如：utf-8。
                        ?参?位于以下网址的 MSDN 文档：
                        http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                        以?看包含受支持??及其格式的表格
                        （位于?面末尾的“名称”列中）。


== CMD_HELP_FILEINFO ==
?注：

    此命令??个所??打印??的属性列表。
    默?情况下，?个属性都打印在新行上。

    可以修改属性列表以??示用?所需的
    属性。???此目的，可以使用 '--^fields=<字段列表>'，
    其中会接受一串用逗号分隔的属性名称。??就只会?示
    已指明名称的那些参数。

    修??部?更集：

    默?情况下会禁用此??。?注意，?索此属性的
    速度明?慢于其余属性，因此我?建?用?
    将尽可能多的??合在一起。??可以避免
    多次?独?行 'cm ^fileinfo'，从而?短?行??。
    ?外，此功能当前不?用于受控目?。

    ?可以在下面找到可用属性名称的完整列表。
    默?情况下不会?示?有星号 ('*') 的名称：
        ^ClientPath              ?在磁?上的本地路径。
        ^RelativePath            相?于工作区的路径。
        ^ServerPath              ?的存??路径。
                                （注意：此??当前
                                不支持已??的工作区）。
        ^Size                    ?大小。
        ^Hash                    ?哈希??和。
        ^Owner                   ?所属的用?。
        ^RevisionHeadChangeset   (*) 已加?到分支?部?更集内的
                                修??更集。
                                （?参?上面的注意事?。）
        ^RevisionChangeset       当前已加?到工作区内的
                                修??更集。
        ^RepSpec                 ?的存???格。
                                （使用 'cm ^help ^objectspec' 可?一?了解
                                存???格。）
        ^Status                  工作区?状?：已添加、已?出、
                                已?除，等等。
        ^Type                    修??型（文本、二?制、目?、符号?接
                                或未知）。
        ^Changelist              ?所属的更改列表（如果有）。
        ^IsLocked                (*) ?是否已被独占?出
                                ?定。
        ^LockedBy                (*) 将?独占?出的用?。
        ^LockedWhere             (*) 将?独占?出的
                                位置。
        ^IsUnderXlink            ?是否位于 Xlink
                                之下。
        ^UnderXlinkTarget        ?所在的 Xlink 的目?
                                （如果有）。
        ^UnderXlinkPath          通? Xlink ?接的存??中的?服?器路径
                                （如果有）。
        ^UnderXlinkWritable      ?所属的 Xlink 是否
                                可写。
        ^UnderXlinkRelative      ?所属的 Xlink 是否
                                是相? Xlink。
        ^IsXlink                 ?本身是否是 Xlink。
        ^XlinkTarget             ?指向的目?存??（如果?
                                是 Xlink）。
        ^XlinkName               ?的 Xlink 名称（如果???上
                                是 Xlink）。
        ^XlinkWritable           Xlink ?是否?
                                可写 Xlink。
        ^XlinkRelative           Xlink ?是否?
                                相? Xlink。

    ?出格式自定?：

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：
        {^ClientPath}
        {^RelativePath}
        {^ServerPath}
        {^Size}
        {^Hash}
        {^Owner}
        {^RevisionHeadChangeset}
        {^RevisionChangeset}
        {^Status}
        {^Type}
        {^Changelist}
        {^IsLocked}
        {^LockedBy}
        {^LockedWhere}
        {^IsUnderXlink}
        {^UnderXlinkTarget}
        {^UnderXlinkPath}
        {^UnderXlinkWritable}
        {^UnderXlinkRelative}
        {^IsXlink}
        {^XlinkTarget}
        {^XlinkName}
        {^XlinkWritable}
        {^XlinkRelative}
        {^RepSpec}

    ?注意，'--^format' 和 '--^xml' ??是互斥的，
    因此不能同?使用。

示例：

    cm ^fileinfo file1.txt file2.txt dir/
    cm ^fileinfo "New Project.csproj" --^xml
    cm ^fileinfo assets.art --^fields=^ServerPath,^Size,^IsLocked,^LockedBy
    cm ^fileinfo proj_specs.docx --^fields=^ServerPath,^RevisionChangeset --^xml
    cm ^fileinfo samples.ogg --^format="{^ServerPath}[{^Owner}] -> {^Size}"

== CMD_DESCRIPTION_FIND ==
?行?似 SQL 的??以?找 Unity VCS ?象。

== CMD_USAGE_FIND ==
用法：

    cm ^find <?象?型>
            [^where <条件字符串>]
            [^on ^repository '<存???格>' | ^on ^repositories '<存???格1>','<存???格2>'[,...]]
            [--^format=<格式字符串>] [--^dateformat=<日期格式>]
            [--^nototal] [--^file=<??文件>] [--^xml]
            [--^encoding=<名称>]

    ?象?型         要?找的?象?型。
                        （使用 'cm ^help ^showfindobjects' 可了解如何指定
                        ?些?象。）
                        ??可以?? 'cm ^find' 指南：
                        https://www.plasticscm.com/download/help/cmfind

??：

    条件字符串      搜索?象属性的条件。
    存???格             搜索存???名或?格。
                        如果是 '^on ^repositories'，?使用逗号
                        ?存???格字段?行分隔。
                        （使用 'cm ^help ^objectspec' 可?一?了解存??
                        ?格。）
    --^format            ?索特定格式的?出消息。
                        ??? 'cm ^find' 指南以?看所有
                        可用作?出格式字符串的?象属性：
                        https://www.plasticscm.com/download/help/cmfind
    --^dateformat        用于?出日期的格式。
    --^nototal           最后不?出??数。
    --^file              ???果的文件。
    --^xml               以 XML 格式将?出打印到?准?出。
    --^encoding          指定?出??，如：utf-8。
                        ?参?位于以下网址的 MSDN 文档：
                        http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                        以?看包含受支持??及其格式的表格
                        （位于?面末尾的“名称”列中）。

== CMD_HELP_FIND ==
?注：

    如果未指定存??，?在工作区中配置的存??上
    ?行搜索。

    从命令行中使用比??算符（>、<、>=、<=）
    ?行???，?注意，shell 将?些?算符?? IO
    重定向。因此，需要用双引号将??
    引起来。

    'cm ^find' 命令接受格式字符串以?示?出。
    ?个?出参数由字符串?行??，用?可以通?
    在 '{' 和 '}' 花括号之??入参数?号来引用参数。
    ?出参数通常??于?象的属性。

    一些有效的?出格式字符串如下：
      --^format={^id}{^date}{^name}
      --^format="{^item}#{^branch} ^with ^date {^date}"

    XML 和??注意事?：

    指定 '--^xml' ??后，?命令在?准?出中将命令?果
    ?示? XML 文本。文本将以操作系?的
    默????示，因此在控制台中可能会
    ??地?示非 ANSI 字符。如果将命令?出重定向到
    文件，?将正??示?出内容。当同?指定了 '--^xml' 和 '--^file'
    ???，默???? utf-8。

示例：

    cm ^find ^revision
    cm ^find ^revision "^where ^changeset=23 ^and ^owner='maria'"
    cm ^find ^branch "^on ^repository 'rep1'"
    cm ^find ^label "^on ^repositories 'rep1', '^rep:default@localhost:8084'"
    cm ^find ^branch "^where ^parent='^br:/main' ^on ^repository 'rep1'"
    cm ^find ^revision "^where ^item='^item:.'" --^format="{^item}#{^branch}"
    cm ^find ^revision "^where ^item='^item:.'" --^xml --^file=c:\queryresults\revs.xml

== CMD_DESCRIPTION_FINDCHANGED ==
?取已更改文件的列表。已弃用此命令，保留此命令只是?了
向后兼容。?改用 'cm ^status'。

== CMD_USAGE_FINDCHANGED ==
用法：

    cm ^findchanged | ^fc [-^R | -^r | --^recursive] [--^checkcontent]
                        [--^onlychanged] [<路径>]

??：

    -^R                  以??方式在目?中?找。
    --^checkcontent      按内容比?文件。
    --^onlychanged       ??找已更改的文件；无法?得
                        ?出。
    路径                （默??：当前目?。）
                        ?找已更改文件的初始路径。

== CMD_HELP_FINDCHANGED ==
?注：

    如果未?出 '--^checkcontent' ??，Unity VCS 将根据文件??戳
    ?找更改。当指定了 '--^checkcontent' ???，比?的是
    文件或文件?内容，而不使用??戳。

    与 Unity VCS 服?器断??接的情况下，此命令?于??
    已更改的文件很有用。可以将?出通?管道??到 checkout 命令，
    从而在稍后??更改（?参?示例）。

示例：

    cm ^findchanged .
    （在当前目?中?找已更改的文件。）

    cm ^findchanged -^R .| cm ^checkout -
    （?出已更改的元素。）

== CMD_DESCRIPTION_FINDCHECKEDOUT ==
?取已?出?的列表。已弃用此命令，保留此命令只是?了
向后兼容。?改用 'cm ^status'。

== CMD_USAGE_FINDCHECKEDOUT ==
用法：

    cm ^findcheckouts | ^fco [--^format=<格式字符串>] [--^basepath]

??：

    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。
    --^basepath          ?始搜索?出的起始路径。如果
                        未指定，?使用当前路径。

== CMD_HELP_FINDCHECKEDOUT ==
?注：

    此命令?合用于在一个??中?入?或撤??出所有
    已?出的?，并将?准?出重定向到其他命令。
    ?参?示例。

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：
        {0}             日期。
        {1}             所有者。
        {2}             工作区信息。
        {3}             客?端机器名称。
        {4}             ?路径。
        {5}             分支和存??信息。

示例：

    cm ^findcheckouts --^format="分支 {5} 上已更改的文件 {4}"
    （?找已?出的?，并使用文件路径以及分支和存??信息
    来格式化?出。）

    cm ^findcheckouts --^format={4} | cm ^checkin -
    （?入所有已?出的?。）

    cm ^findcheckouts --^format={4} | cm ^undocheckout -
    （撤??出所有已?出的?。）

== CMD_DESCRIPTION_FINDPRIVATE ==
?取私有?的列表。已弃用此命令，保留此命令只是?了
向后兼容。?改用 'cm ^status'。

== CMD_USAGE_FINDPRIVATE ==
用法：
    cm ^findprivate | ^fp [-^R | -^r | --^recursive] [--^exclusions] [<路径>]

??：

    -^R                  以??方式在目?中?找。
    --^exclusions        此??允?在文件 ignore.conf 定?的
                        已忽略路径内禁止搜索。
    路径                （默??：当前目?。）
                        ?找私有文件的初始路径。

== CMD_HELP_FINDPRIVATE ==
?注：

    如果指定了任何路径，Unity VCS 将?始从当前目?
    搜索。

    此命令?合用于在文件?上添加私有?，并将?出通?管道
    ??到 add 命令。?参?示例。

示例：

    cm ^findprivate .

    cm ^findprivate -^R | cm ^add -
    （以??方式搜索私有?并添加?些?。）

== CMD_DESCRIPTION_GETCONFIG ==
?取配置信息。

== CMD_USAGE_GETCONFIG ==
用法：

    cm ^getconfig [^setfileasreadonly] [^location] [^extensionworkingmode]
                 [^extensionprefix] [^defaultrepserver]
    
    ^setfileasreadonly       返回受保?文件是否保留?
                            只?。
    ^location                返回客?端配置路径。
    ^extensionworkingmode    返回?展工作模式。
    ^extensionprefix         返回已配置的?展前?。
    ^defaultrepserver        返回默?存??服?器的
                            位置。

== CMD_HELP_GETCONFIG ==
示例：

    cm ^getconfig ^setfileasreadonly

== CMD_DESCRIPTION_GETFILE ==
下??定修?的内容。

== CMD_USAGE_GETFILE ==
用法：

    cm ^getfile | ^cat <修??格> [--^file=<?出文件>] [--^debug]
                     [--^symlink] [--^raw]

    修??格           ?象?格。（使用 'cm ^help ^objectspec' 可
                      ?一?了解?格。）

??：

    --^file            用于保存?出的文件。默?情况下会打印在
                      ?准?出中。
    --^debug           使用目??格?，?命令
                      将?示目?中的所有?、目?的修? ID
                      和文件系?保?。
    --^symlink         将操作?用于符号?接而不是
                      目?。
    --^raw             ?示文件的原始数据。

== CMD_HELP_GETFILE ==
示例：

    cm ^cat myfile.txt#^br:/main
    （在 'myfile.txt' 的分支 '^br:/main' 中?取最后一个修?。）

    cm ^getfile myfile.txt#^cs:3 --^file=tmp.txt
    （?取 'myfile.txt' 的?更集 3 并将??更集写入文件 'tmp.txt'。）

    cm ^cat ^serverpath:/src/foo.c#^br:/main/task003@myrepo
    （在存?? 'myrepo' 中的分支 '/main/task003' 的最后一个?更集?
    ?取 '/src/foo.c' 的内容）

    cm ^cat ^revid:1230@^rep:myrep@^repserver:myserver:8084
    （?得 ID ? 1230 的修?。）

    cm ^getfile ^rev:info\ --^debug
    （?取 'info' 目?中的所有修?。）

== CMD_DESCRIPTION_GETREVISION ==
在工作区中加?修?。

== CMD_USAGE_GETREVISION ==
此命令会修改工作区中加?的修?，因此可能影?
以后的合并。
?是从旧版本?承的高?命令，因此??慎使用。

用法：
    cm ^getrevision <修??格>

    修??格           ?象?格。（使用 'cm ^help ^objectspec' 可
                      ?一?了解修??格。）

== CMD_HELP_GETREVISION ==
示例：

    cm ^getrevision file.txt#^cs:3
    （?取 'file.txt' 的?更集 3 修?。）

== CMD_DESCRIPTION_GETSTATUS ==
?取?的状?。

== CMD_USAGE_GETSTATUS ==
?是一个自?化命令，?用于自??行 'cm'。
此命令的用?友好度不如?期。

用法：

    cm ^getstatus | ^gs <?路径>[ ...][--^format=<格式字符串>] [--^stats]
                      [-^R | -^r | --^recursive]

    ?路径           要?取相?状?的一个或多个?。使用双引号
                        (" ") 指定包含空格的路径。使用
                        空格?各个路径?行分隔。

??：

    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。
    --^stats             打印一些有??取状??程的??信息。
    -^R                  以??方式?示目?中的状?。

== CMD_HELP_GETSTATUS ==
?注：

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：
        {0}             ?路径。
        {1}             ?状?：
            0   私有，
            1   已?入，
            2   已?出。

从 stdin ?取?入：

    '^getstatus' 命令可从 stdin ?取路径。?此，???
    一个破折号 "-"。
    示例：cm ^getstatus -

    路径将一直?取到有空行?入?止。
    ??情况下允?使用??指定要?取?些路径的状?。
    示例：
      dir /S /B *.c | cm ^getstatus --^format="路径 {0} 状? {1}" -
      （在 Windows 中，?取工作区中所有 .c 文件的状?。）

示例：

    cm ^getstatus file1.txt file2.txt
    （?取文件的状?。）

    cm ^gs info\ -^R --^format="? {0} 的状?? {1}"
    （?取目?及其所有?的状?，并?示
    格式化的?出。）

== CMD_DESCRIPTION_GETTASKBRANCHES ==
?取与某个任??接的分支。

== CMD_USAGE_GETTASKBRANCHES ==
?是一个自?化命令，?用于自??行 'cm'。
此命令的用?友好度不如?期。

用法：

    cm ^gettaskbranches | ^gtb <任?名称> [--^format=<格式字符串>]
                             [--^dateformat=<日期格式>]

    任?名称           任???符。

??：

    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。
    --^dateformat        用于?出日期的格式。

== CMD_HELP_GETTASKBRANCHES ==
?注：

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：
        {^tab}           插入一个制表符空格位。
        {^newline}       插入一个新行。
        {^name}          分支名称。
        {^owner}         分支的所有者。
        {^date}          ?建分支的日期。
        {^type}          分支?型（如果是智能分支，?? 'T'，否?? 'F'）。
        {^parent}        父分支。
        {^comment}       分支的注?。
        {^repname}       分支所在的存??。
        {^repserver}     服?器名称。

示例：

    cm ^gettaskbranches 4311
    cm ^gtb 4311 --^format="^br:{^name}"
    cm ^gtb 4311 --^format="^br:{^name} {^date}" --^dateformat="yyyy/MM/dd HH:mm:ss"

== CMD_DESCRIPTION_GETWORKSPACEINFO ==
?示有?工作区??器的信息。

== CMD_USAGE_GETWORKSPACEINFO ==
用法：

    cm ^wi [<工作区路径>]

??：

    工作区路径             工作区在机器上的路径。

== CMD_HELP_GETWORKSPACEINFO ==
?注：
    '^wi' 命令?示工作区（存??、分支和/或??）
    的工作配置。

示例：
    cm ^wi c:\mywk

== CMD_DESCRIPTION_GETWORKSPACEFROMPATH ==
从路径?取工作区信息。

== CMD_USAGE_GETWORKSPACEFROMPATH ==
?是一个自?化命令，?用于自??行 'cm'。
此命令的用?友好度不如?期。

用法：

    cm ^getworkspacefrompath | ^gwp <?路径> [--^format=<格式字符串>]

    ?路径           磁?上的文件或文件?。

??：
    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。

== CMD_HELP_GETWORKSPACEFROMPATH ==
?注：

    此命令?示有?路径中的工作区的信息。

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：
        {0} | {^wkname}          工作区名称。
        {1} | {^wkpath}          工作区路径。
        {2} | {^machine}         客?端机器名称。
        {3} | {^owner}           工作区所有者。
        {4} | {^guid}            工作区 GUID。

        {^tab}                   插入一个制表符空格位。
        {^newline}               插入一个新行。

示例：

    cm ^gwp c:\myworkspace\code\file1.cpp --^format="工作区名称：{^wkname}"

== CMD_DESCRIPTION_HELP ==
?取有? Unity VCS 命令的?助。

== CMD_USAGE_HELP ==
用法：

    cm ^help <命令>

== CMD_HELP_HELP ==

== CMD_DESCRIPTION_IOSTATS ==
?示有?硬件的??信息。

== CMD_USAGE_IOSTATS ==
用法：

    cm ^iostats [<存??服?器?格>] [<??列表>[ ...]]
               [--^nettotalmb=<?_mb>] [--^networkiterations=<?_迭代>]
               [--^diskdatasize=<?_大小>] [--^disktestpath=<?_路径>]
               [--^systemdisplaytime=<?_??>]
               [--^systemdisplaytimeinterval=<?_?隔>]

??：

    存??服?器?格                 用于?行网???的可用 Unity VCS
                                  服?器，例如 "serverUploadTest"
                                  和/或 "serverDownloadTest"。
                                  如果未提供服?器，?命令
                                  将??与默?配置的服?器
                                  通信。
                                  （使用 'cm ^help ^objectspec' 可?一?了解
                                  服?器?格。）
    ??列表                 可用的??。使用空格?各个??字段
                                  ?行分隔。
                                  ?参?“?注”以了解更多信息。
    --^nettotalmb                  表示在网???中??的
                                  用?数据量（以兆字???位），
                                  例如 "^serverDownloadTest" 或
                                  "^serverUploadTest"。
                                  ?必?介于 "4" 到 "512" 之?。
                                  （默??：16）
    --^networkiterations           表示要?行的
                                  "^serverDownloadTest" 和/或 "^serverUploadTest"
                                  的迭代次数。
                                  ?必?介于 "1" 到 "100" 之?。
                                  （默??：1）
    --^diskdatasize                表示将在 "^diskTest" 上写入
                                  并随后?取的数据量（以兆字?
                                  ??位）。
                                  ?必?介于 "100" 到 "4096" 之?。
                                  （默??：512）
    --^disktestpath                "^diskTest" 将??文件写入到的
                                  路径。如果未提供此参数，
                                  ?命令将??使用系???
                                  路径。
    --^systemdisplaytime           ?示系??源使用情况的???隔
                                  （?位：秒）。此??可用于
                                  以下??："^systemNetworkUsage"
                                  和 "^systemDiskUsage"。
                                  ?必?介于 "1" 到 "3600" 之?。
                                  （默??：5 秒）。
     --^systemdisplaytimeinterval  系?性能采?的???隔
                                  （?位：秒）。此??可用于
                                  以下??：
                                  "^systemNetworkUsage" 和 "^systemDiskUsage"。
                                  ?必?介于 "1" 到 "60" 之?。
                                  （默??：1 秒）。

== CMD_HELP_IOSTATS ==
?注：

    此命令要求在网速??（"^serverUploadTest" 和/或
    "^serverDownloadTest"）期?使用可用的服?器。

    '--^diskTestPath' 必?指向属于要??的物理
    磁???器的路径。如果未指定路径，?命令将??
    使用系?默?的??路径。
    指定路径的磁???器必?具有足?的可用空?才能?行
    ??。

    在?行命令期?，?行的??可能?致
    系?性能下降。

    可用的??：
        --^serveruploadtest      （默??）?量从 Unity VCS 客?端到
                                服?器的数据上?速度。
        --^serverdownloadtest    （默??）?量从 Unity VCS 服?器到
                                客?端的数据下?速度。
        --^disktest              （默??）?量磁??取速度和磁?
                                写入速度。
        --^systemnetworkusage    ?示当前的系?网??源
                                使用情况。
                                （?示 Microsoft Windows 提供的网?接口
                                性能?数器）。
                                ?在 Microsoft Windows 中可用。
        --^systemdiskusage       ?示当前的系?物理磁?
                                使用情况。
                                （?示 Microsoft Windows 提供的网?接口
                                性能?数器）。
                                ?在 Microsoft Windows 中可用。

示例：

    cm ^iostats MYSERVER:8087 --^serveruploadtest --^serverdownloadtest --^nettotalmb=32

== CMD_DESCRIPTION_ISSUETRACKER ==
在指定的??跟踪程序中?取、更新或?找??状?。

== CMD_USAGE_ISSUETRACKER ==
用法：

    cm ^issuetracker <名称> ^status ^get <任?_ID> <参数>[ ...]
    cm ^issuetracker <名称> ^status ^update <任?_ID> <状?> <参数>[ ...]
    cm ^issuetracker <名称> ^status ^find <状?> <参数>[ ...]
    cm ^issuetracker <名称> ^connection ^check <参数>[ ...]
    
    名称                要?接的??跟踪程序的名称。
                        目前?支持 Jira。
    任?_ID             要??或更新的??的?号。
    ^status              ??跟踪程序中某个??的有效状?。

Jira 参数（全部?必需参数）：

    --^user=<用?>         要??身?的用?。
    --^password=<密?> 用于身???的密?。
    --^host=<URL>          ??跟踪程序的目? URL。
    --^projectkey=<密?>    Jira ?目的?目密?。
    
== CMD_HELP_ISSUETRACKER ==
示例：

    cm ^issuetracker jira ^status ^get 11 --^user=user@mail.es --^password=pwd \
      --^host=https://user.atlassian.net --^projectkey=PRJ
    （?取 'PRJ' ?目的?? 11 的状?。）

    cm ^issuetracker jira ^status ^update 11 "完成" --^user=user@mail.es \
      --^password=pwd --^host=https://user.atlassian.net --^projectkey=PRJ
    （将 'PRJ' ?目的?? 11 的状?更新?“完成”。）
    
    cm ^issuetracker jira ^status ^find "完成" --^user=user@mail.es --^password=pwd \
      --^host=https://user.atlassian.net --^projectkey=PRJ
    （? 'PRJ' ?目?取相?状??置?“完成”的任? ID）

    cm ^issuetracker jira ^connection ^check --^user=user@mail.es --^password=pwd \
      --^host=https://user.atlassian.net --^projectkey=PRJ
    （??配置参数是否有效。）

== CMD_DESCRIPTION_LICENSEINFO ==
?示?可?信息和?可?使用情况。

== CMD_USAGE_LICENSEINFO ==
用法：

    cm ^licenseinfo | ^li [--^server=<存??服?器?格>] [--^inactive] [--^active]
                        [--^sort=(^name|^status)]

??：

    --^server            从指定的服?器?取?可?信息。
                        如果未指定服?器，?在客?端上配置的
                        服?器上?行命令。
                        （使用 'cm ^help ^objectspec' 可?一?了解存??
                        服?器?格。）
    --^inactive          在“?可?使用情况”部分中??示非活?用?。
    --^active            在“?可?使用情况”部分中??示活?用?。
    --^sort              按指定的排序??之一?用??行排序：
                        '^name' 或 '^status'。

== CMD_HELP_LICENSEINFO ==
?注：

    ?示的信息包括到期日期、已激活的用?和
    已停用的用?等。

示例：

    cm ^licenseinfo
    cm ^licenseinfo --^server=myserver:8084
    cm ^licenseinfo --^sort=^name

== CMD_DESCRIPTION_LINKTASK ==
将?更集?接到任?。

== CMD_USAGE_LINKTASK ==
?是一个自?化命令，?用于自??行 'cm'。
此命令的用?友好度不如?期。

用法：

    cm ^linktask | ^lt <?更集?格> <?展前?> <任?名称>

    ?更集?格            要?接到任?的完整?更集?格。
                        （使用 'cm ^help ^objectspec' 可?一?了解?更集
                        ?格。）
    ?展前?          要使用的已配置??跟踪系?的
                        ?展前?。
    任?名称           ??跟踪系?上的任???符。

== CMD_HELP_LINKTASK ==
示例：

    cm ^lt ^cs:8@^rep:default@^repserver:localhost:8084 jira PRJ-1

== CMD_DESCRIPTION_LOCK_LIST ==
?示服?器上的?定情况。

== CMD_USAGE_LOCK_LIST ==
用法：

    cm ^lock ^list | ^ls [<修??格 > [ ...]] [--^server=<服?器>]
                      [--^onlycurrentuser] [--^onlycurrentworkspace]
                      [--^ignorecase]

    修??格             如果存在一个或多个修?，?此命令将
                        ??个指定的修??示一个?定行
                        （如果在服?器中?定了?修?的???）。否?，
                        此命令将列出默?服?器中的所有?定?
                        （或者使用 '--^server' ??来?置的已?定?）
                        使用多个修??格?，使用空格?各个修??格
                        ?行分隔。
                        （使用 'cm ^help ^objectspec' 可?一?了解修??格。）

??：

    --^server                存??服?器?格。
                            此??将覆盖从当前工作区
                            或 client.conf 文件?索到的默?
                            服?器。
                            （使用 'cm ^help ^objectspec' 可?一?了解
                            服?器?格。）
    --^onlycurrentuser       ???果，??示当前用?
                            ?行的?定。
    --^onlycurrentworkspace  ???果，??示?当前工作区
                            ?行的?定（按名称?行匹配）。
    --^ignorecase            使用服?器路径?格?，忽略路径中的
                            大小写。使用此?志后，即使用?写入 "/sRc/fOO.c"，
                            ?命令也将? "/src/foo.c" 有效。

== CMD_HELP_LOCK_LIST ==
?注：

    ?命令将列出默?服?器中当前
    已?定的?。?命令?接受若干修??格；在??
    情况下，??示属于所??的?定。
    可使用 '--^server=<服?器>' 来?置要??的默?服?器。

    ?命令??指定服?器中的?个?定都会?示一行：
        - 已?定?的 GUID。
        - ?行?定的用?名。
        - ?定操作所在的工作区的名称。
        - ?定?的路径（服?器路径格式）。

示例：

    cm ^lock ^list
    cm ^lock ^list --^server=myserver:8084
    cm ^lock ^ls ^serverpath:/src/foo.c#^cs:99@default@localhost:8084
    cm ^lock ^list ^revid:3521@default ^itemid:2381@secondary --^onlycurrentuser
    cm ^lock ^ls --^onlycurrentuser
    cm ^lock ^ls --^onlycurrentuser --^onlycurrentworkspace

== CMD_DESCRIPTION_LISTUSERS ==
列出用?和?。

== CMD_USAGE_LISTUSERS ==
用法：

    cm ^listusers | ^lu <存??服?器?格> [--^onlyusers] [--^onlygroups]
                      [--^filter= <??器字符串>]

    存??服?器?格       存??服?器?格。
                        （使用 'cm ^help ^objectspec' 可?一?了解?格。）

??：

    --^onlyusers         ?列出用?。
    --^onlygroups        ?列出?。
    --^filter            ?列出匹配指定??器的用?
                        和/或?。

== CMD_HELP_LISTUSERS ==
示例：

    cm ^lu localhost:8084
    （列出服?器中的所有用?。）

    cm ^listusers localhost:8084 --^onlyusers --^filter=m
    （?列出服?器中包含 "m" 的用?。）

== CMD_DESCRIPTION_LOCATION ==
返回 'cm' 的路径。

== CMD_USAGE_LOCATION ==
用法：

    cm ^location

== CMD_HELP_LOCATION ==

== CMD_DESCRIPTION_LOCK ==
此命令允?用?管理?定。

== CMD_USAGE_LOCK ==
用法：

    cm ^lock <命令> [??]

命令：

    ^list | ^ls
    ^unlock

    要?取有??条命令的更多信息，??行：
    cm ^lock <命令> --^usage
    cm ^lock <命令> --^help

== CMD_HELP_LOCK ==
示例：

    cm ^lock ^list
    cm ^lock
    （如果没有参数，? '^list' ?可??。）
    cm ^lock ^ls ^serverpath:/src/foo.c#^cs:99@default@localhost:8084
    cm ^lock ^unlock 91961b14-3dfe-4062-8c4c-f33a81d201f5

== CMD_DESCRIPTION_LOG ==
?取有??更集内的修?的信息。

== CMD_USAGE_LOG ==
用法：

    cm ^log [<?更集?格> | <存???格>] [--^from=<?更集?格起点>] [--^allbranches]
           [--^ancestors] [--^csformat=<格式字符串>] [--^itemformat=<格式字符串>]
           [--^xml[=<?出文件>]] [--^encoding=<名称>]
           [--^repositorypaths | --^fullpaths | --^fp]

??：

    ?更集?格            ?命令将返回在相??格的?更集内
                        所做的所有更改。
                        （使用 'cm ^help ^objectspec' 可?一?了解?更集
                        ?格。）
    存???格             ?命令将列出在指定存??中?行的
                        所有更改。
                        （使用 'cm ^help ^objectspec' 可?一?了解存??
                        ?格。）
    --^from              列出从?更集?格 [?更集?格起点]
                        到?更集?格 [?更集?格] 的?个?更集内
                        ?行的所有更改。
                        [?更集?格起点] ?更集不包含在
                        ?出中。
                        提供存???格后会忽略此??。
    --^allbranches       ?于?建了相??更集的所有分支，
                        ?示指定???隔内?建的
                        ?些?更集的相?信息。
    --^ancestors         ?示可通??定?更集（[?更集?格]）的
                        父?接和合并?接来??的?更集
                        的相?信息。如果也提供了起点
                        ?更集（[?更集?格起点]），此?更集将用作
                        所有路径的下限。?注：使用此???，
                        不会?示?更集更改。
    --^csformat          ?索特定格式的?更集信息。?参?
                        “?注”以了解更多信息。
    --^itemformat        ?索特定格式的?信息。?参?
                        “?注”以了解更多信息。
    --^xml               以 XML 格式将?出打印到?准?出。
                        可以指定?出文件。
    --^encoding          与 '--^xml' ???合使用，指定要在 XML ?出
                        中使用的??（例如：utf-8）。
                        ?参?位于以下网址的 MSDN 文档：
                        http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                        以?看包含受支持??及其格式的表格
                        （位于?面末尾的“名称”列中）。
    --^fullpaths, --^fp   如果可能，?制打印文件和目?的完整
                        工作区路径。
    --^repositorypaths   打印存??路径（服?器路径）
                        而不是工作区路径。（此??将覆盖
                        '--^fullpaths' ??。）

== CMD_HELP_LOG ==
?注：

    - 如果“?更集?格”和??均未指定，?命令将?示
      ?个分支中上个月内?建的?个?更集的
      相?信息。
    - 如果?包含 '--^from' ??，?命令?示
      在?建指定?更集的分支中从??更集到最后一个
      ?更集的?个?更集的相?信息。
    - 如果?示不????隔的?? '--^allbranches'，
      ?命令会?索与?指定“?更集?格”?相同的
      信息。
    - 如果使用 '--^from'，?出包含从“?更集?格起点”+1 ?始的
      ?更集的信息。
    - 用于?示?更集信息的存??就是在?行命令的
      路径中加?的存??。

    此命令接受?的格式字符串 ('--^itemformat') 以及
    ?更集的格式字符串 ('--^csformat')。

    '--^csformat' 的?出参数如下：
        {^tab}           插入一个制表符空格位。
        {^newline}       插入一个新行。
        {^changesetid}   ?更集?号。
        {^branch}        在其中?建?更集的分支。
        {^date}          ?更集的日期。
        {^owner}         ?更集的所有者。
        {^comment}       ?更集的注?。
        {^items}         ?更集内?及的?。
        {^repository}    ?更集所在的存??。
        {^repserver}     服?器名称。

    '--^itemformat' 的?出参数如下：
        {^tab}           插入一个制表符空格位。
        {^newline}       插入一个新行。
        {^path}          ?路径。
        {^branch}        在其中?建?更集的分支。
        {^date}          ?更集的日期。
        {^owner}         ?更集的所有者。
        {^shortstatus}   打印短格式。?参?下文。
        {^fullstatus}    打印?格式。?参?下文。

        短格式及其??的?格式：
            '^A'   ^Added
            '^D'   ^Deleted
            '^M'   ^Moved
            '^C'   ^Changed

    ?些是有效的?出字符串：
        --^csformat="{^newline}?更集 {^changesetid} ?建于 {^date}；{^tab} 已更改?：{^items}。"
        --^itemformat="{^newline}分支 {^branch} 中的? {^path} 已更改。"

示例：

    cm ^log
    （?示?个分支中上个月?建的?个?更集的相?
    信息。）

    cm ^log ^cs:16
    （?示在其中?建?更集的分支内的?更集 16 中
    所做更改的相?信息。）

    cm ^log ^cs:16 --^csformat="{^newline}?更集 {^changesetid} ?建于 \
      {^date}；{^tab} 已更改?：{^items}。"
    （按指定格式?示此信息。）

    cm ^log --^from=^cs:20 ^cs:50
    （?示从?更集 21 到?更集 50 的?个?更集中包含的
    ?个修?的相?信息。）

    cm ^log --^from=^cs:20 ^cs:50 --^allbranches
    （?示存??的?个分支中从?更集 21 到
    ?更集 50 的?个?更集中包含的?个修?的
    相?信息。）

    cm ^log ^rep:myrep@localhost:8084
    （?示在指定存??中?行的更改的相?信息。
    无需工作区也可?行此命令。）

    cm ^log --^from=^cs:20@^rep:mainRep@localhost:8084
    （?示从?更集 21 ?始的?个?更集内包含的?个修?
    的相?信息。无需工作区也可?行此命令，因?
    已指定完整?更集?格。）

== CMD_DESCRIPTION_LIST ==
列出?的内容。

== CMD_USAGE_LIST ==
用法：

    cm ^ls | ^dir [<路径>[ ...]] [--^format=<格式字符串>] [--^symlink]
                [--^selector[=<??器格式>]] [--^tree=<?象?格>]
                [-^R | -^r | --^recursive]
                [--^xml[=<?出文件>]] [--^encoding=<名称>]

??：

    路径               要?示的路径的列表。使用空格?各个路径?行
                        分隔。
                        使用双引号 (" ") 指定包含空格的
                        路径。
    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。
    --^symlink           将操作?用于符号?接而不是
                        目?。
    --^selector          从活?的工作区??器?取内容。
                        如果已指定??器格式，?列出
                        指定的??器。
                        自 Plastic SCM 4.x ?始，??器不再是 Unity VCS
                        的重要特性，因此大多数情况下已弃用。
    --^tree              列出指定?更集或分支中的?。
                        （使用 'cm ^help ^objectspec' 可?一?了解?格。）
    -^R                  以??方式列出。
    --^xml               以 XML 格式将?出打印到?准?出。
                        可以指定?出文件。
    --^encoding          与 '--^xml' ???合使用，指定要在 XML ?出
                        中使用的??（例如：utf-8）。
                        ?参?位于以下网址的 MSDN 文档：
                        http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                        以?看包含受支持??及其格式的表格
                        （位于?面末尾的“名称”列中）。

== CMD_HELP_LIST ==
?注：

    - 可以使用元字符来?入路径。
    - 此列表取决于工作区??器。
    - 可以指定格式字符串，从而将命令的?出格式化。
    - 如果指定 '--^tree' 或 '--^selector' ??，??定的
      路径必?是服?器路径（也就是：'cm path'）：/dir/file.txt，而不是
      工作区路径：C:\Users\myuser\mywk\dir\file.txt
    - 如果未提供路径，???工作区路径是当前
      目?。如果使用 '--^tree' 或 '--^selector' ??，
      ?采用根路径 ("/")。

    默?格式字符串?：
      "{^size,10} {^date:dd/MM/yyyy} {^date:HH:mm}\
       {^type,-6} {^location,-12} {^checkout,-5} {^name}\
       {^symlinktarget}"

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：
        {^size}
        {^formattedsize}
        {^date}
        {^type}
            ^dir     目?，
            ^txt     文本文件，
            ^File    文件。
        {^location}      示例：^br:branch#cset
        {^checkout}
        {^name}
        {^changeset}
        {^path}
        {^repspec}
        {^owner}
        {^revid}
        {^parentrevid}
        {^itemid}
        {^brid}
        {^repid}
        {^server}
        {^symlinktarget}
        {^hash}
        {^chmod}
        {^wkpath}        相?于工作区根目?的路径
        {^branch}
        {^newlocation}   cset@branch
        {^guid}          （将需要更???来解析）
        {^itemguid}
        {^transformed}   ?示已???的?用??

    可以?置 PLASTIC_LS_FORMAT ?境?量以便自定?
    '^ls' 格式。

示例：

    cm ^ls
    cm ^ls c:\workspace\src

    cm ^ls --^format={^name}
    （?文件名。）

    cm ^ls --^symlink
    （?示有?符号?接的信息，而不是“符号?接”的文件或
    目?。?用于 UNIX ?境。）

    cm ^ls code --^selector
    （?示当前工作区??器中 'code' 子目?的
    内容。）

    cm ^ls /code --^selector="^rep 'myrep' ^path '/' ^branch '/^main'"
    （?示指定??器中 '/code' 子目?的内容。
    ?注意，?路径是按服?器格式指定的。）

    cm ^ls /code --^tree=44@myrep@denver:7070
    （列出服?器 'denver:7070' 存?? 'myrep' ?更集 44 的
    '/code' 子目?。）

    cm ^ls /code --^tree=^br:/main/scm13596@myrep@denver:7070
    （列出服?器 'denver:7070' 存?? 'myrep' 分支
    '/main/scm13596' 中的最新?更集的 '/code' 子目?。）

    cm ^ls /code --^tree=ae1390ed-7ce9-4ec3-a155-e5a61de0dc77@myrep@denver:7070
    （列出服?器 'denver:7070' 存?? 'myrep'
    的?更集 ae1390ed-7ce9-4ec3-a155-e5a61de0dc77 的
    '/code' 子目?。）

== CMD_DESCRIPTION_TRIGGER_LIST ==
列出服?器上?定?型的触?器。

== CMD_USAGE_TRIGGER_LIST ==
用法：

    cm ^trigger | ^tr ^list | ^ls [<子?型-?型>] [--^server=<存??服?器?格>]
                          [--^format=<格式字符串>]

??：

    子?型-?型        触?器?行和触?器操作。
                        ?入 'cm ^showtriggertypes' 可?看触?器?型
                        列表。
    --^server            列出指定服?器上的触?器。
                        如果未指定服?器，?在客?端上配置的
                        服?器上?行命令。
                        （使用 'cm ^help ^objectspec' 可?一?了解服?器
                        ?格。）
    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。

== CMD_HELP_TRIGGER_LIST ==
?注：

    如果未指定?型，?列出服?器上的所有触?器。

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：
        {0}             触?器位置。
        {1}             触?器名称。
        {2}             触?器路径。
        {3}             触?器所有者。
        {4}             触?器?型。
        {5}             触?器??器。

示例：
    cm ^trigger list after-mklabel
    cm ^tr ^ls ^before-mkbranch --^server=myserver:8084

== CMD_DESCRIPTION_MANIPULATESELECTOR ==
将??器更改?日期。

== CMD_USAGE_MANIPULATESELECTOR ==
?是一个自?化命令，?用于自??行 'cm'。
此命令的用?友好度不如?期。

用法：

    cm ^manipulateselector | ^ms [<工作区路径> | <工作区?格>] --^atdate=<??器日期>

    工作区路径             工作区的路径。
    工作区?格             工作区的?格。（使用 'cm ^help ^objectspec' 可
                        ?一?了解?格。）
??：

    --^atdate            返回一个??器以用于重新?建工作区，
                        就像在指定日期看到的工作区一?。

== CMD_HELP_MANIPULATESELECTOR ==
?注：

    如果路径和工作区?格均未指定，??命令将采用
    当前目?作?工作区路径。

示例：

    cm ^manipulateselector c:\workspace --^atdate=yyyy-MM-ddTHH:mm:ss
    cm ^manipulateselector --^atdate=yyyy-MM-ddTHH:mm:ss
    cm ^manipulateselector > mySelector.txt --^atdate=yyyy-MM-ddTHH:mm:ss
    cm ^manipulateselector ^wk:build_wk@BUILDER --^atdate=yyyy-MM-ddTHH:mm:ss

== CMD_DESCRIPTION_MERGE ==
将一个分支与?一个分支合并。

== CMD_USAGE_MERGE ==
用法：

    cm ^merge <源?格> [--^merge] [--^cherrypicking] [--^forced]
                           [--^mergetype=(^onlyone|^onlysrc|^onlydst|^try|^forced)]
                           [--^interval-origin=<?更集?格> | --^ancestor=<?更集?格>]
                           [--^keepsource | --^ks] [--^keepdestination | --^kd]
                           [--^automaticresolution=<冲突?型>[;...]]
                           [--^subtractive] [--^mount] [--^printcontributors]
                           [--^noprintoperations] [--^silent]
                           [(--^to=<分支?格> | --^destination=<分支?格>)[--^shelve]]
                           [--^no-dst-changes]
                           [-^c=<注?字符串> | --^commentsfile=<注?文件>]
                           [--^resolveconflict --^conflict=<索引>
                           --^resolutionoption=(^src|^dst|(^rename --^resolutioninfo=<字符串名称>))
                           --^mergeresultfile=<路径> --^solvedconflictsfile=<路径>]
                           [--^nointeractiveresolution]
                           [--^machinereadable [--^startlineseparator=<分隔符>]
                             [--^endlineseparator=<分隔符>] [--^fieldseparator=<分隔符>]]

    源?格           要合并的源?象的?格：
                          - 分支?格：'[^br:/]br_name'
                          - ???格：'^lb:lb_name'
                          - ?更集?格：'^cs:cs_number'
                          - ?置?格：'^sh:shelve_number'
                          （使用 'cm ^help ^objectspec' 可?一?了解?格。）

??：

    --^merge                   ?行合并。否?，打印找到的
                              冲突。
    --^cherrypicking           合并源?更集内包含的
                              ?更。如果合并源?格是??，
                              ?不会使用此??。
    --^forced                  不??源和目?是否
                              已?接。
                              此???可用于?隔合并
                              和挑?。
    --^mergetype               ?参?“?注”以了解更多信息。
    --^interval-origin         指定将?个?更集??
                              ?隔原点，以便合并?考?
                              源?更集与指定?隔原点
                              之?的差?。
    --^ancestor                ?是 --^interval-origin 的?名。
    --^keepsource              ?于有冲突的?，接受源参与者
                              的所有更改。
    --^keepdestination         ?于有冲突的?，保留目?参与者
                              的更改。
    --^automaticresolution     用于解决目?冲突。此??
                              可??决定??自???
                              源参与者?是目?参与者
                              来解决冲突。
                              使用分号?各个冲突?型?行分隔。
                              ?参?“?注”以了解更多信息。
    --^subtractive             ?除由合并引入的更改。???
                              命令的参数（源?格）用于
                              指定要?除?个源的
                              更改。必?是?更集。如果是?更集
                              ?隔，必?使用 '--^interval-origin' 来
                              定??隔原点。?了?除更改，
                              系?将?建一个新的已?出修?，
                              ?修?将具有上一个
                              修?的内容（不包含已?除的更改）。
    --^mount                   ?定存??的装入点。
    --^printcontributors       打印参与者（基?、源和
                              目?）。
    --^noprintoperations       以静默方式解决合并??，
                              不?示有?解决情况的信息。
    --^silent                  不?示任何?出。
    --^to | --^destination      ?指定分支?行“合并到”操作
                              （通??入分支?格）
                              并完全解决冲突。
                              “合并到”（或无工作区的合并）是在
                              服?器端?行的合并。正常的合并
                              ?生在“从”分支、??或?更集合并的
                              工作区上，而“合并到”完全?生
                              在服?器上。在正常合并中，
                              “目?”是工作区，而在“合并到”中，
                              必?始?指定目?（?就是称之?
                              “到”的原因）。
                              ??看以下?接以了解?于
                              “合并到”功能的更多信息：
                              https://www.plasticscm.com/download/help/mergeto
    --^shelve                  使用合并?果的更改（加上合并
                              可追溯性信息）来?建一个?置，
                              而不是?建新的?更集。合并源?
                              ?置?，此??不可用。???
                              ?可用于服?器端合并
                              （又称?“合并到”）。因此，需要 '--^to' 和
                              '--^merge' ??。
    --^no-dst-changes          ?保目?参与者不具有
                              更改（目??更集也是
                              共同上?）。目?上存在更改?，
                              不允?合并。
    -^c                        将指定的注??用于合并操作中
                              ?建的?更集。
    --^commentsfile            将指定文件中的注??用于合并操作中
                              ?建的?更集。
    --^resolveconflict         （主要由插件使用。?参?“?注”以了解更多信息。）
                              用于解决目?冲突。
    --^conflict                与 '--^resolveconflict' ?志?合使用，指定
                              要解决的冲突的索引（从 1 ?始）。
    --^resolutionoption        与 '--^resolveconflict' ?志?合使用，指示
                              冲突解决?型。使用以下
                              ??之一：'^src'、'^dst'、'^rename'。
                              ?参?“?注”以了解更多信息。
    --^resolutioninfo          与 '--^resolveconflict' ?志?合使用，提供在
                              '--^resolutionoption' ??? 'rename' ?
                              要使用的名称。
    --^mergeresultfile         与 '--^resolveconflict' ?志?合使用，将
                              不同?用之?的合并?果的信息
                              ?出到文件中。指定的路径将在第一次
                              ?用期??建，并在?个后?
                              ?用?更新。
    --^solvedconflictsfile     与 '--^resolveconflict' ?志?合使用，
                              将不同?用之?已解决的冲突
                              的信息?出到文件中。指定的路径
                              将在第一次?用期??建，并在?个
                              后??用?更新。
    --^nointeractiveresolution （主要由插件使用。?参?“?注”以了解更多信息。）
                              避免提示用?已?生手?冲突。
                              ??就不会解决目?冲突。
    --^machinereadable         （主要由插件使用。?参?“?注”以了解更多信息。）
                              以易于解析的格式?出?果。
    --^startlineseparator      与 '--^machinereadable' ?志?合使用，指定
                              行?如何??。（默??：空字符串。）
    --^endlineseparator        与 '--^machinereadable' ?志?合使用，指定
                              行?如何?尾。（默??：空字符串。）
    --^fieldseparator          与 '--^machinereadable' ?志?合使用，指定
                              ?如何分隔字段。（默??：
                              空格。）

== CMD_HELP_MERGE ==
?注：

    此命令用于合并?个分支之?或??与
    分支之?的更改。合并的目?必?始??分支。
    合并源将指定?参数。
    目?是工作区的当前内容。
    例如，要?示从分支 task001 合并到主分支
    的元素，??器必?指向主分支，
    工作区必?完成更新，然后：
        cm ^merge ^br:/task001

    添加 '--^merge' ??来切??行合并：
        cm ^merge ^br:/task001 --^merge

    要定?合并源，可以使用以下?格：

    - 分支?格：
        [^br:/]br_name
        示例：^br:/main/task001
        （以上示例从此分支上的最后一个?更集?行合并。）

    - ???格：
        ^lb:lb_name
        示例：^lb:BL001
        （从??的?更集合并。）

    - ?更集?格：
        ^cs:cs_number
        示例：^cs:25
        （从?定的?更集内容合并。）

    - ?置?格：
        ^sh:shelve_number
        示例：^sh:2
        （从?定的?置内容合并。）

    要自?解决目?冲突，?使用 '--^automaticresolution'
    ??并指定冲突?型，然后指定在合并操作
    期?必???的参与者（源或目?）。
    （使用分号 (;) ?各个“冲突?型”-“参与者”??行分隔。）
    例如：
        cm ^merge ^cs:2634 --^merge --^automaticresolution=^eviltwin-src;^changedelete-src
        （自?更集 2634 ?始的合并操作将会解决 "^eviltwin" 和
        "^changedelete" 冲突，解决方法是在??情况下保留源 ("-^src")
        参与者）。
    - 冲突?型之后的 "-^src" 后?告?合并命令保留
      源参与者更改。
    - "-^dst" 后?将保留目?参与者更改。
    以下是合并命令支持的冲突?型的列表：
      "^movedeviltwin"、"^eviltwin"、"^changedelete"、"^deletechange"、"^movedelete"、
      "^deletemove"、"^loadedtwice"、"^addmove"、"^moveadd"、"^divergentmove"、
      "^cyclemove"、"^all"。
    "^all" ?会覆盖其他??。在以下示例中，
    "^eviltwin-dst" 将被忽略：
        cm ^merge ^br:/main/task062 --^merge --^automaticresolution=^all-src;^eviltwin-dst
    ??看以下?接以了解?于合并冲突的更多信息：
    https://www.plasticscm.com/download/help/directorymerges

    以下是 '--^mergetype' 的??：
        ^onlyone         ?有一个参与者修改了??的情况下，才?行
                        自?合并。
        ^onlysrc         ?有一个源参与者修改了??的情况下，才?行
                        自?合并。
        ^onlydst         ?有一个目?参与者修改了??的情况下，才?行
                        自?合并。
        ^try             ?有一个参与者修改了冲突代?段（?个冲突）
                        的情况下，才?行自?合并。
        ^forced          始???解决所有非自?冲突。

    以下是主要由插件和集成使用的??：
        - '--^resolveconflict'，用于解决目?冲突。?必?使用
          以下??：
              - '--^conflict' 是要解决的冲突的索引
                （从 1 ?始）。
              - '--^resolutionoption' 指示要使用的冲突解决
                 方法。??可以是：
                    - '^src'，用于保留源更改而放弃
                      目?更改
                    - '^dst'，用于保留目?更改而放弃
                      源更改
                    - '^rename'（?在冲突?型支持此解决方法
                      的情况下），用于将目?重命名?
                      通? '--^resolutioninfo' ??提供的?定名称。
                        - '--^resolutioninfo'，用于提供在 '^rename' 解决
                          方法中使用的名称
              - '--^mergeresultfile' 和 '--^solvedconflictsfile'，??者用于
                存?不同?用之?的合并信息。
        - '--^nointeractiveresolution' 指示合并操作不要求用?
          ?行手?冲突解决。
        - '--^machinereadable' 和 '--^startlineseparator'、'--^endlineseparator'、
          '--^fieldseparator' ??，用于以机器可?的方式
          （易于解析）打印?出。
        示例：
        cm ^merge --^machinereadable --^startlineseparator=start@_@line \
          --^endlineseparator=new@_@line --^fieldseparator=def#_#sep \
          --^mergeresultfile=C:\Users\Borja\AppData\Local\Temp\2tmp4D6C.tmp \
          --^solvedconflictsfile=C:\Users\Borja\AppData\Local\Temp\2tmp4D6D.tmp \
          --^resolveconflict --^conflict=1 --^resolutionoption=rename  \
          --^resolutioninfo=bin_dst ^br:/main/task --^merge

示例：

    cm ^merge ^br:/task001
    （不合并，?打印要合并的?。）

    cm ^merge ^br:/task001 --^merge
    （从分支 'task001' ?行合并。）

    cm ^merge ^cs:5 --^merge --^cherrypicking --^interval-origin=^cs:2
    （从?更集?隔 (2,5] ?行挑?。）

    cm ^merge ^cs:8 --^merge --^subtractive --^keepdestination
    （从?更集 8 ?行?法合并，保留那些有冲突的
    元素的目?更改。）

    cm ^merge ^br:/main/task001 --^to=^br:/main --^merge -^c="集成的新 UI"
    （?行从分支 'task001' 到分支 'main' 的服?器端合并
    （又称?“合并到”），并?置注?。）

    cm ^merge ^br:/main/task001 --^to=^br:/main --^merge --^shelve
    （?行从分支 'task001' 到分支 'main' 的服?器端合并，
    并将?果保留在?置中。）

    cm ^merge ^sh:2 --^to=^br:/main --^merge --^no-dst-changes
    （?在从当前的 'main' ?部?建?，才将?置 2
    ?用于 'main'）

== CMD_DESCRIPTION_ATTRIBUTE_CREATE ==
?建新的属性。

== CMD_USAGE_ATTRIBUTE_CREATE ==
用法：

    cm ^attribute | ^att ^create | ^mk <属性名>

    属性名            属性名称

== CMD_HELP_ATTRIBUTE_CREATE ==
示例：

    cm ^attribute ^create 状?
    （?建属性 '状?'。）

    cm ^att ^mk 集成
    （?建属性 '集成'。）

== CMD_DESCRIPTION_BRANCH ==
允?用?管理分支。

== CMD_USAGE_BRANCH ==
用法：

    cm ^branch | ^br <命令> [??]

命令：

    ^create | ^mk
    ^delete | ^rm
    ^rename
    ^history
    ^showmain
    ^showmerges

    要?取有??条命令的更多信息，??行：
    cm ^branch <命令> --^usage
    cm ^branch <命令> --^help

== CMD_HELP_BRANCH ==
示例：

    cm ^branch /main/scm21345
    cm ^branch ^create /main/scm21345
    cm ^branch ^delete /main/scm21345
    cm ^branch ^rename /main/scm21345 scm21346
    cm ^branch ^history /main/scm21345
    cm ^branch ^showmain
    cm ^branch ^showmerges file.txt

== CMD_DESCRIPTION_BRANCH_CREATE ==
?建新的分支。

== CMD_USAGE_BRANCH_CREATE ==
用法：

    cm ^branch | ^br [^create | ^mk] <分支?格>
                   [--^changeset=<?更集?格> | --^label=<???格>]
                   [-^c=<注?字符串> | -^commentsfile=<注?文件>]

    分支?格         新分支名称或?格。
                   （使用 'cm ^help ^objectspec' 可?一?了解分支?格。）

??：

    --^changeset     用作新分支的起点的?更集。
                    （使用 'cm ^help ^objectspec' 可?一?了解?更集?格。）
    --^label         用作新分支的起点的??。
                    （使用 'cm ^help ^objectspec' 可?一?了解???格。）
    -^c              用指定的文本填充新分支的“注?”
                    字段。
    -^commentsfile   用指定文件的内容填充新分支的“注?”
                    字段。

== CMD_HELP_BRANCH_CREATE ==
?注：

    要?建??分支，?指定不含任何?次??的名称。
    例如：

        cm ^br /dev

    如果未指定可?参数 '--^changeset'，新分支的基?
    将是父分支上的最后一个?更集。如果新分支
    是??分支，?使用的基??更集将??更集 0。

    可以使用 '-^c' 或 '-^m' ??来指定注?：

        cm ^branch /main/task001 -^c="?是注?"
        cm ^branch /main/task001 -^m "?是注?"

    ?置 PLASTICEDITOR ?境?量可指定用于?入注?的
    ??器。如果已?置 PLASTICEDITOR ?境?量，
    并且注??空，?会自?????器
    以用于指定注?。

示例：

    cm ^branch task001
    cm ^branch ^create task001
    cm ^branch ^mk task001
    cm ^br ^mk task001
    （在当前工作区的存??中?建??分支
    'task001'。）

    cm ^branch ^br:/task001/task002@
    （?建 'task002' 分支作? 'task001' 的子?。）

    cm ^br /main/task001@myrep@myserver:8084 -^c="我的注?"
    （在存?? 'myrep@myserver:8084' 中，?建 'task001' 分支
    作? 'main' 的子?，并包含注? '我的注?'。）

    cm ^branch ^br:/main/task001 --^changeset=2837 -^commentsfile=commenttask001.txt
    （?建 'task001' 分支作? 'main' 的子?，基??更集 'changeset=2837'，
    并?用 'commenttask001.txt' 文件中的注?。）

== CMD_DESCRIPTION_BRANCH_DELETE ==
?除一个或多个分支。

== CMD_USAGE_BRANCH_DELETE ==
用法：

    cm ^branch | ^br ^delete | ^rm <分支?格>[ ...]

    分支?格              要?除的分支。使用空格?各个分支?行分隔。
                        （使用 'cm ^help ^objectspec' 可?一?了解分支
                        ?格。）

== CMD_HELP_BRANCH_DELETE ==
?注：

    此命令将?除一个或多个分支。

示例：

    cm ^branch ^delete /main/task001
    （在当前工作区的存??中?除名? 'task001' 分支
    （'main' 的子?）。）

    cm ^br ^rm main/task002 /main/task012@reptest@myserver:8084
    （?除当前工作区的存??中的分支 '/main/task002 
    以及存?? 'reptest@myserver:8084' 中的分支 '/main/task012'。）

== CMD_DESCRIPTION_BRANCH_RENAME ==
重命名分支。

== CMD_USAGE_BRANCH_RENAME ==
用法：

    cm ^branch | ^br ^rename <分支?格> <新名称>

    分支?格          要重命名的分支。
                    （使用 'cm ^help ^objectspec' 可?一?了解分支?格。）
    新名称        分支的新名称。

== CMD_HELP_BRANCH_RENAME ==
?注：

    此命令将重命名分支。

示例：

    cm ^branch ^rename /main/task0 task1
    （将分支 '/main/task0' 重命名? '/main/task1'。）

    cm ^br ^rename ^br:/main@reptest@server2:8084 secondary
    （将存?? 'reptest' 的 'main' 分支重命名? 'secondary'。）

== CMD_DESCRIPTION_BRANCH_HISTORY ==
?示分支的?史??。

== CMD_USAGE_BRANCH_HISTORY ==
用法：

    cm ^branch | ^br ^history <分支?格> [--^dateformat=<日期格式>]
                           [--^machinereadable]

    分支?格          用于?取?史??的分支?格。
                    （使用 'cm ^help ^objectspec' 可?一?了解分支?格。）

??：

    --^dateformat            用于?出日期的格式。
    --^machinereadable       以易于解析的格式?出?果。

== CMD_HELP_BRANCH_HISTORY ==
示例：

    cm ^branch ^history ^br:/main/scm001@myrepository@myserver:8084
    （?示 'myserver' 服?器中的 'myrepository' 存??的 '/main/scm001'
    分支的?史??。）

    cm ^br ^history main --^dateformat="yyyy, dd MMMM" --^machinereadable
    （以?定的日期格式和易于解析的格式，?示当前
    存??的 'main' 分支的?史??。）

== CMD_DESCRIPTION_BRANCH_SHOWMAIN ==
?示存??的主分支。
?是一个自?化命令，?用于自??行 'cm'。
存??的主分支很有可能是 '/main'。

== CMD_USAGE_BRANCH_SHOWMAIN ==
用法：

    cm ^branch | ^br ^showmain [<存???格>] [--^encoding=<名称>]
                            [--^format=<格式字符串>] [--^dateformat=<日期格式>]

    存???格             要?示分支的存??
                        ?格。
                        （使用 'cm ^help ^objectspec' 可?一?了解存???格。）

??：

    --^encoding          指定?出中使用的??，
                        如：utf-8。
                        ?参?位于以下网址的 MSDN 文档：
                        http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                        以?看包含受支持??及其格式的表格
                        （位于?面末尾的“名称”列中）。
    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。
    --^dateformat        用于?出日期的格式。

== CMD_HELP_BRANCH_SHOWMAIN ==
?注：

    此命令将?示存??的主分支。

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：
        {^id}                分支 ID。
        {^comment}           注?。
        {^date}              日期。
        {^name}              名称。
        {^owner}             所有者。
        {^parent}            父分支名称。
        {^repository}        存??。
        {^repname}           存??名称。
        {^repserver}         服?器。
        {^changeset}         分支的?部?更集。

示例：

    cm ^branch ^showmain
    （?示当前工作区的存??的主分支。）

    cm ^branch ^showmain repo@server:8084
    （?示服?器 'server:8084' 中的存?? 'repo' 的
    主分支。）

    cm ^br ^showmain --^dateformat="yyyy, dd MMMM" --^encoding=utf8
    （以?定日期格式?示存??的主分支，
    ?出??? utf8。）

    cm ^br ^showmain --^format="{^id} - {^name}"
    （?示存??的主分支，?打印其 ID 和名称。）

== CMD_DESCRIPTION_BRANCH_SHOWMERGES ==
?示待合并的分支。

== CMD_USAGE_BRANCH_SHOWMERGES ==
?是一个自?化命令，?用于自??行 'cm'。
此命令的用?友好度不如?期。

用法：

    cm ^branch | ^br ^showmerges <?路径>[ ...]
                              [--^format=<格式字符串>]
                              [--^dateformat=<日期格式>]

??：
    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。
    --^dateformat        用于?出日期的格式。

== CMD_HELP_BRANCH_SHOWMERGES ==
?注：

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：
        {^id}                分支 ID。
        {^comment}           注?。
        {^date}              日期。
        {^name}              名称。
        {^owner}             所有者。
        {^parent}            父分支名称。
        {^parentid}          父分支 ID。
        {^repid}             存?? ID。
        {^repository}        存??。
        {^repname}           存??名称。
        {^repserver}         存??服?器。

示例：

    cm ^branch ^showmerges file.txt
    （?示 'file.txt' 的待定合并中?及的分支。）

    cm ^branch ^showmerges file.txt --^format="{^date} {^name}" --^dateformat="yyMMdd"
    （?示合并中?及的分支，?以?定的日期格式打印
    日期和名称。）

== CMD_DESCRIPTION_REPOSITORY ==
允?用?管理存??。

== CMD_USAGE_REPOSITORY ==
用法：

    cm ^repository | ^repo <命令> [??]

命令：

    ^create | ^mk
    ^delete | ^rm
    ^list   | ^ls
    ^rename
    ^add

    要?取有??条命令的更多信息，??行：
    cm ^repository <命令> --^usage
    cm ^repository <命令> --^help

== CMD_HELP_REPOSITORY ==
示例：

    cm ^repository
    cm ^repository ^list
    cm ^repository 新存??
    cm ^repository ^create 新存??
    cm ^repository ^rename 旧名称 新名称
    cm ^repository ^add C:\repo\

== CMD_DESCRIPTION_REPOSITORY_CREATE ==
在服?器上?建存??。

== CMD_USAGE_REPOSITORY_CREATE ==
用法：

    cm ^repository | ^repo <存??名称>
    cm ^repository | ^repo <存??服?器?格> <存??名称>[ ...]
    cm ^repository | ^repo [^create | ^mk] <存??名称>

    存??服?器?格       存??服?器?格。
                        （使用 'cm ^help ^objectspec' 可?一?了解存??
                        服?器?格。）
    存??名称            一个或多个新存??的名称。
                        使用空格?各个存??名称?行分隔。

== CMD_HELP_REPOSITORY_CREATE ==
示例：

    cm ^repository MyRep
    cm ^repo 192.168.1.140:8087 Rep01 Rep01/ModuleA Rep01/ModuleB
    cm ^repo ^create Rep01
    cm ^repo ^mk 列表

== CMD_DESCRIPTION_REPOSITORY_DELETE ==
从服?器?除存??。

== CMD_USAGE_REPOSITORY_DELETE ==
用法：

    cm ^repository | ^repo ^delete | ^rm <存???格>

??：

    存???格            存??的?格。
                       （使用 'cm ^help ^objectspec' 可?一?了解存???格。）

== CMD_HELP_REPOSITORY_DELETE ==
?注：

    从存??服?器?除存??。
    不会从数据?后端?除数据，而是会断?，
    因此将无法再?行??。
    （以后可以重新?接数据，?参? 'cm ^repository ^add'。）

示例：

    cm ^repository ^delete myrepository@^repserver:myserver:8084
    cm ^repository ^rm myrepository@myserver:8084
    cm ^repo ^rm myrepository

== CMD_DESCRIPTION_REPOSITORY_LIST ==
列出服?器上的存??。

== CMD_USAGE_REPOSITORY_LIST ==
用法：

    cm ^repository | ^repo [^list | ^ls] [<存??服?器?格>] [--^format=<格式字符串>]

??：

    存??服?器?格       存??服?器?格。
                        （使用 'cm ^help ^objectspec' 可?一?了解存??
                        服?器?格。）
    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。

== CMD_HELP_REPOSITORY_LIST ==
?注：

    此命令接受格式字符串以?示?出。

    此命令的?出参数如下：

        {^repid}     | {0}           存????符。
        {^repname}   | {1}           存??名称。
        {^repserver} | {2}           服?器名称。
        {^repowner}  | {3}           存??所有者。
        {^repguid}   | {4}           存??的唯一??符。
        {^tab}                       插入一个制表符空格位。
        {^newline}                   插入一个新行。

    如果格式参数?? '^TABLE'，?会使用具有 {^repid}、
    {^repname} 和 {^repserver} 字段的表格式来打印?出。

示例：

    cm ^repository
    （列出所有存??。）

    cm ^repository ^list localhost:8084 --^format="{1, -20} {3}"
    （将存??名称写入 20 个空格位，左??，然后留一个空白，
    再然后是存??所有者。）

    cm ^repository ^ls localhost:8084 --^format="{^repname, -20} {^repowner}"
    （写入方式与前一个示例相同。）

    cm ^repo ^ls localhost:8084 --^format=^TABLE
    （使用具有以下字段的表格式来写入存??列表：
    存?? ID、存??名称和存??服?器名称。）

== CMD_DESCRIPTION_REPOSITORY_RENAME ==
重命名存??。

== CMD_USAGE_REPOSITORY_RENAME ==
用法：

    cm ^repository | ^repo ^rename [<存???格>] <新名称>

    存???格             要重命名的存??。
                        （使用 'cm ^help ^objectspec' 可?一?了解存??
                        ?格。）
    新名称            存??的新名称。

== CMD_HELP_REPOSITORY_RENAME ==
?注：

    此命令将重命名存??。
    如果未指定存???格，?会采用当前存??。

示例：

    cm ^repository ^rename development
    （将当前存??重命名? 'development'。）

    cm ^repo ^rename ^rep:default@SERVER:8084 development
    （将 'SERVER' 上的 'default' 存??重命名? 'development'。）

== CMD_DESCRIPTION_REPOSITORY_ADD ==
通?添加?有存??的数据?来?接此存??。

== CMD_USAGE_REPOSITORY_ADD ==
用法：

    cm ^repository | ^repo ^add <数据?文件> <存??名称> <存??服?器?格>

    数据?文件             数据?后端上的数据?文件的名称。
    存??名称            存??的名称。
    存??服?器?格       存??服?器?格。
                        （使用 'cm ^help ^objectspec' 可?一?了解存??
                        服?器?格。）

== CMD_HELP_REPOSITORY_ADD ==
?注：

    将?有存??数据?重新?接到服?器。
    示例：使用 'cm ^repository ^delete' 命令后，使用 '^add' 命令
    将存??从一个服?器移?到?一个服?器或?原
    已存档的存??。

示例：

    cm ^repository ^add rep_27 myrepository myserver:8084

== CMD_DESCRIPTION_TRIGGER_CREATE ==
在服?器上?建新触?器。

== CMD_USAGE_TRIGGER_CREATE ==
用法：

    cm ^trigger | ^tr ^create | ^mk <子?型-?型> <新名称> <脚本路径>
                                [--^position=<新位置>]
                                [--^filter=<??器字符串>]
                                [--^server=<存??服?器?格>]

    子?型-?型        触?器?行和触?器操作。
                        ?入 'cm ^showtriggertypes' 可?看触?器?型
                        列表。
    新名称            新触?器的名称。
    脚本路径         服?器上包含要?行的脚本的
                        磁?路径。如果命令行以 "^webtrigger " ??，
                        ???此触?器是 Web 触?器。?参?
                        “?注”以了解更多信息。

??：

    --^position          指定触?器的新位置。
                        此位置不能正在由相同?型的?一个
                        触?器使用。
    --^filter            ???与指定??器匹配的?。
    --^server            在指定服?器上?建触?器。
                        如果未指定服?器，?在客?端上配置的
                        服?器上?行命令。
                        （使用 'cm ^help ^objectspec' 可?一?了解存??
                        服?器?格。）

== CMD_HELP_TRIGGER_CREATE ==
?注：

    Web 触?器：?建 Web 触?器的方法是?入 "^webtrigger <目?-URI>"
    作?触?器命令。在??情况下，触?器将??指定的 URI
    ?行 POST ??，其中，?求主体包含?有触?器
    ?境?量的 JSON 字典，?有一个指向字符串数?的
    固定 INPUT ?。

示例：

    cm ^trigger ^create ^after-setselector "BackupMgr" "/path/to/script" --^position=4

    cm ^tr ^mk ^before-mklabel new "/path/to/script" --^server=myserver:8084

    cm ^tr ^mk ^after-mklabel Log "/path/to/script" --^filter="^rep:myRep,LB*"
    （?当??名称以 'LB' ??并且??是在名? 'myRep' 的
    存??中?建?，才会?行此触?器。）

    cm ^tr ^mk ^after-checkin NotifyTeam "^webtrigger http://myserver.org/api"

== CMD_DESCRIPTION_MOVE ==
移?或重命名文件或目?。

== CMD_USAGE_MOVE ==
用法：

    cm ^move | ^mv <源路径> <目?路径> [--^format=<格式字符串>]
                 [--^errorformat=<格式字符串>]

    源路径            源?的路径。
    目?路径            目??的路径。

??：

    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。
    --^errorformat       ?索特定格式的??消息。?参?
                        “?注”以了解更多信息。

== CMD_HELP_MOVE ==
?注：

    此命令将移?或重命名存??中的?。
    ?会在本地文件系?中?行更改。

    如果源路径是文件，?目?路径可以是文件或
    目?。在第一?情况下会重命名文件；否?会移?
    相?的?。
    如果源路径是目?，?目?路径必?是目?。

    要移?或重命名的?必?已存在。

    格式：
        {0}         源路径（'--^format' 和 '--^errorformat'）
        {1}         目?路径（'--^format' 和 '--^errorformat'）

示例：

    cm ^move file.txt file.old
    （重命名相?的?。）

    cm ^mv .\file.old .\oldFiles
    （将 'file.old' 移到 'oldFiles'。）

    cm ^move .\src .\src2
    （重命名目?。）

== CMD_DESCRIPTION_LABEL ==
允?用?管理??。

== CMD_USAGE_LABEL ==
用法：

    cm ^label | ^lb <命令> [??]

命令：

    ^create | ^mk
    ^delete | ^rm
    ^rename

    要?取有??条命令的更多信息，??行：
    cm ^label <命令> --^usage
    cm ^label <命令> --^help

== CMD_HELP_LABEL ==
示例：

    cm ^label myNewLabel ^cs:42
    （'^create' 命令是可?命令。）
    
    cm ^label ^rename myNewLabel newLabelName
    cm ^label ^delete newLabelName

== CMD_DESCRIPTION_LABEL_CREATE ==
将???用于?更集，并在需要??建??。

== CMD_USAGE_LABEL_CREATE ==
用法：

    cm ^label [^create] <???格> [<?更集?格> | <工作区路径>]
                        [--^allxlinkedrepositories]
                        [-^c=<注?字符串> | -^commentsfile=<注?文件>]

    ???格              新??名称。
                        （使用 'cm ^help ^objectspec' 可?一?了解??
                        ?格。）
    ?更集?格            要??的?更集的名称或完整?格。
                        （使用 'cm ^help ^objectspec' 可?一?了解?更集
                        ?格。）
    工作区路径             要??的工作区的路径。（工作区指向的
                        ?更集将被??。）

??：

    --^allxlinkedrepositories  在所有通? Xlink ?接的存??中?建新??。
    -^c                        将指定的注??用于新??。
    -^commentsfile             将指定文件中的注??用于
                              新??。

== CMD_HELP_LABEL_CREATE ==
?注：

    ?置 PLASTICEDITOR ?境?量可指定用于?入注?的
    ??器。

示例：

    cm ^label ^create ^lb:BL001 ^cs:1203 -^commentsfile=commentlb001.txt
    （?建附加到?更集 1203 的?? 'BL001'，并?用 'commentlb001.txt'
    文件中的注?。）

    cm ^label BL002 ^cs:1203 -^c="首次?布"
    （?建?有注?的?? 'BL002'，并附加到?更集 1203。）

== CMD_DESCRIPTION_LABEL_DELETE ==
?除一个或多个??。

== CMD_USAGE_LABEL_DELETE ==
用法：

    cm ^label ^delete <???格>[ ...]

    ???格          要?除的??。使用空格?各个???行分隔。
                    （使用 'cm ^help ^objectspec' 可?一?了解??
                    ?格。）

== CMD_HELP_LABEL_DELETE ==
?注：

    此命令将?除一个或多个??。

示例：

    cm ^label ^delete ^lb:BL001
    （?除?? 'BL001'。）

    cm ^label ^delete ^lb:BL001 ^lb:BL002@reptest@server2:8084
    （?除?? 'BL001' 和 'BL002'。）

== CMD_DESCRIPTION_LABEL_RENAME ==
重命名??。

== CMD_USAGE_LABEL_RENAME ==
用法：

    cm ^label ^rename <???格> <新名称>

    ???格          要重命名的??。
                    （使用 'cm ^help ^objectspec' 可?一?了解???格。）
    新名称        ??的新名称。

== CMD_HELP_LABEL_RENAME ==
?注：

    此命令将重命名??。

示例：

    cm ^label ^rename ^lb:BL001 BL002
    （将?? 'BL001' 重命名? 'BL002'。）

== CMD_DESCRIPTION_OBJECTSPEC ==
描述如何?写?象?格。

== CMD_USAGE_OBJECTSPEC ==
用法：
    cm ^objectspec
    ?取?于如何?建?象?格的所有信息。

== CMD_HELP_OBJECTSPEC ==
一些 Unity VCS 命令要求将 '?象?格' 作??入来引用
?定?象（通常是分支、?更集、存??等）。

本文档介?可用的不同“?格”以及如何
?建?格。

?个?格?型以唯一??（例如 "^rep:" 或 "^cs:"）??。必??
采用一般?象?格的命令指定??（例如：
"cm ^setowner ?象?格" 命令），但?于?采用?一?型?格的命令
常常可以省略??（例如："cm ^getfile 修??格" 命令）。

-- 存??服?器?格 --
    ^repserver:name:port

    示例：
        cm ^repo ^list ^repserver:skull:8084
        cm ^repo ^list skull:8084

    旁注：
        由于?史原因，我?称之?“存??服?器?格”，而不只是
        “服?器?格”。很久以前，我?有?独的工作区服?器和
        存??服?器，命名方式沿用至今。

-- 存???格 --
    ^rep:rep_name@[存??服?器?格]

    示例：
        cm ^showowner ^rep:codice@localhost:6060
        （此?的 "^rep:" 是必需的，因? ^showowner 不?容?存??，
        而且容?其他?型的?象。因此，需要用?指明
         ?象?型。）

-- 分支?格 --
    ^br:[/]br_name[@repspec]

    示例：
        cm ^switch ^br:/main@^rep:plastic@^repserver:skull:9095
        （??情况下不需要 "^br:"、"^rep" 和 "^repserver"，所以
         ?命令容??短很多的格式：
        "cm ^switch main@plastic@skull:9095"。）

        cm ^find ^revisions "^where ^branch='^br:/main/task001'"

    ?注：
        分支上的初始 '/' 不是必需的。我?曾?将所有分支
        指定? /main、/main/task001，依此?推。但是?在，我?首?
        更短的 main、main/task001 形式，?使命令更??。

-- ?更集?格 --
    ^cs:cs_number|cs_GUID[@repspec]

    可以指定?更集的?号或 GUID。

    示例：
        cm ^ls /code --^tree=ae1390ed-7ce9-4ec3-a155-e5a61de0dc77@code@skull:7070

-- ???格 --
    ^lb:lb_name[@repspec]

    示例：
        cm ^switch ^lb:RELEASE2.0
        cm ^switch ^lb:RELEASE1.4@myrep@MYSERVER:8084

-- 修??格 --
有不同?型的修??格：

    ^rev:item_path[#(分支?格|?更集?格|???格)]

    ^rev:^serverpath:item_path#(分支?格|?更集?格|???格)

    ^rev:^revid:rev_id[@rep_spec]

    ^rev:^itemid:item_id#(分支?格|?更集?格|???格)

    示例：
        cm ^diff ^rev:readme.txt#^cs:19 ^rev:readme.txt#^cs:20

        cm ^diff ^serverpath:/doc/readme.txt#^cs:19@myrepo \
            ^serverpath:/doc/readme.txt#^br:/main@myrepo@localhost:8084

        cm ^cat ^revid:1230@^rep:myrep@^repserver:myserver:8084

-- ??格 --
    ^item:path
    ?少使用。

    示例：
        cm ^find ^revision "^where ^item='^item:.'"

-- 属性?格 --
    ^att:att_name[@repspec]

    示例：
        cm ^attribute ^set ^att:merged@code@doe:8084 ^cs:25@code@doe:8084 完成

-- ?置?格 --
    ^sh:sh_number[@repspec]

    示例：
        cm ^diff ^sh:2 ^sh:4

-- 工作区?格 --
    ^wk:name@clientmachine

?少使用，因???用于工作区相?的命令。?合用于
按名称和机器（而不是路径）来指定工作区。

    示例：
        cm ^showselector ^wk:codebase@modok

    旁注：
        ?些?格来自早期的 Plastic SCM 2.x，当?的
        “工作区服?器”是一?集中存?工作区元数据
        的方式。后期由于性能??而被弃用。

== CMD_DESCRIPTION_PARTIAL ==
在非完整的工作区中?行命令。

== CMD_USAGE_PARTIAL ==
用法：

    cm ^partial <命令> [??]

命令：

    ^configure
    ^add
    ^undo
    ^co   | ^checkout
    ^unco | ^undocheckout
    ^ci   | ^checkin
    ^mv   | ^move
    ^rm   | ^remove
    ^stb  | ^switch
    ^upd  | ^update

    要?取有??条命令的更多信息，??行：
    cm ^partial <命令> --^usage
    cm ^partial <命令> --^help

== CMD_HELP_PARTIAL ==
示例：

    cm ^partial ^configure +/background-blue.png
    cm ^partial ^update landscape-1024.png
    cm ^partial ^checkin eyes-green.png eyes-black.png

== CMD_DESCRIPTION_PARTIAL_ADD ==
向版本控制中添加?。

== CMD_USAGE_PARTIAL_ADD ==
用法：

    cm ^partial ^add [-^R | -^r | --^recursive] [--^silent] [--^parents]
                   [--^ignorefailed] [--^skipcontentcheck] <?路径>[ ...]

    ?路径           要添加的?。使用双引号 (" ") 指定包含空格
                        的路径。使用空格?各个路径?行分隔。
                        使用 * 添加当前目?的所有内容。

??：

    -^R                  以??方式添加?。
    --^silent            不?示任何?出。
    --^parents           在操作中包含指定?
                        的父目?。
    --^ignorefailed      如果无法添加某一?，?添加操作将在没有
                        ?一?的情况下???行。注意：如果无法添加
                        某个目?，?不会添加目?中的内容。
    --^skipcontentcheck  如果不足以根据?展名将文件?置?
                        文本或二?制，默?情况下会将?文件?置?二?制，
                        而不是??内容来???型。

== CMD_HELP_PARTIAL_ADD ==
?注：

    添加?的要求：
    - 要添加的?的父目?必?已事先添加。

示例：

    cm ^partial ^add pic1.png pic2.png
    （添加 'pic1.png' 和 'pic2.png' ?。）

    cm ^partial ^add c:\workspace\picture.png
    （添加路径 'c:\workspace' 中的 'picture.png' ?。）

    cm ^partial ^add -^R c:\workspace\src
    （以??方式添加 'src'。）
    
    cm ^partial ^add --^parents samples\design01.png
    （添加 'design01.png' 文件和 'samples' 父文件?。）
    
    cm ^partial ^add -^R *
    （以??方式添加当前目?的所有内容。）

== CMD_DESCRIPTION_PARTIAL_CHECKIN ==
将更改存?在存??中。

== CMD_USAGE_PARTIAL_CHECKIN ==
用法：

    cm ^partial ^checkin | ^ci [<?路径>[ ...]]
                            [-^c=<注?字符串> | -^commentsfile=<注?文件>]
                            [--^all | -^a] [--^applychanged] [--^keeplock]
                            [--^symlink] [--^ignorefailed]

??：

    ?路径           要?入的?。使用双引号 (" ") 指定
                        包含空格的路径。使用空格?各个路径?行
                        分隔。
                        使用 . 将?入?用到当前目?。
    -^c                  指定在?入操作中?建的?更集
                        的注?。
    -^commentsfile       将指定文件中的注??用于在?入操作中
                        ?建的?更集。
    --^all | -^a          ?包括在指定路径上?行了本地更改、移?
                        和?除的?。
    --^applychanged      将?入操作?用于在工作区中
                        ??到的已更改?以及
                        ?出的?。
    --^keeplock          ?入操作后保持?定?的
                        ?定状?。
    --^symlink           将?入操作?用于符号?接而不是
                        目?。
    --^ignorefailed      无法?用的任何更改（因?无法?得
                        ?定状?（又称?“独占?出”）或者
                        因?本地更改与服?器更改冲突）
                        将被?弃，然后没有?些更改的情况下
                        ???行?入操作。

== CMD_HELP_PARTIAL_CHECKIN ==
?注：

    - 如果未指定 <?路径>，??入操作将?及
      工作区中的所有待定更改。
    - ?入操作始?以??方式从?定路径?用。
    - 要?入?，必??足以下条件：
    - ?必?受源代?管理。
    - 必??出?。
    - 如果已更改但未?出?，?除非 <?路径> 是目?
      或包含通配符 ('*')，否?不需要
      '--^applychanged' ?志。

    修?内容必?与以前的修?不同才能
    ?入。

    ?置 PLASTICEDITOR ?境?量可指定用于?入注?的
    ??器。

从 stdin ?取?入：

    '^partial ^checkin' 命令可从 stdin ?取路径。?此，???
    一个破折号 "-"。
    示例：cm ^partial ^checkin -

    路径将一直?取到有空行?入?止。
    ??情况下允?使用??指定要?入的文件。
    示例：
      dir /S /B *.c | cm ^partial ^checkin --^all -
      （在 Windows 中，?入工作区中的所有 .c 文件。）

示例：

    cm ^partial ^checkin figure.png landscape.png
    （将?入?用于已?出的文件 'figure.png' 和 'landscape.png'。）

    cm ^partial ^checkin .-^commentsfile=mycomment.txt
    （将?入?用于当前目?并?置 'mycomment.txt' 文件
    中的注?。）

    cm ^partial ^ci background.png -^c="我的注?" --^keeplock
    （将?入?用于 'background.png'，包含注?，并保持
    ?定。）

    cm ^partial ^checkin --^applychanged
    （将?入?用于工作区中的所有待定更改。）
    
    cm ^partial ^checkin ?接 --^symlink
    （将?入?用于?接文件而非目?，?用于
    UNIX ?境。）

    cm ^partial ^checkin .--^ignorefailed
    （将?入?用于当前目?，忽略无法?用的
    更改。）

== CMD_DESCRIPTION_PARTIAL_CHECKOUT ==
将文件???修改就?。

== CMD_USAGE_PARTIAL_CHECKOUT ==
用法：

    cm ^partial ^checkout | ^co [<?路径>[ ...]] [--^resultformat=<格式字符串>]
                             [--^silent] [--^ignorefailed]

??：

    ?路径           要?出的?。使用双引号 (" ") 指定
                        包含空格的路径。使用空格?各个路径?行
                        分隔。
                        使用 . 将?出?用到当前目?。
    --^resultformat      ?索特定格式的?出?果
                        消息。
    --^silent            不?示任何?出。
    --^ignorefailed      如果无法?定某?（无法?行独占?出），
                        ??出操作将在没有?一?的情况下
                        ???行。

== CMD_HELP_PARTIAL_CHECKOUT ==
?注：

    要?出?，必??足以下条件：
    - ?必?受源代?管理。
    - 必??入?。
        
    如果在服?器上配置了?（存在 lock.conf），??次
    在某条路径上?行?出?，Unity VCS 都会??是否符合??，
    如果符合，?路径将?于独占?出（?定）状?，以便任何其他人
    都无法同??行?出。
    可使用 'cm ^lock ^list' ?取服?器中的所有?。
    ??看《管理?指南》以了解?定的工作原理：
    https://www.plasticscm.com/download/help/locking

示例：

    cm ^partial ^checkout pic1.png pic2.png
    （?出 'pic1.png' 和 'pic2.png' 文件。）
    
    cm ^partial ^co *.png
    （?出所有 png 文件。）

    cm ^partial ^checkout .
    （?出当前目?。）
    
    cm ^partial ^checkout -^R c:\workspace\src
    （以??方式?出 'src' 文件?。）

== CMD_DESCRIPTION_PARTIAL_CONFIGURE ==
可以通?在工作区中加?或卸??来配置工作区。

== CMD_USAGE_PARTIAL_CONFIGURE ==
用法：

    cm ^partial ^configure <+|-路径>[ ...][--^silent] [--^ignorefailed]
                         [--^ignorecase] [--^restorefulldirs]

    路径           要加?或卸?的路径。使用双引号 (" ")
                   指定包含空格的路径。使用空格?各个路径?行
                   分隔。
                   路径必?以 "/" ??。

??：

    --^silent            不?示任何?出。
    --^ignorefailed      跳???程中的所有??。??路径
                        不会?致命令停止。
    --^ignorecase        忽略路径中的大小写。使用此?志后，
                        即使用?写入 "/data/teXtures"，'^configure' 也?
                        "/Data/Textures" 有效。
    --^restorefulldirs   重置无效的目?配置（在非完整的工作区上
                        ?行完整操作??生）。
                        此列表中的目?将??完全配置
                        （完全??），?意味着?些目?将在
                        更新期?自?下?新内容。
                        此操作不会下?任何文件，只会
                        在非完整的工作区上?原目?
                        配置。

== CMD_HELP_PARTIAL_CONFIGURE ==
?注：

    ?命令采用??操作。

示例：

    cm ^partial ^configure +/landscape_grey.png
    （加? 'landscape_grey.png' ?。）

    cm ^partial ^configure -/landscape_black.png
    （卸? 'landscape_black.png' ?。）

    cm ^partial ^configure +/soft -/soft/soft-black.png
    （加?除 'soft-black.png' 以外的所有 'soft' 目?子?。）

    cm ^partial ^configure -/
    （卸?整个工作区。）

    cm ^partial ^configure -/ +/
    （加?整个工作区。）

    cm ^partial ^configure -/figure-64.png --^ignorefailed
    （即使已卸? 'figure-64.png' ?，也要卸???。）
    
    cm ^partial ^configure +/ --^restorefulldirs
    （?置所有目?来自?下?新内容。）
    
    cm ^partial ^configure +/src/lib --^restorefulldirs
    （??置 '/src/lib' 及其子目?来自?下?
    新内容。）

== CMD_DESCRIPTION_PARTIAL_MOVE ==
移?或重命名文件或目?。

== CMD_USAGE_PARTIAL_MOVE ==
用法：

    cm ^partial ^move | ^mv <源路径> <目?路径> [--^format=<格式字符串>]

    源路径            源?的路径。
    目?路径            目??的路径。

??：

    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。

== CMD_HELP_PARTIAL_MOVE ==
?注：

    此命令将移?或重命名存??中的?。
    ?会在本地文件系?中?行更改。
    
    如果源路径是文件，?目?路径可以是文件或
    目?。在第一?情况下将重命名文件；否?将移?
    相?的?。
    如果源路径是目?，?目?路径必?是目?。

    要移?或重命名的?必?已存在。

    格式：
        {0}             源路径。
        {1}             目?路径。

示例：

    cm ^partial ^move file.png file-blue.png
    （重命名相?的?。）

    cm ^partial ^mv .\file-blue.png .\blueFiles
    （将 'file-blue.png' 移?到 'blueFiles'。）

    cm ^partial ^move .\design .\marketing
    （重命名目?。）

== CMD_DESCRIPTION_PARTIAL_REMOVE ==
从版本控制中?除一个文件或目?。

== CMD_USAGE_PARTIAL_REMOVE ==
用法：

    cm ^partial ^remove | ^rm <?路径>[ ...][--^nodisk]

    ?路径       要?除的?路径。使用双引号 (" ")
                    指定包含空格的路径。使用空格?各个路径?行
                    分隔。

??：

    --^nodisk        从版本控制中?除相?的?，但是将??保留在磁?上。

== CMD_HELP_PARTIAL_REMOVE ==
?注：

    磁?中会?除?。?除的?会从源代?管理
    中的父目?中被?除。

    要求：
    - ?必?受源代?管理。

示例：

    cm ^partial ^remove src
    （?除 'src'。如果 'src' 是目?，?等同于：
    cm ^partial ^remove -^R src。）

    cm ^partial ^remove c:\workspace\pic01.png --^nodisk
    （从版本控制中?除 'pic01.png'，但将其保留在磁?上。）

== CMD_DESCRIPTION_PARTIAL_SWITCH ==
将分支?置?工作分支。

== CMD_USAGE_PARTIAL_SWITCH ==
用法：

    cm ^switch <分支?格> [--^report | --^silent] [--^workspace=<路径>]
    （?置工作分支并更新工作区。）

    cm ^switch <分支?格> --^configure <+|-路径>[ ...][--^silent]
                            [--^ignorefailed] [--^ignorecase] [--^workspace=<路径>]
    （?置工作分支并?行工作区配置，就像 'cm
    ^partial ^configure' 命令一?。）

    分支?格         分支的?格。（使用 'cm ^help ^objectspec' 可
                        ?一?了解分支?格。）
    路径                要加?或卸?的路径。使用双引号 (" ")
                        指定包含空格的路径。使用空格?各个路径
                        ?行分隔。路径必?以 "/" ??。

??：

    --^silent            不?示任何?出。
    --^report            命令完成后，打印已?用更改
                        的列表。使用 '--^silent' 将会覆盖此?置。
                        此???在未指定 '--^configure' ??
                        ?有效。
    --^configure         更新工作分支后配置
                        （加?/卸??）工作区。?看 'cm ^partial
                        ^configure --^help' 可了解如何指定要配置的
                        路径。
    --^ignorefailed      跳?配置?程中的所有??。
                        ??路径不会?致命令停止。
    --^ignorecase        忽略路径中的大小写。使用此?志后，即使
                        用?写入 "/data/teXtures"，?? '--^configure' 也?
                        "/Data/Textures" 有效。
    --^workspace=路径    工作区所在的路径。

== CMD_HELP_PARTIAL_SWITCH ==
?注：

    此命令允?用?更新工作分支。更新分支之后，
    ?命令就像 'cm ^partial ^update' 命令一?会
    将工作区更新到新分支。然而，如果指定了 '--^configure'
    ??，?命令就像 'cm ^partial ^configure' 命令一?会
    允?使用新的分支配置来配置工作区。

示例：

    cm ^switch ^br:/main/task
    （将 /main/task ?置?工作分支并更新工作区。）

    cm ^switch ^br:/main/task --^configure +/art/images
    （将 /main/task ?置?工作分支，并配置工作区来
    加? /art/images 文件?。）

== CMD_DESCRIPTION_PARTIAL_UNDOCHECKOUT ==
撤??的?出。

== CMD_USAGE_PARTIAL_UNDOCHECKOUT ==
用法：

    cm ^partial ^undocheckout | ^unco <?路径>[ ...][--^silent]

    ?路径           要?用操作的?。使用双引号 (" ")
                        指定包含空格的路径。使用空格?各个路径
                        ?行分隔。
                        使用 . 将操作?用到当前目?。

??：

    --^silent            不?示任何?出。

== CMD_HELP_PARTIAL_UNDOCHECKOUT ==
?注：

    如果已?出某个?并且不想将?个??入，?可以
    使用此命令撤??出。文件和文件?都可以取消
    ?出。?将更新??出之前的状?。

    要求：
      - ?必?受源代?管理。
      - 必??出?。

示例：

    cm ^partial ^undocheckout .
    （撤?当前目?中的?出。）

    cm ^partial ^undocheckout pic1.png pic2.png
    cm ^unco c:\workspace\design01.png
    （撤??出所?文件。）

== CMD_DESCRIPTION_PARTIAL_UNDO ==
撤?工作区中的更改。

== CMD_USAGE_PARTIAL_UNDO ==
用法：

    cm ^partial ^undo [<路径>[ ...]] [--^symlink] [-^r | --^recursive]
                    [<??器>[ ...]]
                    [--^silent | --^machinereadable [--^startlineseparator=<分隔符>]
                                [--^endlineseparator=<分隔符>] [--^fieldseparator=<分隔符>]]

    路径                    要?用操作的文件或目?
                            的路径。使用双引号 (" ") 指定
                            包含空格的路径。使用空格?各个路径
                            ?行分隔。
                            如果未指定路径，?默?情况下，
                            撤?操作将?用于当前目?中的所有
                            文件。
    ??器                  将一个或多个指定??器?用于?定
                            路径。使用空格?各个??器?行分隔。?参?
                            “??器”部分以了解更多信息。

??：

    --^symlink               将撤?操作?用于符号?接而不是
                            目?。
    -^r                      以??方式?行撤?。
    --^silent                不?示任何?出。
    --^machinereadable       以易于解析的格式?出?果。
    --^startlineseparator    与 '--^machinereadable' ?志?合使用，指定
                            行?如何??。
    --^endlineseparator      与 '--^machinereadable' ?志?合使用，指定
                            行?如何?尾。
    --^fieldseparator        与 '--^machinereadable' ?志?合使用，指定
                            ?如何分隔字段。

??器：

    如果未指定?志，?默?情况下将撤?所有更改，但是
    可以使用以下一个或多个?志来??路径。
    如果文件或目?匹配一个或多个指定??的更改，
    ?将撤??文件或目?上的所有更改。
    例如，如果同?指定了 '--^checkedout' 和 '--^moved'，?在
    已?出并移?文件的情况下，??更改都将被撤?。

    --^checkedout            ??已?出的文件和目?。
    --^unchanged             ??内容不?的文件。
    --^changed               ??本地更改的或?出的文件和
                            目?。
    --^deleted               ??已?除的文件和目?。
    --^moved                 ??已移?的文件和目?。
    --^added                 ??已添加的文件和目?。

== CMD_HELP_PARTIAL_UNDO ==
?注：

    ^undo 命令有??，因??命令在撤?工作后将不可逆。
    ^undo 完成后，无法将受其影?的文件和目?
    恢?到先前状?。如果参数中
    未指定路径，?默?情况下，?命令将撤?
    当前目?中的所有更改，但不采用??方式。
    从 /src 目??行?，以下命令是等效的：

        /src
        |- file.txt
        |- code.cs
        \- /test
           |- test_a.py
           \- test_b.py

        cm ^partial ^undo
        cm ^partial ^undo *
        cm ^partial ^undo file.txt code.cs /test

        cm ^partial ^undo .
        cm ^partial ^undo /src file.txt code.cs

    如果希望操作以??方式?行，必?指定 '-^r' ?志。

    撤?某一目?下的所有更改（包括影?目?
    本身的更改）：

        cm ^partial ^undo 目?路径 -^r

    如果目?路径是工作区路径，?工作区中的?个更改都将
    被撤?。

示例：

    cm ^partial ^undo .-^r
    （以??方式撤?当前目?中的所有更改。如果从工作区的
    根目??行，?撤?整个工作区中的所有更改。）

    cm ^partial ^co file.txt
    cm ^partial ^undo file.txt
    （撤?? file.txt 的?出。）

    ^echo ^content >> file.txt
    cm ^partial ^undo file.txt
    （撤?? file.txt 的本地更改。）

    cm ^partial ^undo src
    （撤?? src 目?及其文件的更改。）

    cm ^partial ^undo src/*
    （撤?? src 中包含的?个文件和目?的更改，而不会
    影? src。）

    cm ^partial ^undo *.cs
    （撤??当前目?中与 *.cs 匹配的?个文件或目?的
    更改。）

    cm ^partial ^undo *.cs-^r
    （撤??当前目?以及下??个目?中与 *.cs 匹配的
    ?个文件或目?的更改。）

    cm ^partial ^co file1.txt file2.txt
    ^echo ^content >> file1.txt
    cm ^partial ^undo --^unchanged
    （撤??未更改的 file2.txt 的?出，并忽略本地更改的
    file1.txt。）

    ^echo ^content >> file1.txt
    ^echo ^content >> file2.txt
    cm ^partial ^co file1.txt
    cm ^partial ^undo --^checkedout
    （撤?已?出文件 file1.txt 中的更改，并忽略 file2.txt，
    因?后者未?出。）

    cm ^partial ^add file.txt
    cm ^partial ^undo file.txt
    （撤?添加 file.txt，使?文件再次成?私有文件。）

    ^rm file1.txt
    ^echo ^content >> file2.txt
    cm ^partial ^add file3.txt
    cm ^partial ^undo --^deleted --^added *
    （撤? file1.txt 的?除和 file3.txt 的添加，并忽略 file2.txt
    的更改。）

== CMD_DESCRIPTION_PARTIAL_UPDATE ==
更新非完整的工作区并下?最新的更改。

== CMD_USAGE_PARTIAL_UPDATE ==
用法：

    cm ^partial ^update [<?路径>[ ...]] [--^changeset=<?号>]
                      [--^silent | --^report] [--^dontmerge]

    ?路径           要更新的?。使用双引号 (" ") 指定
                        包含空格的路径。使用空格?各个路径?行
                        分隔。
                        使用 . 将更新?用到当前目?。
                        如果未指定路径，?当前非完整的
                        工作区将完全更新。

??：

    --^changeset         将非完整的工作区更新到特定?更集。
    --^silent            不?示任何?出。
    --^report            命令完成后，打印已?用更改
                        的列表。使用 '--^silent' 将会覆盖此?置。
    --^dontmerge         不合并文件冲突，只是跳??些冲突。
                        其他更改会正??用。此??可用于
                        自?化，从而避免用?交互。


== CMD_HELP_PARTIAL_UPDATE ==
?注：

    '^partial ^update' 命令将更新已?期的文件。

    ?命令采用??操作。

    在使用 '--^changeset' ???，如果所有指定的路径都是
    同一 Xlink 内的文件，?会在通? Xlink ?接的存??的
    指定?更集内搜索要下?的版本。

示例：

    cm ^partial ^update
    （更新当前非完整的工作区中的所有内容。）

    cm ^partial ^update .
    （更新所有当前目?子?。）

    cm ^partial ^update backgroud-blue.png
    （更新 'backgroud-blue.png' ?。）

    cm ^partial ^update soft_black.png soft-grey.png
    （更新 'soft_black.png' 和 'soft-grey.png' ?。）

    cm ^partial ^update src --^report
    （更新所有 'src' 目?子?，并在最后打印已?用的
    更改列表。）

    cm ^partial ^update src --^changeset=4
    （将所有 'src' 目?子?更新??更集 4 中
    加?的相?内容。）

    cm ^partial ^update xlink/first.png --^changeset=4
    （将 'xlink/first.png' ?更新?通? Xlink ?接的存??的?更集 4 中
    加?的相?内容。）

== CMD_DESCRIPTION_PATCH ==
从?格生成一个?丁文件，或者将生成的?丁?用于当前
工作区。

== CMD_USAGE_PATCH ==
用法：

    cm ^patch <源?格> [<源?格>] [--^output=<?出文件>]
             [--^tool=<差?比?路径>]
    生成一个?丁文件，?文件包含分支的差?差、
    ?更集的差?差或?更集之?的差?。?用于跟踪
    文本文件和二?制文件的差?。

    cm ^patch --^apply <?丁文件> [--^tool=<修?路径>]
    允??用当前工作区中生成的?丁文件的
    内容。

    源?格     ?更集或分支的完整?格。（使用
                    'cm ^help ^objectspec' 可?一?了解?格。）
    ?出文件     用于保存?丁内容的文件。如果未指定文件，
                    ?会在?准?出中打印?丁内容。
    ?丁文件      要在当前工作区中?用的?丁文件。

??：

    --^output        ?置 patch 命令的?出文件。
    --^tool          ?置要使用的?用程序（差?比??是修?）。

== CMD_HELP_PATCH ==
限制：

    如果?出?丁文件已?存在，?命令不会覆盖?个文件。

    ?用?丁?，如果磁?上没有已修改的文件，?命令不会
    将更改?用于?些文件。

重要信息：

    此命令需要差?比?工具和修?工具；可从以下网址公??得?些工具：
    http://gnuwin32.sourceforge.net/packages/patch.htm 和
    http://gnuwin32.sourceforge.net/packages/diffutils.htm

    安装后，建?将工具的位置添加到 PATH
    ?境?量。

示例：

    cm ^patch ^cs:4@default@localhost:8084
    （以?一格式在控制台上打印?更集 4 的差?。）

    cm ^patch ^br:/main --^output=file.patch
    （使用分支 "main" 的差?来生成?丁文件。）

    cm ^patch ^br:/main --^output=file.patch --^tool=C:\gnu\diff.exe
    （同上，使用的是自定??行程序。）

    cm ^patch ^cs:2@default ^cs:4@default
    （以?一格式在控制台上打印?更集 2 和 4 之?的差?。）

    cm ^patch --^apply file.patch --^tool=C:\gnu\patch.exe
    （使用自定??行程序将 'file.patch' 中的?丁?用于本地工作区。）

== CMD_DESCRIPTION_QUERY ==
?行 SQL ??。需要 SQL 存?。

== CMD_USAGE_QUERY ==
用法：

    cm ^query <SQL_命令> [--^outputfile=<?出文件>]
                           [--^solveuser=<列名称>[,...]]
                           [--^solvepath=<列名称>[,...]]
                           [--^columnwidth=<?>] [--^nocolumnname]
                           [--^columnseparator=<分隔符>] [--^repository=<名称>]

    SQL_命令         要?行的 SQL ??。

??：

    --^outputfile        将?果写入?出文件中。
    --^solveuser         将指定的列?置?用?名列。??
                        解?器将假定?些列的数据将是用?，
                        并将??解析?些数据。
    --^solvepath         将指定的列?置? itemid 列。??
                        解?器将??将? ID 解析?文件系?
                        路径。
    --^columnwidth       指定?列的?度以便格式化?出。
    --^nocolumnname      不打印列名称。
    --^columnseparator   使用字符作?列分隔符，而不是使用制表符 (\t)。
    --^repository        要??的存??。

== CMD_HELP_QUERY ==
?注：

    此命令允?用?在服?器数据?中?行 SQL ??。

    ?了?写 SQL ??，?使用以下?个?定?的函数来管理
    用?和路径：
    - '^SolveUser(<用?名>)'，将用?名解析? Unity VCS 格式。
    - '^SolvePath(<路径>)'，将磁?路径解析?? ID。

    此外，?可使用??以便于人工??的形式?示???果。

    可以使用?? '--^solveuser=<列名称>' 和
    '--^solvepath=<列名称>' 来指定必?由??解?器
    ???易?文本的列。可以指定多个列名称，
    以逗号分隔。

示例：

    cm ^query "^SELECT * ^FROM ^revision" --^columnwidth=25 --^repository=reptest
    （从存?? 'reptest' 中的 '修?' 表?索数据。）

    cm ^query "^SELECT b.^sname ^as 分支名称, o.^dtimestamp ^as 日期 ^from ^branch b, \
        ^object o, ^seid s ^where b.^iobjid=o.^iobjid ^and o.^fidowner=s.^iseidid ^and \
        s.^scode='^SolveUser(john)'" --^outputfile=query.txt
    （将所有者? 'john' 的分支?出到文件中。）

    cm ^query "^select r.^iobjid, r.^fiditem ^as 路径, s.^scode ^as 用?名 ^FROM \
        ^revision r, ^object o, ^seid s ^WHERE r.^iobjid=o.^iobjid ^and \
        o.^fidowner=s.^iseidid ^and o.^dtimestamp>2014/04/25" \
    --^solveuser=用?名 --^solvepath=路径 --^repository=reptest@server2:9095
    （从所?存???索?定数据。）

    cm ^query "^SELECT * ^FROM ^revision ^WHERE ^fiditem=^SolvePath(c:\mywkpath\info)"
    （?索路径 'info' 的所有修?数据。）

== CMD_DESCRIPTION_ATTRIBUTE_DELETE ==
?除一个或多个属性。

== CMD_USAGE_ATTRIBUTE_DELETE ==
用法：

    cm ^attribute | ^att ^delete | ^rm <属性?格>[ ...]

    属性?格            要?除的属性。使用空格?各个属性?行
                        分隔。
                        （使用 'cm ^help ^objectspec' 可?一?了解属性
                        ?格。）

== CMD_HELP_ATTRIBUTE_DELETE ==
?注：

    此命令将?除一个或多个属性。

示例：

    cm ^attribute ^delete ^att:status
    （?除属性 '状?'。）

    cm ^att ^rm 状? ^att:integrated@reptest@server2:8084
    （?除属性 '状?' 和 '集成'。）

== CMD_DESCRIPTION_ATTRIBUTE_UNSET ==
取消?置?象的属性。

== CMD_USAGE_ATTRIBUTE_UNSET ==
用法：

    cm ^attribute | ^att ^unset <属性?格> <?象?格>

    属性?格            属性的?格。（使用 'cm ^help ^objectspec' 可
                        ?一?了解属性?格。）
    ?象?格         要?除相?属性的?象
                        的?格。可以在以下?象上?置属性：分支、?更集、
                        ?置集、??、?和修?。
                        （使用 'cm ^help ^objectspec' 可?一?了解?格。）

== CMD_HELP_ATTRIBUTE_UNSET ==
?注：

    ?命令将取消?置先前在?象上?置的某个属性。?命令
    不会?除属性?象本身。

示例：

    cm ^attribute ^unset ^att:status ^br:/main/SCM105
    （从分支 'main/SCM105' ?除属性?? '状?'。）

    cm ^att ^unset ^att:integrated@reptest@localhost:8084 ^cs:25@reptest@localhost:8084
    （从?更集 25 ?除属性?? '集成'，全都在
    存?? 'reptest' 中。）

== CMD_DESCRIPTION_ATTRIBUTE_RENAME ==
?属性重命名。

== CMD_USAGE_ATTRIBUTE_RENAME ==
用法：

    cm ^attribute | ^att ^rename <属性?格> <新名称>

    属性?格            要重命名的属性。（使用 'cm ^help ^objectspec' 可
                        ?一?了解属性?格。）
    新名称            属性的新名称。

== CMD_HELP_ATTRIBUTE_RENAME ==
?注：

    此命令将重命名一个属性。

示例：

    cm ^attribute ^rename ^att:status state
    （将属性 '状?' 重命名? '状况'。）

== CMD_DESCRIPTION_ATTRIBUTE_EDIT ==
??属性的注?。

== CMD_USAGE_ATTRIBUTE_EDIT ==
用法：

    cm ^attribute | ^att ^edit <属性?格> <新注?>

    属性?格        要更改相?注?的属性。（使用 'cm ^help ^objectspec'
                    可?一?了解属性?格。）
    新注?     属性的新注?。?可以?属性指定
                    一个默??列表。

== CMD_HELP_ATTRIBUTE_EDIT ==
?注：

    此命令将更改属性的注?。

    要指定属性的默??列表，只需将
    如下的行包含在属性注?中：
    'default: ?_一, "?二", ?3, "最??"'。

示例：

    cm ^attribute ^edit ^att:status "CI 管道中分支的状?。"
    （??属性 '状?' 的注?。）

    cm ^attribute ^edit ^att:status "分支的状?。默??：未完成、已解决、已??"
    （??属性 '状?' 的注?。?指定一个?
    列表。因此，当属性 '状?' ?置??象?，可以??
    以下?之一："未完成"、"已解决" 或 "已??"。）

== CMD_DESCRIPTION_REPLICATE ==
警告：已弃用此命令。

?使用 'cm ^pull'（等同于 '^replicate'）和 'cm ^push'（等同于
'^replicate --^push'）。

== CMD_USAGE_REPLICATE ==

== CMD_HELP_REPLICATE ==

== CMD_DESCRIPTION_PULL ==
从?一个存??拉取分支。

== CMD_USAGE_PULL ==
用法：

    cm ^pull <源分支?格> <目?存???格>
            [--^preview] [--^nodata] [????]
            [--^user=<用?名> [--^password=<密?>] | 身?????]
     （服?器之?的直接?制。从存??拉取分支。）

    cm ^pull <目?存???格> --^package=<包文件> [身?????]
     （基于包的?制。将包?入目?存??中。）

    cm ^pull ^hydrate <目?分支?格> [<源存???格>]
                    [--^user=<用?名> [--^password=<密?>] | 身?????]
     （?分支中先前使用 '--^nodata' ?制的所有?更集
     引入缺失的数据。如果未指定用于?取数据的存??，
     Unity VCS 会??使用“?制源”（?制分支
     的来源））。

    cm ^pull ^hydrate <目??更集?格> [<源存???格>]
                    [--^user=<用?名> [--^password=<密?>] | 身?????]
     （?先前使用 '--^nodata' ?制的?更集引入缺失的
     数据。如果未指定用于?取数据的存??，Unity VCS 会??
     使用“?制源”）。

    源分支?格     要从?程存??拉取的分支。
                    （使用 'cm ^help ^objectspec' 可?一?了解分支?格。）
    目?分支?格     要?行 hydrate 的分支。
                    （使用 'cm ^help ^objectspec' 可?一?了解分支?格。）
    目??更集?格     要?行 hydrate 的?更集。
                    （使用 'cm ^help ^objectspec' 可?一?了解?更集
                    ?格。）
    目?存???格    目?存??。
                    （使用 'cm ^help ^objectspec' 可?一?了解存??
                    ?格。）
    --^package       指定要?入的先前?建的包文件以?行
                    基于包的?制。
                    ?合在没有直接网??接?用于在服?器
                    之?移?数据。
                    ?参? 'cm ^push' 以?建包文件。

??：

    --^preview           提供?于将拉取?些更改的信息，
                        但??上不?行任何更改。此??
                        可用于??在?制更改之前要??
                        的数据。
    --^nodata            ?制分支更改而不?制
                        数据。?制包?，不允?使用
                        此??。
    ????    ?参?“????”部分以了解更多信息。
    --^user, --^password  源和目?的身???模式不同并且
                        没有可用于目?身???的配置文件
                        的情况下使用的凭据。
    身?????         ?参?“身?????”部分以了解更多
                        信息。

????：

    --^trmode=(^copy|^name|^table --^trtable=<??表文件>)
      源和目?存??可能使用不同的身???
      模式。'--^trmode' ??指定如何将用?名从源
      ??到目?。'--^trmode' 必??以下?
      之一：
          ^copy    （默??）。表示只会?制用???符。
          ^name    按名称来匹配用???符。
          ^table   使用?? '--^trtable' 中指定的??表
                  （?下文）。

    --^trtable=<??表文件>
        如果??模式? 'table'，???表文件中包含
        格式? <旧名称;新名称> 的行（?行一个）。将分支
        写入目?存???，源存??中由“旧名称”
        ??的用?所?建的?象将?置到目?上
        具有“新名称”的用?。

身?????：

    可以使用以下??模式之一来指定身???数据：

    1) 使用身???参数：--^authmode=<模式> --^authdata=<数据>

        --^authmode=(^NameWorkingMode|^LDAPWorkingMode|^ADWorkingMode|^UPWorkingMode)
        示例：
        (^LDAPWorkingMode) --^authdata=::0:dave:fPBea2rPsQaagEW3pKNveA
        (^UPWorkingMode)   --^authdata=dave:fPBea2rPsQaagEW3pKNveA==

        '--^authdata' 行是 client.conf 文件和 profiles.conf 文件
        中的 <^SecurityConfig> 条目的内容。可以从
        Unity VCS GUI 中的“首??”下的“?制配置文件”???
        ?松生成 profiles.conf 文件。

        如果使用 ^UPWorkingMode，?只需指定：

        --^authmode=^UPWorkingMode --^user=<用?> --^password=<密?>

    2) 身???文件，?于?接到的?个服?器，文件可能会
       有所不同，其中包含?服?器的凭据。

        --^authfile=<身???文件>
            ?文件包含 2 行：
            第 1 行是模式，如 '--^authmode' 中所述
            第 2 行是身???数据，如 '--^authdata' 中所述

== CMD_HELP_PULL ==
?注：

    '^pull' 命令可以在源存??与目?存??之?
    ?制分支（以及?些分支的?更集）。
    ?些存??可以位于不同服?器中。

    有???制操作：'^push' 和 '^pull'。

    '^pull' 操作表示?制操作要求将数据从源存??
    存?到目?存??。
    客?端将?接到目?存??，然后建立
    从?主机到源存??的?接以便?索
    目?数据。在拉取期?，目?服?器将
    ?接到源。

    尽管在典型的分布式?景中，??者会将数据
    从本地服?器推送到主服?器，但??者可能也希望
    从主服?器拉取最新的存??更新。

    ?制操作可以解决在同一分支上?个?制的存??
    已?行并?更改的情况：

    - 推送：如果??将数据推送到一个存??，但?个存??中的
      更改比?送的更改更新，?系?会要求提取最新的更改，
      解决合并操作??，最后??再次推送。

    - 拉取：?次从?程分支拉取?更集?，都会将?些?更集
      正??接到相?的父?更集。如果拉取的?更集不是
      分支中最后一个?更集的子?，?会出?
      一?多??景。?分支将具有多个“?部”，即分支中的
      最后一个?更集。此?需要先合并?个“?部”，
      然后才能再次推送。

    可以按??模式?行拉取：

    1) 服?器之?的直接通信：目?服?器将
       从源服?器提取数据，自??指定
       分支同?数据。

    2) 使用推送和 '--^package' ??来?入先前生成的包。

    模式 1) 要求?程服?器??行命令的用??行身???，
    方法是使用 client.conf 文件中的默?身???，
    或者指定 '--^authmode' 和 '--^authdata' 修?符
    或 '--^authmode'、'--^user' 和 '--^password'（如果身???模式?
    ^UPWorkingMode）。

    模式 2) 要求使用先前通?推送命令生成的
    包文件。

    ??住，拉取?制是以?接方式?行的。?行?，
    ?命令要求目?存???接到源
    并?取所?分支。

    然而，可以使用推送命令来直接???一点。
    因此，?命令会将所?分支从源?制到
    目?。

示例：

    cm ^pull ^br:/main@project1@remoteserver:8084 projectx@myserver:8084
    （将 'main' 分支从 'remoteserver' 拉取到 'myserver'。??情况下，
    ?个服?器会配置?相同的身???模式。）

    cm ^pull ^br:/main@project1@remoteserver:8084 projectx@myserver:8084 \
      --^authmode=^LDAPWorkingMode --^authdata=::0:dave:fPBea2rPsQaagEW3pKNveA
    （拉取与以前相同的分支，但?在将?程服?器配置?
    使用 Active Directory ?用??行身???。例如，我正在从 Linux
    ?算机?接到配置?使用 Active Directory 集成模式
    的 Windows 服?器。我会指定自己的 Active Directory 用?名和密?，
    并以 LDAP 形式将此信息???服?器。）

    cm ^pull ^br:/main@project1@remoteserver:8084 projectx@myserver:8084 \
      --^authmode=^UPWorkingMode --^user=dave --^password=mysecret
    （拉取同一个分支，但?在利用 Unity VCS 中
    包含的用?名/密?数据?在?程服?器上?用?
    ?行身???。）

    cm ^pull ^br:/main@project1@remoteserver:8084 projectx@myserver:8084 --^nodata
    （将 'main' 分支从 'remoteserver' ?制到 'myserver' 而不?制数据。）

    cm ^pull ^hydrate ^br:/main@projectx@myserver:8084 projectx@remoteserver:8084
    （从?程服?器?取数据，? 'main' 分支中的所有?更集
    ?行 hydrate 操作。）

    cm ^pull ^hydrate ^cs:122169@projectx@myserver:8084 projectx@remoteserver:8084
    （从?程服?器?取数据，? 'myserver' 中的?更集 122169 ?行
    hydrate 操作。）

== CMD_DESCRIPTION_PUSH ==
将分支推送到?一个存??。

== CMD_USAGE_PUSH ==
用法：

    cm ^push <源分支?格> <目?存???格>
            [--^preview] [????]
            [--^user=<用?名> [--^password=<密?>] | 身?????]
     （服?器之?的直接?制。从存??推送分支。）

    cm ^push <源分支?格> --^package=<包文件> [身?????]
     （基于包的?制。使用所?分支在源服?器中
     ?建?制包。）

    源分支?格     要推送到?程存??的分支。
                    （使用 'cm ^help ^objectspec' 可?一?了解分支?格。）
    目?存???格    目?存??。
                    （使用 'cm ^help ^objectspec' 可?一?了解存??
                    ?格。）
    --^package       指定?制包的?出路径，从而?行
                    基于包的?制。
                    ?合在没有直接网??接?用于在服?器
                    之?移?数据。

??：

    --^preview           提供?于将推送?些更改的信息，
                        但??上不?行任何更改。此??
                        可用于??在?制更改之前要??
                        的数据。
    ????    ?参?“????”部分以了解更多信息。
    --^user, --^password  源和目?的身???模式不同并且
                        没有可用于目?身???的配置文件
                        的情况下使用的凭据。
    身?????         ?参?“身?????”部分以了解更多
                        信息。

????：

    --^trmode=(^copy|^name|^table --^trtable=<??表文件>)
        源和目?存??可能使用不同的身???
        模式。'--^trmode' ??指定如何将用?名从源
        ??到目?。'--^trmode' 必??以下?
        之一：
          ^copy    （默??）。表示只会?制用???符。
          ^name    按名称来匹配用???符。
          ^table   使用?? '--^trtable' 中指定的??表
                  （?下文）。

    --^trtable=<??表文件>
        如果??模式? 'table'，???表文件中包含
        格式? <旧名称;新名称> 的行（?行一个）。将分支
        写入目?存???，源存??中由“旧名称”
        ??的用?所?建的?象将?置到目?上
        具有“新名称”的用?。

身?????：

    可以使用以下??模式之一来指定身???数据：

    1) 使用身???参数：--^authmode=<模式> --^authdata=<数据>

        --^authmode=(^NameWorkingMode|^LDAPWorkingMode|^ADWorkingMode|^UPWorkingMode)
        示例：
        (^LDAPWorkingMode) --^authdata=::0:dave:fPBea2rPsQaagEW3pKNveA
        (^UPWorkingMode)   --^authdata=dave:fPBea2rPsQaagEW3pKNveA==

        '--^authdata' 行是 client.conf 文件和 profiles.conf 文件
        中的 <^SecurityConfig> 条目的内容。可以从
        Unity VCS GUI 中的“首??”下的“?制配置文件”???
        ?松生成 profiles.conf 文件。

        如果使用 ^UPWorkingMode，?只需指定：

        --^authmode=^UPWorkingMode --^user=<用?> --^password=<密?>

    2) 身???文件，?于?接到的?个服?器，文件可能会
       有所不同，其中包含?服?器的凭据。

        --^authfile=<身???文件>
            ?文件包含 2 行：
            第 1 行是模式，如 '--^authmode' 中所述
            第 2 行是身???数据，如 '--^authdata' 中所述

== CMD_HELP_PUSH ==
?注：

    '^push' 命令可以在源存??与目?存??之?
    ?制分支（以及?些分支的?更集）。
    ?些存??可以位于不同服?器中。

    有???制操作：'^push' 和 '^pull'。

    '^push' 操作表示?制操作会将数据从源存??
    ?送到目?存??。在??情况下，
    客?端将?接到源存??，?取要?制的数据，
    然后将数据?送到目?存??。?然
    前者（源）必?与目??接，但是后者（目?）
    本身不会?接到源。

    在典型的分布式?景中，??者会将数据从本地服?器
    推送到主服?器。此外，??者可能也希望从主服?器
    拉取最新的存??更新。

    ?制可以解决在?个?制存??的同一分支上
    已?行并?更改的情况。

    - 推送：如果??将数据推送到一个存??，但?个存??中的
      更改比?送的更改更新，?系?会要求提取最新的更改，
      解决合并操作??，最后??再次推送。

    - 拉取：?次从?程分支拉取?更集?，都会将?些?更集
      正??接到相?的父?更集。如果拉取的?更集不是
      分支中最后一个?更集的子?，?会出?
      一?多??景。?分支将具有多个“?部”，即分支中的
      最后一个?更集。此?需要先合并?个“?部”，
      然后才能再次推送。

    可以按??模式?行推送：

    1) 服?器之?的直接通信：源服?器将
       把数据?送到目?服?器，自??指定
       分支同?数据。

    2) ?出包模式：客?端将??接到源
       并?取指定分支的数据和元数据来生成一个
       ?制包。此模式下将使用 '--^package' 修?符。

    ???模式都要求服?器??行命令的用??行身???，
    方法是使用 client.conf 文件中的默?身???，
    或者指定 '--^authmode' 和 '--^authdata' 修?符。

    ^push 推送?制是以直接方式?行的。?行?，
    ?命令将所?分支从源?制到目?
    （而不是要求目?存???接到源）并?取
    所?分支（与拉取操作一?）。

示例：

    cm ^push ^br:/main@project1@myserver:8084 projectx@remoteserver:8084
    （将 'main' 分支从 'myserver' ?制到 'remoteserver'。??情况下，
    ?个服?器会配置?相同的身???模式。）

    cm ^push ^br:/main@project1@myserver:8084 projectx@remoteserver:8084 \
      --^authmode=^LDAPWorkingMode --^authdata=::0:dave:fPBea2rPsQaagEW3pKNveA
    （?制与以前相同的分支，但?在将?程服?器配置?
    使用 Active Directory ?用??行身???。例如，我正在从 Linux
    ?算机?接到配置?使用 Active Directory 集成模式
    的 Windows 服?器。我会指定自己的 Active Directory 用?名和密?，
    并以 LDAP 形式将此信息???服?器。）

    cm ^push ^br:/main@project1@myserver:8084 projectx@remoteserver:8084 \
      --^authmode=^UPWorkingMode --^user=dave --^password=mysecret
    （?制同一个分支，但?在利用 Unity VCS 中
    包含的用?名/密?数据?在?程服?器上?用?
    ?行身???。）

== CMD_DESCRIPTION_CLONE ==
克隆?程存??。

== CMD_USAGE_CLONE ==
用法：

    cm ^clone <源存???格> [<目?存???格> | <目?存??服?器?格>]
             [--^user=<用?名> [--^password=<密?>] | 身?????]
                [????]
    （存??之?的直接克隆。）

    cm ^clone <源存???格> --^package=<包文件>
             [--^user=<用?名> [--^password=<密?>] | 身?????]
    （克隆到中?包，随后可以使用拉取操作将中?包
    ?入到目?存??。）

    源存???格        克隆操作的源存??。
                        （使用 'cm ^help ^objectspec' 可?一?了解存??
                        ?格。）
    目?存???格        克隆操作的目?存??。如果
                        已存在，必??空。如果不存在，
                        ?会?行?建。
                        如果未指定，?命令将使用用?的
                        默?存??服?器。
                        （使用 'cm ^help ^objectspec' 可?一?了解存??
                        ?格。）
    目?存??服?器?格  克隆操作的目?存??服?器。
                        如果存在与目?存??服?器中的
                        <源存???格> 同名的存??，此存??
                        必??空。如果没有，?会?行?建。
                        如果未指定，?命令将使用用?的
                        默?存??服?器。
                        （使用 'cm ^help ^objectspec' 可?一?了解存??
                        服?器?格。）

??：

    --^user, --^password  源和目?的身???模式不同并且
                        没有可用于目?身???的配置文件
                        的情况下使用的凭据。
    --^package           将指定的存???出到包文件，
                        而不是存??。
                        ?合在没有直接网??接?用于在服?器
                        之?移?数据。
                        必?使用拉取命令来?入
                        生成的包。
    ????    ?参?“????”部分以了解更多信息。
    身?????         ?参?“身?????”部分以了解更多
                        信息。

????：
    --^trmode=(^copy|^name|^table --^trtable=<??表文件>)
      源和目?存??可能使用不同的身???
      模式。'--^trmode' ??指定如何将用?名从源
      ??到目?。'--^trmode' 必??以下?
      之一：
          ^copy    （默??）。表示只会?制用???符。
          ^name    按名称来匹配用???符。
          ^table   使用?? '--^trtable' 中指定的??表
                  （?下文）。

    --^trtable=<??表文件>
        如果??模式? 'table'，???表文件中包含
        格式? <旧名称;新名称> 的行（?行一个）。将分支
        写入目?存???，源存??中由“旧名称”
        ??的用?所?建的?象将?置到目?上
        具有“新名称”的用?。

身?????：

    可以使用以下??模式之一来指定身???数据：

    1) 使用身???参数：--^authmode=<模式> --^authdata=<数据>

        --^authmode=(^NameWorkingMode|^LDAPWorkingMode|^ADWorkingMode|^UPWorkingMode)
        示例：
        (^LDAPWorkingMode) --^authdata=::0:dave:fPBea2rPsQaagEW3pKNveA
        (^UPWorkingMode)   --^authdata=dave:fPBea2rPsQaagEW3pKNveA==

        '--^authdata' 行是 client.conf 文件和 profiles.conf 文件
        中的 <^SecurityConfig> 条目的内容。可以从
        Unity VCS GUI 中的“首??”下的“?制配置文件”???
        ?松生成 profiles.conf 文件。

        如果使用 ^UPWorkingMode，?只需指定：

        --^authmode=^UPWorkingMode --^user=<用?> --^password=<密?>

    2) 身???文件，?于?接到的?个服?器，文件可能会
       有所不同，其中包含?服?器的凭据。

        --^authfile=<身???文件>
            ?文件包含 2 行：
            第 1 行是模式，如 '--^authmode' 中所述
            第 2 行是身???数据，如 '--^authdata' 中所述

== CMD_HELP_CLONE ==
?注：

    克隆命令可以将分支（以及?些分支的?更集、
    ??、属性、??等）从源存???制到
    目?存??。?些存??可以位于不同服?器中。

    可以?先?建目?存??，但是如果此存??包含
    以前的数据，克隆操作会失?。

    克隆操作不会克隆存??子模?，也不会克隆 Xlink 下的
    存??。

示例：

    cm ^clone awesomeProject@tardis@cloud
    （将 'awesomeProject' 存??从 'tardis@cloud' ??
    克隆到同名的本地存??。）

    cm ^clone repo@server.home:9095 repo-local
    （将 'repo' 从 'server.home:9095' 克隆到用?的默?存??服?器
    中的 'repo-local'。）

    cm ^clone project@192.168.111.130:8084 ^repserver:192.168.111.200:9095
    （将 'project' 存??从 '192.168.111.130:8084' 克隆到
    'project@192.168.111.200:9095'。）

    cm ^clone project@ldapserver:8084 --authfile=credentials.txt \
      --^trmode=table --^trtable=table.txt
    （从 'ldapserver:8084' 克隆 'project' 存??，使用??
    ?程存??的身???文件，并按照指定的
    ??表来??用?。）

    cm ^clone project@server.home:9095 --^package=project.plasticpkg
    cm ^repository ^create project@mordor.home:8084
    cm ^pull --^package=project.plasticpkg project@mordor.home:8084
    （将 'project' 存??从 'server.home:9095' 克隆到包
    'project.plasticpkg'，随后通?拉取操作将?个包?入到
    'mordor.home:8084' 的'project' 存??。）

== CMD_DESCRIPTION_REVERT ==
将??原到先前的修?。

== CMD_USAGE_REVERT ==
用法：

    cm ^revert <修??格>

    修??格             包含修?（修?的内容将加?到工作区中）
                        的?更集的?格。
                        （使用 'cm ^help ^objectspec' 可?一?了解修?
                        ?格。）

== CMD_HELP_REVERT ==
?注：

    必??入?。

示例：

    cm ^revert dir#^cs:0
    cm ^revert C:\mywks\dir\file1.txt#23456

== CMD_DESCRIPTION_HISTORY ==
?示文件或目?的?史??。

== CMD_USAGE_HISTORY ==
用法：

    cm ^history | ^hist <?路径>[ ...][--^long | --^format=<格式字符串>]
                      [--^symlink] [--^xml[=<?出文件>]] [--^encoding=<名称>]

    ?路径           ?的路径。使用空格?各个路径?行分隔。使用
                        双引号 (" ") 指定包含空格的路径。
                        路径也可以是服?器路径修?。
                        （使用 'cm ^help ^objectspec' 可?一?了解?格。）

??：

    --^long              ?示其他信息。
    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。
    --^symlink           将?史??操作?用于符号?接而不是
                        目?。
    --^xml               以 XML 格式将?出打印到?准?出。
                        可以指定?出文件。
    --^encoding          与 '--^xml' ???合使用，指定要在 XML ?出
                        中使用的??（例如：utf-8）。
                        ?参?位于以下网址的 MSDN 文档：
                        http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                        以?看包含受支持??及其格式的表格
                        （位于?面末尾的“名称”列中）。

== CMD_HELP_HISTORY ==
?注：

    此命令将?示?定?的修?列表，以及?个修?的??、
    分支和注?信息。

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：
        {0} | {^date}              日期。
        {1} | {^changesetid}       ?更集?号。
        {2} | {^branch}            分支。
        {4} | {^comment}           注?。
        {5} | {^owner}             所有者。
        {6} | {^id}                修? ID。
        {7} | {^repository}        存??。
        {8} | {^server}            服?器。
        {9} | {^repspec}           存???格。
        {^tab}                     插入一个制表符空格位。
        {^newline}                 插入一个新行。

示例：

    cm ^history file1.txt "file 2.txt"

    cm ^hist c:\workspace --^long
    （?示所有信息。）

    cm ^history ?接 --^symlink
    （将?史??操作?用于 '?接' 文件而不是目?，
    ?用于 UNIX ?境。）

    cm ^history ^serverpath:/src/foo/bar.c#^br:/main/task001@myserver
    （从?定分支中的服?器路径?索修??史??。）

== CMD_DESCRIPTION_REVISION_TREE ==
?示?的修??。

== CMD_USAGE_REVISION_TREE ==
用法：

    cm ^tree <路径> [--^symlink]

    路径        ?路径。

??：

    --^symlink   将操作?用于?接文件而不是目?。

== CMD_HELP_REVISION_TREE ==
示例：

    cm ^tree fichero1.txt
    cm ^tree c:\workspace
    cm ^tree ?接 --^symlink
    （将操作?用于?接文件而不是目?；?用于
    UNIX ?境。）

== CMD_DESCRIPTION_REMOVE ==
允?用??除文件和目?。

== CMD_USAGE_REMOVE ==
用法：

    cm ^remove | ^rm <命令> [??]

命令：

    ^controlled（可?）
    ^private

    要?取有??条命令的更多信息，??行：
    cm ^remove <命令> --^usage
    cm ^remove <命令> --^help

== CMD_HELP_REMOVE ==
示例：

    cm ^remove \path\controlled_file.txt
    cm ^remove ^private \path\private_file.txt

== CMD_DESCRIPTION_REMOVE_CONTROLLED ==
从版本控制中?除一个文件或目?。

== CMD_USAGE_REMOVE_CONTROLLED ==
用法：

    cm ^remove | ^rm <?路径>[ ...][--^format=<格式字符串>]
                   [--^errorformat=<格式字符串>] [--^nodisk]

    ?路径           要?除的?路径。使用双引号 (" ") 指定
                        包含空格的路径。使用空格?各个路径?行
                        分隔。

??：

    --^format            ?索特定格式的?出?度
                        消息。?参?“示例”以了解更多信息。
    --^errorformat       ?索特定格式的??消息
                        （如果有）。?参?“示例”以了解更多信息。
    --^nodisk            从版本控制中?除相?的?，但是将??保留在
                        磁?上。

== CMD_HELP_REMOVE_CONTROLLED ==
?注：

    磁?中会?除?。?除的?会从源代?管理
    中的父目?中被?除。

    要求：
    - ?必?受源代?管理。

从 stdin ?取?入：

    '^remove' 命令可从 stdin ?取路径。?此，???一个
    破折号 "-"。
    示例：cm ^remove -

    路径将一直?取到有空行?入?止。
    ??情况下允?使用??指定要?除的文件。
    示例：
      dir /S /B *.c | cm ^remove -
      （在 Windows 中，?除工作区中的所有 .c 文件。）

示例：

    cm ^remove src
    （?除 'src'。如果 src 是目?，?等同于
    'cm ^remove -^R src'。）

    cm ^remove c:\workspace\file.txt --^format="{0} - 已?除" \
        --^errorformat="{0} - ?除?出?"
    （从版本控制中和磁?上?除 'file.txt'，
    如果操作成功，?写入 "c:\workspace\file.txt - ^REMOVED"，
    否?写入 "c:\workspace\file.txt - ^ERROR ^REMOVING"。）

    cm ^remove c:\workspace\file.txt --^nodisk
    （从版本控制中?除 'file.txt'，但将其保留在磁?上。）

== CMD_DESCRIPTION_REMOVE_PRIVATE ==
?除一个私有文件或目?。

警告：使用?命令?除的文件将被永久擦除而无法
恢?。建?使用 '--^dry-run' ??来??
?些文件将受到?命令的影?。

== CMD_USAGE_REMOVE_PRIVATE ==
用法：

    cm ^remove | ^rm ^private <路径>[ ...][-^R | -^r | --^recursive] [--^ignored]
                           [--^verbose] [--^dry-run]

    路径                要?除的文件或目?的路径。使用双
                        引号 (" ") 指定包含空格的路径。使用
                        空格?各个路径?行分隔。

??：

    --^r                 以??方式从受控目?内?除私有
                        文件。
    --^ignored           ??除已忽略和掩蔽的文件和目?。
    --^verbose           打印所有受影?的路径。
    --^dry-run           ?行此命令而不在磁?上?行任何更改。

== CMD_HELP_REMOVE_PRIVATE ==
?注：

    如果路径是私有文件或目?，?会从磁?上将其?除。
    如果路径是受控文件，?命令会失?。
    如果路径是受控目?，?除非指定 '-^r' ??
    ，否??命令将失?，在??情况下，它将?除指定目?下的
    所有私有文件和目?。

示例：

    cm ^remove ^private private_directory
    （?除 'private_directory'。）

    cm ^remove ^private c:\workspace\controlled_directory
    （失?，因? 'controlled_directory' 并非私有目?。）

    cm ^remove ^private -^r c:\workspace\controlled_directory
    （?除 'controlled_directory' 下的所有私有文件和目?。）

    cm ^rm ^private --^dry-run --^verbose c:\workspace\controlled_directory -^r
    （?示 'controlled_directory' 下的私有文件被?除所影?的
    所有路径，??上不?除任何内容。）

    cm ^rm ^private --^verbose c:\workspace\controlled_directory -^r
    （?示 'controlled_directory' 下的私有文件被?除所影?的
    所有路径，并?行?除。）

== CMD_DESCRIPTION_TRIGGER_DELETE ==
?除触?器。

== CMD_USAGE_TRIGGER_DELETE ==
用法：

    cm ^trigger | ^tr ^delete | ^rm <子?型-?型> <位置?号>
                                [--^server=<存??服?器?格>]

    子?型-?型        触?器?行和触?器操作。
                        ?入 'cm ^showtriggertypes' 可?看触?器?型
                        列表。
    位置?号     ?建触?器?分配?触?器的位置。

??：

    --^server            ?除指定服?器上的触?器。
                        如果未指定服?器，?在客?端上配置的
                        服?器上?行命令。

== CMD_HELP_TRIGGER_DELETE ==
示例：

    cm ^trigger ^delete ^after-setselector 4
    cm ^tr ^rm ^after-setselector 4

== CMD_DESCRIPTION_ATTRIBUTE_SET ==
在?定?象上?置属性。

== CMD_USAGE_ATTRIBUTE_SET ==
用法：

    cm ^attribute | ^att ^set <属性?格> <?象?格> <属性?>

    属性?格           属性的?格。（使用 'cm ^help ^objectspec' 可
                       ?一?了解属性?格。）
    ?象?格        要?置相?属性的?象的?格。
                       可以在以下?象上?置属性：分支、?更集、
                       ?置集、??、?和修?。
                       （使用 'cm ^help ^objectspec' 可?一?了解?格。）
    属性?          要??象?置的属性?。

== CMD_HELP_ATTRIBUTE_SET ==
?注：

    可以在一个?象上?置属性来保存此?象的
    其他信息。
    可以在以下?象上?置属性：分支、?更集、
    ?置集、??、?和修?。

示例：

    cm ^attribute ^set ^att:status ^br:/main/SCM105 未完成
    （将属性 '状?' ?置?分支 'SCM105'，?? '未完成'。）

    cm ^att ^set ^att:integrated@reptest@server2:8084 ^lb:LB008@reptest@server2:8084 是
    （在存?? 'reptest' 中将属性 '集成' ?置??? 'LB008'，
    ?? '是'。）

== CMD_DESCRIPTION_SETOWNER ==
?置?象的所有者。

== CMD_USAGE_SETOWNER ==
用法：

    cm ^setowner | ^sto --^user=<用?名> | --^group=<?> <?象?格>

    --^user              用?名。?象的新所有者。
    --^group             ?名称。?象的新所有者。
    ?象?格         要?置新所有者的?象的?格。
                        可以在以下?象上?置所有者：
                        存??服?器、存??、分支、?更集、
                        ??、?、修?和属性。
                        （使用 'cm ^help ^objectspec' 可?一?了解?格。）

== CMD_HELP_SETOWNER ==
?注：

    ?象的所有者可以是用?或?。

    可以在以下?象上?置所有者：存??服?器、
    存??、分支、?更集、??、?、修?和属性。

示例：

    cm ^setowner --^user=john ^repserver:localhost:8084
    （将 'john' ?置?存??服?器所有者。）

    cm ^sto --^group=development ^rep:mainRep@PlasticServer:8084
    （将 'development' ??置? 'mainRep' 存??的所有者。）

== CMD_DESCRIPTION_SETSELECTOR ==
将??器?置?工作区。

== CMD_USAGE_SETSELECTOR ==
已弃用此命令。?了向后兼容，此命令仍然存在，
但是在 Plastic SCM 4.0 中，已在很大程度上弃用??器。仍然存在的??器
是?了指定工作分支或?更集，但不再支持
用于??路径的旧??。

用法：
    cm ^setselector | ^sts [--^file=<??器文件>] [--^ignorechanges]
                         [--^forcedetailedprogress] [<工作区路径> | <工作区?格>]

??：

    --^file                   要从中加???器的文件。
    --^ignorechanges          忽略在更新工作区?
                             ??到待定更改的情况下?示
                             的待定更改警告消息。
    --^forcedetailedprogress  即使在?准?出被重定向的情况下也要?制?示
                             ???度。
    工作区路径                  用于?置??器的工作区的路径。
    工作区?格                  工作区的?格。（使用 'cm ^help ^objectspec'
                             可?一?了解工作区?格。）

== CMD_HELP_SETSELECTOR ==
?注：

    此命令将?置工作区的??器。

    工作区需要某些信息才能从存??加?修?。
    ?了?得?些信息，Unity VCS 需要使用??器。

    通?使用??器，可以从?定的分支、??或
    ?更集加?修?。

    如果未指定用于加???器的文件，?将?行默?的
    操作系???器。

    示例??器：

    ^repository "^default" // 工作存??
      ^path "/"           // ??将?用于根目?
        ^branch "/^main"   // 从 ^br:/^main ?取最新修?
        ^checkout "/^main" // 将?出内容放在 ^br:/^main 分支中

示例：

    cm ^sts
    （打?要?用的当前??器文件。）

    cm ^sts ^wk:workspace_projA@reptest
    （打?要?用的指定??器文件。）

    cm ^setselector --^file=c:\selectors\sel.xml
    （在当前工作区中?置指定的??器文件。）

    cm ^setselector --^file=c:\selectors\sel.xml ^wk:MyWorkspace
    （在?定工作区中?置指定的??器文件。）

== CMD_DESCRIPTION_SHELVE ==
?置已?出?的内容。

== CMD_USAGE_SHELVE ==
已弃用此命令。?改用 'cm ^shelveset'。

用法：

    cm ^shelve [<?路径>+] [--^all] [--^dependencies]
              [-^c=注?字符串 | -^commentsfile=<注?文件>]
              [--^encoding=名称] [--^comparisonmethod=比?方法]
    （?置内容。）

    cm ^shelve --^apply=<?置?格> [--^mount]
    （?用存?的?置集。）

    --^apply             ?原指定?置集的已?置内容。
                        ?置?格：?看 'cm ^help ^objectspec'。

    cm ^shelve --^delete=<?置?格>
    （?除存?的?置集。）

    --^delete            ?除指定的?置集。
                        ?置集?格：?看 'cm ^help ^objectspec'。

??：

    ?路径           要?置的?（以空格分隔）。可以使用引号 (")
                        来指定包含空格的路径。
    --^all               ?包括在?定路径上?行了本地更改、
                        移?和?除的?。
    --^dependencies      在要?置的?中包含本地更改
                        依??。
    -^c                  将指定的注??用于?建的?置集。
    -^commentsfile       将指定文件中的注??用于?建的
                        ?置集。
    --^encoding          指定?出??，如：utf-8。
                        ?参?位于以下网址的 MSDN 文档：
                        http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                        以?看包含受支持??及其格式的表格
                        （位于?面末尾的“名称”列中）。
    --^comparisonmethod  ?置比?方法。?参?“?注”以了解更多信息。
    --^mount             ?定存??的装入点。

== CMD_HELP_SHELVE ==

?注：

    如果既没有指定 <?路径>，也没有指定任何其他??，??置将?及
    工作区中的所有待定更改。

    ?置操作始?以??方式从?定路径?用。

    ?置?的要求：
    - ?必?受源代?管理。
    - 必??出或更改?（必?使用 --^all ??）。

    比?方法：
        ^ignoreeol               忽略行尾差?。
        ^ignorewhitespaces       忽略空格差?。
        ^ignoreeolwhitespaces    忽略行尾和空格差?。
        ^notignore               ??行尾和空格差?。

    ?置 PLASTICEDITOR ?境?量可指定用于?入注?的
    ??器。

示例：

    cm ^shelve -^c="我的注?"
    （?置当前工作区中的所有待定更改，包括
    注?。）

    cm ^shelve file1.txt "file 2.txt" -^commentsfile=commentshelve.txt
    （?置?定的待定更改并?用 commentshelve.txt
    文件中的注?。）

    cm ^shelve --^apply=^sh:3
    （?用存?的?置集。）

    cm ^shelve --^delete=^sh:3
    （?除存?的?置集。）

    cm ^status --^short --^changelist=pending_to_review | cm ^shelve -
    （?置客?端更改列表。
    以上命令将列出名? 'pending_to_review'
    的更改列表中的路径，并且路径列表将重定向到
    shelve 命令的?入。）

== CMD_DESCRIPTION_SHELVESET ==
允?用?管理?置集。

== CMD_USAGE_SHELVESET ==
用法：

    cm ^shelveset <命令> [??]

命令：

    ^create | ^mk
    ^delete | ^rm
    ^apply

    要?取有??条命令的更多信息，??行：
    cm ^shelveset <命令> --^usage
    cm ^shelveset <命令> --^help

== CMD_HELP_SHELVESET ==
示例：

    cm ^shelveset ^create -^c="我的注?"
    cm ^shelveset ^delete ^sh:3
    cm ^shelve ^apply ^sh:3

== CMD_DESCRIPTION_SHELVESET_CREATE ==
?置待定更改。

== CMD_USAGE_SHELVESET_CREATE ==
用法：

    cm ^shelveset ^create | ^mk [<?路径>[ ...]] [--^all] [--^dependencies]
                             [-^c=<注?字符串> | -^commentsfile=<注?文件>]

??：

    ?路径           要?置的?。使用空格?各个用?名?行分隔。
                        使用双引号 (" ") 指定包含空格的
                        路径。
    --^all               ?包括在?定路径上?行了本地更改、
                        移?和?除的?。
    --^dependencies      在要?置的?中包含本地更改
                        依??。
    -^c                  将指定的注??用于?建的?置。
    -^commentsfile       将指定文件中的注??用于?建的
                        ?置。

== CMD_HELP_SHELVESET_CREATE ==
'^shelveset ^create' 命令可将已?出?的内容存?在
    存??中。??，无需?入文件便可以
    保?内容。

?注：

    如果既没有指定 <?路径>，也没有指定任何其他??，??置集将
    包含工作区中的所有待定更改。

    '^shelveset ^create' 操作始?从?定路径以??方式
    ?用。

    ?置?的要求：
    - ?必?受源代?管理。
    - 必??出或更改?（必?使用 '--^all' ??）。

    ?置 PLASTICEDITOR ?境?量可指定用于?入注?的
    ??器。

示例：

    cm ^shelveset ^create -^c="我的注?"
    （?置当前工作区中的所有待定更改，包括
    注?。）

    cm ^shelveset file1.txt "file 2.txt" -^commentsfile=commentshelve.txt
    （?置?定的待定更改并?用 'commentshelve.txt'
    文件中的注?。注意，'^create' 是默?的子命令。）

    cm ^status --^short --^changelist=pending_to_review | cm ^shelveset -
    （?置客?端更改列表。
    以上命令将列出名? 'pending_to_review'
    的更改列表中的路径，并且路径列表将重定向到
    '^shelveset' 命令的?入。）

== CMD_DESCRIPTION_SHELVESET_DELETE ==
?除?置集。

== CMD_USAGE_SHELVESET_DELETE ==
用法：

    cm ^shelveset ^delete | ^rm <?置?格>
    
    ?置?格             ?置集?格。（使用 'cm ^help ^objectspec' 可
                        ?一?了解?置集?格。）

== CMD_HELP_SHELVESET_DELETE ==
'^shelveset ^delete' 命令可?除?置集。

示例：

    cm ^shelveset ^delete ^sh:3
    （?除存?的?置集。）

== CMD_DESCRIPTION_SHELVESET_APPLY ==
?用存?的?置集。

== CMD_USAGE_SHELVESET_APPLY ==
用法：

    cm ^shelveset ^apply <?置?格> [--^mount] [--^encoding=<名称>]
                       [--^comparisonmethod=(^ignoreeol | ^ignorewhitespaces| \
                                            ^ignoreeolwhitespaces | ^notignore)]

    ?置?格             ?置集?格。（使用 'cm ^help ^objectspec' 可
                        ?一?了解?置集?格。）

??：

    --^mount             ?定存??的装入点。
    --^encoding          指定?出??，如：utf-8。
                        ?参?位于以下网址的 MSDN 文档：
                        http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                        以?看包含受支持??及其格式的表格
                        （位于?面末尾的“名称”列中）。
    --^comparisonmethod  ?置比?方法。?参?“?注”以了解更多信息。

== CMD_HELP_SHELVESET_APPLY ==
'^shelveset ^apply' 命令可?原存?的?置集的内容。

?注：

    比?方法：
        ^ignoreeol               忽略行尾差?。
        ^ignorewhitespaces       忽略空格差?。
        ^ignoreeolwhitespaces    忽略行尾和空格差?。
        ^notignore               ??行尾和空格差?。

示例：

    cm ^shelveset ^apply ^sh:3
    （?用存?的?置。）

== CMD_DESCRIPTION_SHOW_FIND_OBJECTS ==
列出?象和属性。

== CMD_USAGE_SHOW_FIND_OBJECTS ==
用法：

    cm ^showfindobjects

== CMD_HELP_SHOW_FIND_OBJECTS ==
可用?象和属性：

^attribute:
    可以通?使用以下字段?行??来?找属性：

    ^type    ：字符串。

              示例：
                  cm ^find ^attribute "^where ^type = '状?'"
                  （?找 '状?' ?型的所有属性。）

    ^value   ：字符串。
    ^date    ：日期。
              ?在本指南中?看“日期常量”以了解更多信息。

              示例：
                  cm ^find ^attribute "^where ^date > '^this ^week'"
                  （?找本周内?用的所有属性。）

    ^owner   ：用?。
              容?特殊用? '^me'。

              示例：
                  cm ^find ^attribute "^where ^value = '已解决' ^and ^owner = '^me'"
                  （?找具有 '已解决' ?并由我?用的所有属性。）

    ^GUID    ：全局唯一??符。
              十六?制 ID，格式? xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx。
    ^comment ：字符串。
    ^srcobj  ：?象?格：?路径、分支、?更集、修?或??。
              使用 'cm ^help ^objectspec' 可了解如何指定?些?象。

              示例：
                  cm ^find ^attribute "^where ^srcobj = '^item:readme.txt'"
                  （?找?用于? 'readme.txt' 的属性。）

                  cm ^find ^attribute "^where ^srcobj = '^br:/main/scm23343'"
                  （?找?用于分支 scm23343 的属性。）

                  cm ^find ^attribute "^where ^srcobj = '^rev:readme.txt#^br:/main/task002'"
                  （?找?用于指定修?的属性。）

                  cm ^find ^attribute "^where ^srcobj = '^rev:^revid:1126'"
                  （?找?用于指定修? ID 的属性。）

    ^ID      ：整数。

^attributetype:
    可以通?使用以下字段?行??来?找属性?型：

    ^name    ：字符串。

              示例：
                  cm ^find ^attributetype "^where ^name ^like 'st%'"
                  （?找名称以 'st' ??的所有属性。）

    ^value   ：字符串。
    ^date    ：日期。
              ?在本指南中?看“日期常量”以了解更多信息。

              示例：
                  cm ^find ^attribute "^where ^date > '^today'"
                  （?找今天?用的所有属性。）

    ^owner   ：用?。
              容?特殊用? '^me'。
    ^GUID    ：全局唯一??符。
              十六?制 ID，格式? xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx。
    ^comment ：字符串。

              示例：
                  cm ^find ^attributetype "^where ^comment != ''" --^xml
                  （?找具有某个注?的所有属性?型，并且以 XML 格式
                  将?出打印到?准?出。）

    ^source  ：?象?格：?路径、分支、?更集或??。
              使用 'cm ^help ^objectspec' 可了解如何指定?些?象。

              示例：

                  cm ^find ^attributetype "^where ^source = '^item:readme.txt'"
                  （?找? 'readme.txt' 中的所有属性?型。）

                  cm ^find ^attributetype "^where ^source = '^cs:30'"
                  （?找?更集 '30' 中的所有属性?型。）

                  cm ^find ^attributetype "^where ^source = '^lb:v0.14.1'"
                  （?找?? 'v0.14.1' 中的所有属性?型。）

    ^ID      ：整数。

    ?制字段。?看下文的“与?制相?的字段”。
        ^ReplLogId
        ^ReplSrcDate
        ^ReplSrcId
        ^ReplSrcRepository
        ^ReplSrcServer

^branch:
    可以通?使用以下字段?行??来?找分支：

    ^name       ：字符串。

                 示例：
                    cm ^find ^branch "^where ^name ^like 'scm23%'"
                    （?找名称以 'scm23' ??的分支。）

    ^date       ：日期。
                 ?在本指南中?看“日期常量”以了解更多信息。

                 示例：
                    cm ^find ^branch "^where ^date > '^one ^week ^ago'"
                    （?找上周?建的分支。）

    ^changesets ：日期（分支中?更集的日期）。
                 ?在本指南中?看“日期常量”以了解更多信息。

                 示例：
                    cm ^find ^branch "^where ^changesets >= '^today'"
                    （?找包含今天?建的?更集的分支。）

    ^attribute  ：字符串。
    ^attrvalue  ：字符串。

                 示例：
                    cm ^find ^branch "^where ^attribute = '状?' ^and ^attrvalue = '已失?'"
                    （?找具有属性 '状?' 且属性?? '已失?'
                    的分支。）
                    
    ^owner      ：用?。
                 容?特殊用? '^me'。
    ^parent     ：分支?格。
                 使用 'cm ^help ^objectspec' 可了解如何指定此?象。

                 示例：
                     cm ^find ^branch "^where ^owner != '^me' ^and ^parent != '^br:/main'"
                     （?找由我以外的人?建且父分支
                     不是 '/main' 的分支。）

    ^comment    ：字符串。
    ^GUID       ：全局唯一??符。
                 十六?制 ID，格式? xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx。

    ^ID         ：整数。

                 示例：
                    cm ^find ^branch "^where ^id = 2029607"
                    （?找 ID ? 2029607 的分支。）

    ?制字段。?看下文的“与?制相?的字段”。
        ^ReplLogId
        ^ReplSrcDate
        ^ReplSrcId
        ^ReplSrcRepository
        ^ReplSrcServer

^changeset:
    可以通?使用以下字段?行??来?找?更集：

    ^branch            ：分支?格。
                        使用 'cm ^help ^objectspec' 可了解如何指定此
                        ?象。

                        示例：
                            cm ^find ^changeset "^where ^branch = '/main/scm23119'"
                            （?找分支 'scm23119' 中的所有?更集。）

    ^changesetid       ：整数。
    ^attribute         ：字符串。

                        示例：
                            cm ^find ^changeset "^where ^attribute = '状?'"
                            （?找包含属性 '状?' 的?更集。）

    ^attrvalue         ：字符串。
    ^date              ：日期。
                        ?在本指南中?看“日期常量”以了解更多信息。
    ^owner             ：用?。
                        容?特殊用? '^me'。

                        示例：
                            cm ^find ^changeset "^where ^date >= '2018/8/6' ^and ^owner != '^me'"
                            （?找?建日期等于或?于 2018/8/6
                            并且由我以外的人?建的所有?更集。）

    ^GUID              ：全局唯一??符。
                        十六?制 ID，格式? xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx。

                        示例：
                            cm ^find ^changeset "^where ^guid = '1b30674f-14cc-4fd7-962b-676c8a6f5cb6'"
                            （?找具有指定 GUID 的?更集。）

    ^comment           ：字符串。

                        示例：
                            cm ^find ^changeset "^where ^comment = ''"
                            （?找不含注?的?更集。）

    ^onlywithrevisions ：布??。
                        ???更集是否具有修?。

                        示例：
                            cm ^find ^changeset "^where ^onlywithrevisions = 'false'"
                            （?找不含修?的?更集。）

    ^returnparent      ：布??。
                        一?返回?更集父?的方法。非常?合用于?写脚本。

                        示例：
                            cm ^find ^changeset "^where ^changesetid = 29 ^and ^returnparent = 'true'"
                            （?找?更集 29 的父?。）

    ^parent            ：?更集 ID（整数）。

                        示例：
                            cm ^find ^changeset "^where ^parent = 548"
                            （?找父???更集 548 的所有?更集。）

    ^ID                ：整数。

    ?制字段。?看下文的“与?制相?的字段”。
        ^ReplLogId
        ^ReplSrcDate
        ^ReplSrcId
        ^ReplSrcRepository
        ^ReplSrcServer

^label:
    可以通?使用以下字段?行??来?找??：

    ^name      ：字符串。

                示例：
                    cm ^find ^label "^where ^name ^like '7.0.16.%'"
                    （?找名称以 '7.0.16.' ??的??。）

    ^attribute ：字符串。
    ^attrvalue ：字符串。
    ^date      ：日期。
                ?在本指南中?看“日期常量”以了解更多信息。

                示例：
                    cm ^find ^label "^where ^date >= '^this ^month' ^and \
                      ^attribute = '?布状?' ^and ^attrvalue != '已?布'"
                    （?找本月?建并且属性 '?布状?' ?置?
                    '已?布' 以外的?的??。）
                    
    ^owner     ：用?。
                容?特殊用? '^me'。
    ^GUID      ：全局唯一??符。
                十六?制 ID，格式? xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx。
    ^branch    ：分支?格。
                使用 'cm ^help ^objectspec' 可了解如何指定此?象。

                示例：
                    cm ^find ^label "^where ^branch = '/main'"
                    （?找?用于主分支的所有??。）

    ^branchid  ：整数。
    ^changeset ：?更集 ID（整数）。

                示例：
                    cm ^find ^label "^where ^changeset = 111733"
                    （?找?用于?更集 111733 的??。）

    ^comment   ：字符串。
    ^ID        ：整数。

    ?制字段。?看下文的“与?制相?的字段”。
        ^ReplLogId
        ^ReplSrcDate
        ^ReplSrcId
        ^ReplSrcRepository
        ^ReplSrcServer

^merge:
    可以通?使用以下字段?行??来?找合并：

    ^srcbranch    ：分支?格。
                   使用 'cm ^help ^objectspec' 可了解如何指定此?象。

                   示例：
                      cm ^find ^merge "^where ^srcbranch = '^br:/main'"
                      （从主分支中?找合并。）

    ^srcchangeset ：?更集 ID（整数）。
    ^dstbranch    ：分支?格。
                   使用 'cm ^help ^objectspec' 可了解如何指定此?象。
    ^dstchangeset ：?更集 ID（整数）。

                   示例：
                      cm ^find ^merge "^where ^dstchangeset = 108261" \
                        --^format="{^srcbranch} {^srcchangeset} {^dstbranch} {^dstchangeset} {^owner}"
                      （?找与?更集 108261 的合并，并打印
                      格式化的?出，从而?示源（分支和?更集 ID）、
                      目?（分支和?更集 ID）和合并所有者。）

    ^date         ：日期。
                   ?在本指南中?看“日期常量”以了解更多信息。
    ^owner        ：用?。
                   容?特殊用? '^me'。
    ^GUID         ：全局唯一??符。
                   十六?制 ID，格式? xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx。
    ^type         ：字符串。
                   可能的?有 '^merge'、'^cherrypick'、
                     '^cherrypicksubstractive'、'^interval'、'^intervalcherrypick'
                     和 '^intervalcherrypicksubstractive'

                   示例：
                      cm ^find ^merge "^where ^type = '^cherrypick' ^and ^owner = '^me'"
                      （?找我所有的挑?。）

    ^ID           ：整数。

^replicationlog:
    可以通?使用以下字段?行??来?找?制日志：

    ^branch         ：分支?格。
                     使用 'cm ^help ^objectspec' 可了解如何指定此?象。

                     示例：
                         cm ^find ^replicationlog "^where ^branch = '/main/gm22358'"
                         （?找分支 'gm22358' 的?制日志。）

    ^repositoryname ：字符串。
    ^owner          ：用?。
                     容?特殊用? '^me'。
    ^date           ：日期。
                     ?在本指南中?看“日期常量”以了解更多信息。
    ^server         ：字符串。
    ^package        ：布??。

                     示例：
                         cm ^find ^replicationlog "^where ^package = 'T' ^and ^server ^like '%cloud%'"
                         （?找从服?器名称中包含 'cloud' 的包
                         ?建的?制日志。）

    ^ID             ：整数。

^review:
    可以通?使用以下字段?行??来?找代???：

    ^status     ：字符串。
    ^assignee   ：字符串。

                 示例：
                    cm ^find ^review "^where ^status = '待定' ^and ^assignee = '^me'"
                    （?找我所有的待定??。）

    ^title      ：字符串。
    ^target     ：?象?格：分支或?更集。
                 使用 'cm ^help ^objectspec' 可了解如何指定此?象。

                 示例：
                    cm ^find ^review "^where ^target = '^br:/main/scm17932'"
                    （?找与分支 'scm17932' 相?的??。）

    ^targetid   ：整数。
    ^targettype ：字符串。
                 可能的?有 '^branch' 和 '^changeset'。

                 示例：
                    cm ^find ^review "^where ^targettype = '^changeset'"
                    （?找目??型??更集的??。）

    ^date       ：日期。
                 ?在本指南中?看“日期常量”以了解更多信息。
    ^owner      ：用?。
                 容?特殊用? '^me'。
    ^GUID       ：全局唯一??符。
                 十六?制 ID，格式? xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx。
    ^ID         ：整数。

^revision:
    可以通?使用以下字段?行??来?找修?：

    ^branch              ：分支?格。
                          使用 'cm ^help ^objectspec' 可了解如何指定此?象。
    ^changeset           ：?更集 ID（整数）。

                          示例：
                              cm ^find ^revision "^where ^changeset >= 111756"
                              （?找在?更集 111756 以及更高版本中
                              ?建的修?。）

    ^item                ：字符串或整数。
    ^itemid              ：整数。

                          示例：
                              cm ^find ^revision "^where ^item = 'readme.txt' ^or ^itemid = 2250"
                              （?找? 'readme.txt' 和? ID 2250
                              的修?。）

                              cm ^find ^revision "^where ^item = 'readme.txt' ^or ^item = 2250"
                              （?取与先前示例相同的修?。）

    ^attribute           ：字符串。
    ^attrvalue           ：字符串。

                          示例：
                              cm ^find ^revision "^where ^attribute = '状?' ^and ^attrvalue != '未完成'"
                              （?找包含属性 '状?' 且属性?
                              不是 '未完成' 的修?。）

    ^archived            ：布??。

                          示例：
                              cm ^find ^revision "^where ^archived = 'true'"
                              （?找在外部存?中存档
                              的修?。）

    ^comment             ：字符串。
    ^date                ：日期。
                          ?在本指南中?看“日期常量”以了解更多信息。
    ^GUID                ：全局唯一??符。
                          十六?制 ID，格式? xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx。
    ^owner               ：用?。
                          容?特殊用? '^me'。
    ^parent              ：修? ID（整数）。
    ^returnparent        ：布??。
    ^shelve              ：?置 ID（整数）。
    ^size                ：整数（以字???位）。
    ^type                ：字符串。
                          可能的?有 '^dir'、'^bin' 和 '^txt'。

                          示例：
                              cm ^find ^revision "^where ^type = '^txt' and \
                                ^size > 300000 ^and ^owner = '^me' and ^date >= '2 ^months ^ago'"
                              （?找我?个月前?建且大小
                              大于 3MB 的文本修?。）

    ^workspacecheckoutid ：整数。
    ^ID                  ：整数。

    ?制字段。?看下文的“与?制相?的字段”。
        ^ReplLogId
        ^ReplSrcDate
        ^ReplSrcId
        ^ReplSrcRepository
        ^ReplSrcServer

^shelve:
    可以通?使用以下字段?行??来?找?置：

    ^owner     ：用?。
                容?特殊用? '^me'。
    ^date      ：日期。
                ?在本指南中?看“日期常量”以了解更多信息。

                示例：
                    cm ^find ^shelve "^where ^owner != '^me' ^and ^date >= '^1 ^years ^ago'"
                    （?找去年我以外的人?建的
                    ?置。）

    ^attribute ：字符串。
    ^attrvalue ：字符串。
    ^comment   ：字符串。
    ^GUID      ：全局唯一??符。
                十六?制 ID，格式? xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx。
    ^parent    ：整数。
    ^shelveid  ：整数。

                示例：
                    cm ^find ^shelve "^where ^shelveid = 2"
                    （?找名称? 2 的?置。）

    ^ID        ：整数。

                示例：
                    cm ^find ^shelve "^where ^id >= 3848"
                    （?找?象 ID 大于 3848 的?置。）

    ?制字段。?看下文的“与?制相?的字段”。
        ^ReplLogId
        ^ReplSrcDate
        ^ReplSrcId
        ^ReplSrcRepository
        ^ReplSrcServer


与?制相?的字段：
    ?多?象会跟踪?制数据，?意味着 Unity VCS 会跟踪最初?建?制
    的位置。

    可以使用的字段包括：

        ^ReplSrcServer     ：存???格。表示“?制源服?器”。
                            从中?制?象的服?器。

              示例：
                            cm ^find ^branch "^where ^replsrcserver='skull.codicefactory.com:9095'"
                            （?找从服?器 'skull' ?制的分支。）

        ^ReplSrcRepository ：字符串。表示“?制源存??”。?是
                            从中?制?象的存??。

              示例：
                            cm ^find ^branch "^where ^replsrcserver = 'skull.codicefactory.com:9095' \
                              ^and ^replsrcrepository = 'codice'"
                            （?找从服?器 'skull' 和存?? 'codice'
                            ?制的分支。）

        ^ReplLogId         ：整数。?制操作的 ID。在 Unity VCS 中，
                            ?次从?本?建新?象?，
                            都会?建一个新的 'replicationlog'。

              示例：
                            cm ^find ^revision "^where ^repllogid = 2019974"
                            （?找从?本 2019974 ?制
                            的修?。）

        ^ReplSrcDate       ：日期。?是???生?制的
                            日期。
                            ?制的?象将保留其原始?建
                            日期，如果要?找在特定??
                            范?内?制的?象，?此字段
                            很有用。

              示例：
                            cm ^find ^label "^where ^replsrcdate >= '^one ^month ^ago' \
                              ^and ^date >= '15 ^days ^ago'"
                            （?找 15 天前?建并在一个月前
                            ?制的??。）
                     
                            cm ^find ^replicationlog "^where ^date > '^one ^week ^ago'"
                            8780433  27/09/2018 8:49:38 codice@BACKYARD:8087 F   mbarriosc
                            （?找一周前?建的?制日志。）

                            ?在可以???制的分支是否是
                            在?制之前?建的：

                            cm ^find ^branch "^where ^repllogid = 8780433"
                            8780443  26/09/2018 12:20:55 /main/scm23078 maria    codice T

        ^ReplSrcId         ：整数。?是?制源服?器的 ID。
                            可以使用 'cm ^find' 命令搜索
                            '^replicationsource' ?象来??此 ID。

              示例：
                            cm ^find ^replicationsource
                            7860739  codice@AFRODITA:8087 d9c4372a-dc55-4fdc-ad3d-baeb2e975f27
                            8175854  codice@BACKYARD:8087 66700d3a-036b-4b9a-a26f-adfc336b14f9

                            ?在可以?找从 codice@AFRODITA:8087
                            ?制的?更集：

                            cm ^find ^changesets "^where ^replsrcid = 7860739"


日期常量：
    可以使用遵循?算机本地化?置的日期格式。
    例如，如果?算机以 'MM-dd-yyyy' 格式?示日期，
    ?可以在??中使用 '12-31-2019' 之?的日期。

    ?可以使用以下常量来?化??：
        '^today'         ：今天的日期。
        '^yesterday'     ：昨天的日期。
        '^this ^week'     ：本周星期一的日期。
        '^this ^month'    ：本月第一天的日期。
        '^this ^year'     ：本年 1 月 1 日。
        '^one ^day ^ago'   ：当前日期前一天。
        '^one ^week ^ago'  ：当前日期前七天。
        '^one ^month ^ago' ：当前日期前一个月。
        'n ^days ^ago'    ：当前日期前 'n' 天。
        'n ^months ^ago'  ：当前日期前 'n' 个月。
        'n ^years ^ago'   ：当前日期前 'n' 年。

    以下 '^where' 子句?于?型? '^date' 的字段有效：
        '(...)^where ^date > '^today' (...)'
        '(...)^where ^date < '^yesterday' (...)'
        '(...)^where ^date > '^this ^week' (...)'
        '(...)^where ^date > '^this ^month' (...)'
        '(...)^where ^date < '^one ^day ^ago' ^and ^date > '3 ^days ^ago' (...)'
        '(...)^where ^date < '^one ^week ^ago' ^and ^date > '3 ^weeks ^ago' (...)'
        '(...)^where ^date < '^one ^month ^ago' ^and ^date > '3 ^months ^ago' (...)'
        '(...)^where ^date > '1 ^year ^ago' (...)'

    ?可以在 'cm ^find' 命令中?制使用特定日期格式，方法是使用
    --^dateformat ?志。??看 'cm ^find --^help' 以了解更多??信息。

== CMD_DESCRIPTION_TRIGGER_SHOWTYPES ==
?示可用的触?器?型。

== CMD_USAGE_TRIGGER_SHOWTYPES ==
用法：

    cm ^trigger ^showtypes

== CMD_DESCRIPTION_SHOWACL ==
?示?象的 ACL。

== CMD_USAGE_SHOWACL ==
用法：

    cm ^showacl | ^sa <?象?格> [--^extended] [--^xml[=<?出文件>]]
                                [--^encoding=<名称>]

    ?象?格         要?示相? ACL 的?象的?格。
                        此命令的有效?象?：
                        存??服?器、存??、分支、?更集、??、?
                        和属性。
                        （使用 'cm ^help ^objectspec' 可?一?了解?格。）

??：

      --^extended        ?示 ACL ?次???。
      --^xml             以 XML 格式将?出打印到?准?出。
                        可以指定?出文件。
      --^encoding        与 '--^xml' ???合使用，指定要在 XML ?出
                        中使用的??（例如：utf-8）。
                        ?参?位于以下网址的 MSDN 文档：
                        http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                        以?看包含受支持??及其格式的表格
                        （位于?面末尾的“名称”列中）。

== CMD_HELP_SHOWACL ==
示例：

    cm ^showacl ^repserver:PlasticServer:8084
    （?示所?服?器的 ACL。）

    cm ^sa ^br:/main --^extended
    （?示所?分支?格的 ACL ?次???。）

== CMD_DESCRIPTION_SHOWCOMMANDS ==
?示所有可用命令。

== CMD_USAGE_SHOWCOMMANDS ==
用法：

    cm ^showcommands

== CMD_HELP_SHOWCOMMANDS ==

== CMD_DESCRIPTION_SHOWOWNER ==
?示?象的所有者。

== CMD_USAGE_SHOWOWNER ==
用法：

    cm ^showowner | ^so <?象?格>

    ?象?格         要?示相?所有者的?象的?格。
                        ?象必??下列其中之一：
                        存??服?器、存??、分支、?更集、
                        ??、属性、修?和?。
                        （使用 'cm ^help ^objectspec' 可?一?了解?格。）

== CMD_HELP_SHOWOWNER ==
?注：

    此命令将?示?象的所有者。所有者可以是用?或
    ?。可以使用 'cm ^setowner' 命令修改所有者。

示例：

    cm ^showowner ^repserver:PlasticServer:8084
    （?示所?服?器的所有者。）

    cm ^so ^item:samples\
    （?示所???格的所有者。）

== CMD_DESCRIPTION_SHOWPERMISSIONS ==
列出可用的?限。

== CMD_USAGE_SHOWPERMISSIONS ==
用法：

      cm ^showpermissions | ^sp

== CMD_HELP_SHOWPERMISSIONS ==
示例：

    cm ^showpermissions

== CMD_DESCRIPTION_SHOWSELECTOR ==
?示工作区??器。

== CMD_USAGE_SHOWSELECTOR ==
已弃用此命令。?了向后兼容，此命令仍然存在，
但是在 Plastic SCM 4.0 中，已在很大程度上弃用??器。仍然存在的??器
是?了指定工作分支或?更集，但不再支持
用于??路径的旧??。

用法：

    cm ^showselector | ^ss [<工作区路径> | <工作区?格>]

    工作区路径             用于?示??器的工作区的路径。
    工作区?格             工作区的?格。（使用 'cm ^help ^objectspec' 可
                        可?一?了解工作区?格。）

== CMD_HELP_SHOWSELECTOR ==
?注：

    如果路径和工作区?格均未指定，??命令将采用
    当前目?作?工作区路径。

示例：

    cm ^showselector c:\workspace
    （?示所?工作区路径的??器。）

    cm ^ss
    （?示当前工作区的??器。）

    cm ^showselector > mySelector.txt
    （将当前工作区的??器写入文件。）

    cm ^showselector ^wk:mywk@reptest
    （?示存?? 'reptest' 中的工作区 'mywk' 的??器。）

== CMD_DESCRIPTION_SUPPORT ==
允?用??行与支持相?的操作。

== CMD_USAGE_SUPPORT ==
用法：

    cm ^support <命令> [??]

命令：

    ^bundle

    要?取有??条命令的更多信息，??行：
    cm ^support <命令> --^usage
    cm ^support <命令> --^help

== CMD_HELP_SUPPORT ==
示例：

    cm ^support
    cm ^support ^bundle
    cm ^support ^bundle c:\outputfile.zip

== CMD_DESCRIPTION_SUPPORT_BUNDLE ==
?建?有相?日志的“支持??包”包。
可以在?求?助、??其他信息或者提交 bug ?
附加?文件。

== CMD_USAGE_SUPPORT_BUNDLE ==
用法：

    cm ^support ^bundle [<?出文件>]

??：

    ?出文件          在指定的位置?建“支持??包”
                        包。

== CMD_HELP_SUPPORT_BUNDLE ==
?注：

此命令允?用??建一个“支持??包”包，然后便可在?求?助、
??其他信息或提交 bug ?附加?个包。
用?可以??指定?出文件的位置；否?，
?出文件将写入到??目?。

示例：

    cm ^support ^bundle
    （在??目?中?建“支持??包”。）

    cm ^support ^bundle c:\outputfile.zip
    （在指定位置?建“支持??包”。）

== CMD_DESCRIPTION_SWITCH ==
将工作区切?到分支、?更集、??或?置集。

== CMD_USAGE_SWITCH ==
用法：

    cm ^switch (<分支?格> | <?更集?格> | <???格> | <?置?格>)
              [--^workspace=<路径>] [--^repository=<名称>]
              [--^forcedetailedprogress]

    （使用 'cm ^help ^objectspec' 可?一?了解分支、?更集、??、
    和?置集?格。）

??：

    --^workspace             工作区所在的路径。
    --^repository            切?到指定的存??。
    --^forcedetailedprogress 即使在?准?出被重定向的情况下也要?制
                            ?示???度。

== CMD_HELP_SWITCH ==
?注：

    此命令允?用?将工作区?更新?指定?象
    （分支、??、?置集或?更集）的内容。

示例：

    cm ^switch ^br:/main
    cm ^switch ^lb:Rel1.1
    cm ^switch ^br:/main/scm002 --^repository=rep2
    cm ^switch ^cs:4375
    cm ^switch ^sh:2

== CMD_DESCRIPTION_SWITCH_TO_BRANCH ==
将分支?置?工作分支。

== CMD_USAGE_SWITCH_TO_BRANCH ==
已弃用此命令。?改用 cm switch。

用法：

    cm ^switchtobranch [??] [分支?格]

    分支?格：分支的?格。

??：

    --^label=名称 | --^changeset=数字：从指定??或?更集
      加?修?。如果未提供任何分支?格，?需要?些??
      之一。
    --^changeset=?更集：切?到指定的?更集。
    --^repository=存??：切?到指定的存??。
    --^workspace | -wk=路径：工作区所在的路径。

== CMD_HELP_SWITCH_TO_BRANCH ==
?注：

    此命令允?用?在分支中工作。
    如果未指定分支?格，?必?指定??或?更集。
    如果未指定存??，?分支将?置?当前存??。

示例：

    cm ^switchtobranch ^br:/main
    cm ^switchtobranch ^br:/main/task001

    cm ^switchtobranch --^label=BL050
    （只?配置。?命令将加?已??的?更集
    的内容。）

== CMD_DESCRIPTION_SYNC ==
与 Git 同?。

== CMD_USAGE_SYNC ==
用法：

    cm ^synchronize | ^sync <存???格> ^git [<url> [--^user=<用?名> --^pwd=<密?>]]
                          [(--^txtsimilaritypercent | --^binsimilaritypercent | \
                            --^dirsimilaritypercent)=<?>]
                          [--^author] [--^skipgitlfs]


    存???格             存??的?格。（使用 'cm ^help ^objectspec' 可
                        ?一?了解存???格。）
    git                 （默??）。

??：

    url                       ?程存?? URL（http(s):// 或 git:// 或
                              SSH URL）。
    --^user                    指定的 URL 的用?名。
    --^pwd                     指定的 URL 的密?。
    --^txtsimilaritypercent \
    --^binsimilaritypercent \
    --^dirsimilaritypercent
                              要??已移?的?，与 Unity VCS GUI 的
                              方法相同。
    --^author                  使用 Git 作者提供的名称和??戳?。
                              （默?? Git 提交者）
    --^skipgitlfs              忽略 .gitattributes 文件中的
                              Git LFS 配置。行??似于不支持
                              Git LFS。

== CMD_HELP_SYNC ==
?注：

    如果 Git 存??需要用?名和密?，??使用 '^url'、'--^user'、
    和 '--^pwd' ??。
    如果 Git 存??不需要用?名和密?，??在第一次同?
    操作中使用 '^url' ??。在后?的同?操作中，'^url'
    ??是可?的。

    要使用 SSH ???行同?，必?将 'ssh' 客?端
    添加到 PATH ?境?量，并正?配置??接到
    ?程主机（即配置私?/公?）。

示例：

    cm ^sync default@localhost:8087 ^git git://localhost/repository

== CMD_DESCRIPTION_TRIGGER ==
允?用?管理触?器。

== CMD_USAGE_TRIGGER ==
用法：

    cm ^trigger | ^tr <命令> [??]

命令：

    ^create | ^mk
    ^delete | ^rm
    ^edit
    ^list   | ^ls
    ^showtypes

    要?取有??条命令的更多信息，??行：
    cm ^trigger <命令> --^usage
    cm ^trigger <命令> --^help

== CMD_HELP_TRIGGER ==
示例：

    cm ^tr ^mk ^before-mklabel new "/path/to/script" --^server=myserver:8084
    cm ^tr ^edit ^before-mklabel 7 --^position=4 --^server=myserver:8084
    cm ^tr ^ls ^before-mkbranch --^server=myserver:8084
    cm ^tr ^rm ^after-setselector 4
    cm ^tr ^showtypes

== CMD_DESCRIPTION_TUBE ==
?行与 Plastic Tube 有?的命令。

== CMD_USAGE_TUBE ==
用法：

    cm ^tube ^config -^u=<用?> -^p=<密?>
    （配置 Unity VCS 以将 Plastic Tube 与指定的用?和密?
    配合使用。）


    cm ^tube ^create <?程用?>
    （?建 Tube“?程用? -> 我的用?”。
    用?“我的用?”允?“?程用?”?接到“我的用?”服?器。
    可以建立从“?程用?”到“我的用?”的?接。
    只能?建其他用?到当前 Tube 用?的 Tube。）

    cm ^tube ^remove <?程用?>
    （?除 Tube“?程用? -> 我的用?”。）


    cm ^tube ^local
    （列出在本地服?器上共享的本地存??以及共享到
    的用?。）
    
    cm ^tube ^remote
    （列出与当前 Tube 用?共享的共享?程
    存??。）


    cm ^tube ^share <存???格>[ ...]-^u=<?程用?> -^a=(^pull | ^push | ^pull,^push)
    （与?程用?共享本地存??并?置指定的
    ??模式。使用空格?各个存???格?行分隔。）

    cm ^tube ^unshare <存???格>[ ...]-^u=<?程用?>
    （取消与?程用?共享本地存??。使用空格
    ?各个存???格?行分隔。）


    cm ^tube ^connect
    （将 Unity VCS 服?器?接到 Plastic Tube。）
    cm ^tube ^disconnect
    （断? Unity VCS 服?器与 Plastic Tube 的?接。）
    cm ^tube ^status
    （?示 Unity VCS 服?器是否已?接到 Plastic Tube。）

??

    -^u                Tube 用?（plasticscm.com 用?）。
    -^p                用?密?。
    -^a                ?置??模式。
    存???格          存??的?格。（使用 'cm ^help ^objectspec' 可
                      ?一?了解存???格。）

== CMD_HELP_TUBE ==
?注：

    使用 'cm ^tube' 命令来管理 Plastic Tube。

示例：

    cm ^tube ^config -^u=ruben@codicesoftware.com -^p=rubenpassword

    cm ^tube ^create pablo@codicesoftware.com
    （'pablo@codicesoftware.com' 可以?接到当前的 Plastic Tube 用?）
    cm ^tube ^remove pablo@codicesoftware.com

    cm ^tube ^local
    cm ^tube ^remote

    cm ^tube ^share repo@server:8087 -^u=pablo@codicesoftware.com -^a=^pull,^push
    cm ^tube ^share repo@server:8087 doc@server:8087 -^u=pablo@codicesoftware.com -^a=^push
    cm ^tube ^unshare repo@server:8087 -^u=pablo@codicesoftware.com

    cm ^tube ^connect
    cm ^tube ^disconnect
    cm ^tube ^status

== CMD_DESCRIPTION_UNDOCHECKOUT ==
撤??的?出。

== CMD_USAGE_UNDOCHECKOUT ==
用法：

    cm ^undocheckout | ^unco <?路径>[ ...][-^a | --^all] [--^symlink] [--^silent]
                           [--^machinereadable [--^startlineseparator=<分隔符>]
                            [--^endlineseparator=<分隔符>] [--^fieldseparator=<分隔符>]]

    ?路径           要?用操作的?。使用空格?各个路径?行
                        分隔。使用双引号 (" ") 指定包含空格
                        的路径。
                        使用 . 将操作?用到当前目?。

??：

    -^a | --^all           撤?指定?中的所有更改。如果
                         已?出?，??出将被
                         ?原。如果?是在本地修改的，?
                         修改将被?原。
    --^symlink            将 undocheckout 操作?用于?接而不是
                         目?。
    --^silent             不?示任何?出。
    --^machinereadable    以易于解析的格式?出?果。
    --^startlineseparator 与 '--^machinereadable' ?志?合使用，
                         指定行?如何??。
    --^endlineseparator   与 '--^machinereadable' ?志?合使用，
                         指定行?如何?尾。
    --^fieldseparator     与 '--^machinereadable' ?志?合使用，
                         指定?如何分隔字段。

== CMD_HELP_UNDOCHECKOUT ==
?注：

    如果已?出某个?并且不想将?个??入，?可以
    使用此命令撤??出。文件和文件?都可以取消
    ?出。?将更新??出之前的状?。

    要求：
      - ?必?受源代?管理。
      - 必??出?。

从 stdin ?取?入：

    '^undocheckout' 命令可从 stdin ?取路径。?此，???
    一个破折号 "-"。
    示例：cm ^undocheckout ^checkin -

    路径将一直?取到有空行?入?止。
    ??情况下允?使用??指定要撤??出的文件。
    示例：
      dir /S /B *.c | cm ^undocheckout --^all -
      （在 Windows 中，撤??工作区中所有 .c 文件的?出。）

示例：

    cm ^undocheckout .
    （撤?当前目?中的?出。）

    cm ^undocheckout file1.txt file2.txt
    cm unco c:\workspace\file.txt
    （撤??出所?文件。）

    cm ^unco -^a file1.txt
    （撤?? 'file1.txt' 的?出或本地修改）

    cm ^unco ?接 --^symlink
    （将 undocheckout 操作?用于?接文件而不是目?，
    ?用于 UNIX ?境。）

    cm ^status --^short --^changelist=pending_to_review | cm ^undocheckout -
    （撤?客?端更改列表。
    以上命令将列出名? 'pending_to_review'
    的更改列表中的路径，并且路径列表将重定向到
    undocheckout 命令的?入。）

    cm ^unco .--^machinereadable
    （撤?当前目?中的?出，并以易于解析的
    ?化格式打印?果。）

    cm ^unco .--^machinereadable --^startlineseparator=">" --^endlineseparator="<" \
      --^fieldseparator=","
    （撤?当前目?中的?出，并以易于解析的?化
    格式打印?果，以指定的字符串作?行的??和?尾以及
    ?各字段?行分隔。）

== CMD_DESCRIPTION_UNDOCHECKOUTUNCHANGED ==
撤?未更改的已?出?。

== CMD_USAGE_UNDOCHECKOUTUNCHANGED ==
用法：

    cm ^uncounchanged | ^unuc <?路径>[ ...][-^R | -^r | --^recursive]
                            [--^symlink] [--^silent]

    ?路径           要?用操作的?。使用空格?各个路径?行
                        分隔。使用双引号 (" ") 指定包含空格
                        的路径。
                        使用 . 将操作?用到当前目?。

??：

    -^R                  在指定路径中以??方式撤?未更改的?。
    --^symlink           将 uncounchanged 操作?用于?接而不是
                        目?。
    --^silent            不?示任何?出。

== CMD_HELP_UNDOCHECKOUTUNCHANGED ==
?注：

    此命令是从工作区根目?以??方式?用的。

从 stdin ?取?入：

    '^uncounchanged' 命令可从 stdin ?取路径。?此，???
    一个破折号 "-"。
    示例：cm ^uncounchanged -

    路径将一直?取到有空行?入?止。
    ??情况下允?使用??指定要撤??出的
    未更改文件。
    示例：
      dir /S /B *.c | cm ^uncounchanged -
      （在 Windows 中，撤??工作区中所有未更改的 .c 文件
      的?出。）

示例：

    cm ^uncounchanged .-^R
    （在当前目?中以??方式撤?未更改文件的?出。）

    cm ^unuc /home/myuser/mywk/project/templates -^R
    （在所?目?中以??方式撤?未更改文件的?出。）

== CMD_DESCRIPTION_UNDELETE ==
使用特定修?来取消?除某个?。

== CMD_USAGE_UNDELETE ==
用法：

    cm ^undelete <修??格> <路径>

    修??格             所含内容将加?到工作区中的
                        修?的?格。（使用 'cm ^help ^objectspec' 可
                        ?一?了解修??格。）
    路径                ?原路径。

== CMD_HELP_UNDELETE ==
?注：

    要取消?除的?不?加?到工作区中。

    Xlink 不支持 '^undelete' 操作。

示例：

    cm ^undelete ^revid:756 C:\mywks\src\foo.c
    cm ^undelete ^itemid:68#^cs:2 C:\mywks\dir\myfile.pdf
    cm ^undelete ^serverpath:/src#^br:/main C:\mywks\Dir

== CMD_DESCRIPTION_UNDOCHANGE ==
撤?路径中的更改。

== CMD_USAGE_UNDOCHANGE ==
用法：

    cm ^undochange | ^unc <?路径>[ ...][-^R | -^r | --^recursive]

    ?路径       要?用操作的?。使用空格?各个路径?行
                    分隔。使用双引号 (" ") 指定包含空格
                    的路径。
                    使用 . 将操作?用到当前目?。

??：

    -^R              以??方式?用操作。

== CMD_HELP_UNDOCHANGE ==
?注：

    如果已?出某个?或已修改但未?入?个?，并且不想
    ?入?个?，?可以使用此命令撤?更改。?将
    更新?之前的内容。

从 stdin ?取?入：

    '^undochange' 命令可从 stdin ?取路径。?此，???
    一个破折号 "-"。
    示例：cm ^undochange -

    路径将一直?取到有空行?入?止。
    ??情况下允?使用??指定要撤?更改的文件。
    示例：
      dir /S /B *.c | cm ^undochange -
      （在 Windows 中，撤??工作区中所有 .c 文件的更改。）

示例：

    cm ^unc .
    （撤?当前目?中的文件更改。）

    cm ^undochange .-^R
    （以??方式撤?当前目?中的文件更改。）

    cm ^unc file1.txt "file 2.txt"
    （撤?所?文件的更改。）

    cm ^unc c:\workspace\file.txt
    （撤?所?文件的更改。）

== CMD_DESCRIPTION_UNDO ==
撤?工作区中的更改。

== CMD_USAGE_UNDO ==
用法：

    cm ^undo [<路径>[ ...]] [--^symlink] [-^r | --^recursive] [<??器>[ ...]]
            [--^silent | --^machinereadable [--^startlineseparator=<分隔符>]
                            [--^endlineseparator=<分隔符>] [--^fieldseparator=<分隔符>]]

    路径                要?用操作的文件或目?
                        的路径。使用双引号 (" ") 指定包含空格的
                        路径。使用空格?各个路径?行分隔。
                        如果未指定路径，?默?情况下，
                        撤?操作将?用于当前目?中的所有
                        文件。
    ??器              将一个或多个指定??器?用于?定
                        路径。使用空格?各个??器?行分隔。?参?
                        “??器”部分以了解更多信息。

??：

    --^symlink               将撤?操作?用于符号?接而不是
                            目?。
    -^r                      以??方式?行撤?。
    --^silent                不?示任何?出。
    --^machinereadable       以易于解析的格式?出?果。
    --^startlineseparator    与 '--^machinereadable' ?志?合使用，指定
                            行?如何??。
    --^endlineseparator      与 '--^machinereadable' ?志?合使用，指定
                            行?如何?尾。
    --^fieldseparator        与 '--^machinereadable' ?志?合使用，指定
                            ?如何分隔字段。
??器：

    如果未指定?志，?默?情况下将撤?所有更改，但是
    可以使用以下一个或多个?志来??路径。
    如果文件或目?匹配一个或多个指定??的更改，
    ?将撤??文件或目?上的所有更改。
    例如，如果同?指定了 '--^checkedout' 和 '--^moved'，?在
    已?出并移?文件的情况下，??更改都将被撤?。

    --^checkedout            ??已?出的文件和目?。
    --^unchanged             ??内容不?的文件。
    --^changed               ??本地更改的或?出的文件和
                            目?。
    --^deleted               ??已?除的文件和目?。
    --^moved                 ??已移?的文件和目?。
    --^added                 ??已添加的文件和目?。

== CMD_HELP_UNDO ==
?注：

    '^undo' 命令有??，因??命令在撤?工作后将不可逆。
    ^undo 完成后，无法将受其影?的文件和目?
    恢?到先前状?。如果参数中
    未指定路径，?默?情况下，?命令将撤?
    当前目?中的所有更改，但不采用??方式。
    从 /src 目??行?，以下命令是等效的：

        /src
        |- file.txt
        |- code.cs
        \- /test
           |- test_a.py
           \- test_b.py

        cm ^undo
        cm ^undo *
        cm ^undo file.txt code.cs /test

        cm ^undo .
        cm ^undo /src file.txt code.cs

    如果希望操作以??方式?行，必?指定 '-^r' ?志。

    撤?某一目?下的所有更改（包括影?目?
    本身的更改）：

        cm ^undo 目?路径 -^r

    如果目?路径是工作区路径，?工作区中的?个更改都将
    被撤?。

已?除?：

    要撤??文件和目?的?除，必?指定?的
    完整路径，或者指定所属目?并使用?? ('-^r')
    ?志。

    例如：

    cm ^undo .
    （不撤?当前目?中的?除。）

    cm ^undo .-^r
    （以??方式撤?当前目?中的所有?除（和其他更改）。）

    cm ^undo src/file.txt
    （撤?? src/file.txt 的?除（或其他更改）。）

示例：

    cm ^undo .-^r
    （以??方式撤?当前目?中的所有更改。如果从工作区的
    根目??行，?撤?整个工作区中的所有更改。）

    cm ^co file.txt
    cm ^undo file.txt
    （撤?? 'file.txt' 的?出。）

    ^echo ^content >> file.txt
    cm ^undo file.txt
    （撤?? 'file.txt' 的本地更改。）

    cm ^undo src
    （撤?? src 目?及其文件的更改。）

    cm ^undo src/*
    （撤?? src 中包含的?个文件和目?的更改，而不会
    影? src。）

    cm ^undo *.cs
    （撤??当前目?中与 *.cs 匹配的?个文件或目?的
    更改。）

    cm ^undo *.cs -^r
    （撤??当前目?中以及下?所有目?中与 *.cs 匹配的
    ?个文件或目?的更改。）

    cm ^co file1.txt file2.txt
    ^echo ^content >> file1.txt
    cm ^undo --^unchanged
    （撤??未更改的 'file2.txt' 的?出，并忽略本地更改的
    'file1.txt'。）

    ^echo ^content >> file1.txt
    ^echo ^content >> file2.txt
    cm ^co file1.txt
    cm ^undo --^checkedout
    （撤?已?出文件 'file1.txt' 中的更改，并忽略 'file2.txt'，
    因?后者未?出。）

    cm ^add file.txt
    cm ^undo file.txt
    （撤?添加 'file.txt'，使?文件再次成?私有文件。）

    ^rm file1.txt
    ^echo ^content >> file2.txt
    cm ^add file3.txt
    cm ^undo --^deleted --^added *
    （撤? 'file1.txt' 的?除和 'file3.txt' 的添加，并忽略 'file2.txt'
    的更改。）

== CMD_DESCRIPTION_LOCK_UNLOCK ==
撤??定服?器上的??定。

== CMD_USAGE_LOCK_UNLOCK ==
用法：

    cm ^lock ^unlock [<存??服?器?格>] <GUID>[ ...]

    存??服?器?格   存??服?器?格。（使用 'cm ^help ^objectspec'
                    可?一?了解存??服?器?格。）
    GUID            要解?的? GUID 的列表。使用空格?各个 GUID
                    ?行分隔。

== CMD_HELP_LOCK_UNLOCK ==
?注：

    - ?命令使用指定的服?器来解??。
    - 如果未指定服?器，??命令会??从当前工作区
      ?取服?器。
    - 如果在先前的??中未?算服?器，?会从当前的
      Unity VCS 客?端配置中?取服?器。
    - 只有服?器的管理?才能?行 'cm ^unlock' 命令。
    - 要指定 GUID，格式?? 32 位数字，并用
      短横?分隔（也可以??用花括号括起来）：

        {00000000-0000-0000-0000-000000000000}
      或 00000000-0000-0000-0000-000000000000

示例：

    cm ^lock ^unlock 91961b14-3dfe-4062-8c4c-f33a81d201f5
    （撤?所???定。）

    cm ^lock ^unlock DIGITALIS:8084 2340b4fa-47aa-4d0e-bb00-0311af847865 \
      bcb98a61-2f62-4309-9a26-e21a2685e075
    （撤?名? 'DIGITALIS' 的?定服?器上的所???定。）

    cm ^lock ^unlock tardis@cloud 4740c4fa-56af-3dfe-de10-8711fa248635 \
      71263c17-5eaf-5271-4d2c-a25f72e101d4
    （撤?名? 'tardis' 的 Cloud ?定服?器上的所???定。）

== CMD_DESCRIPTION_UPDATE ==
更新工作区并下?最新的更改。

== CMD_USAGE_UPDATE ==
用法：

    cm ^update [<?路径> | --^last]
              [--^changeset=<?更集?格>] [--^cloaked] [--^dontmerge] [--^forced]
              [--^ignorechanges] [--^override] [--^recursewk] [--^skipchangedcheck]
              [--^silent] [--^verbose] [--^xml[=<?出文件>]] [--^encoding=<名称>]
              [--^machinereadable [--^startlineseparator=<分隔符>]
                [--^endlineseparator=<分隔符>] [--^fieldseparator=<分隔符>]]
              [--^forcedetailedprogress]

    ?路径           要更新的路径。
                        使用 . 将更新?用到当前目?。
                        如果未指定路径，?当前工作区
                        将完全更新。
    --^last              在更新之前，将工作区??器
                        从?更集配置或??配置更改?
                        分支配置。
                        ??器将更改??更集或??
                        所属的分支。

??：

    --^changeset             将工作区更新?特定?更集。
                            （使用 'cm ^help ^objectspec' 可?一?了解
                            ?更集?格。）
    --^cloaked               在更新操作中包含掩蔽的?。
                            如果未指定此??，?在操作中
                            将忽略掩蔽的?。
    --^dontmerge             如果在更新操作期?需要
                            合并更新，?不要?行合并。
    --^forced                ?制将?更新???器中指定的
                            修?。
    --^ignorechanges         忽略在更新工作区?
                            ??到待定更改的情况下?示
                            的待定更改警告消息。
    --^override              覆盖不受 Unity VCS 控制的已更改文件。
                            ?些文件的内容将被服?器内容
                            覆盖。
    --^recursewk             更新在当前路径中找到的所有
                            工作区。?合用于更新特定路径中包含的
                            所有工作区。
    --^skipchangedcheck      ?更新在?始之前会??工作区中是否
                            存在本地更改。如果始?在修改
                            文件之前?出，?可以?行此??并
                            加快操作速度。
    --^silent                不?示任何?出。
    --^verbose               ?示其他信息。
    --^xml                   以 XML 格式将?出打印到?准?出。
                            可以指定?出文件。
    --^encoding              与 --^xml ???合使用，指定要在 XML ?出
                            中使用的??（例如：utf-8）。
                            ?参?位于以下网址的 MSDN 文档：
                            http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                            以?看包含受支持??及其格式的表格
                            （位于?面末尾的“名称”列中）。
    --^machinereadable       以易于解析的格式?出?果。
    --^startlineseparator    与 '--^machinereadable' ?志?合使用，
                            指定行?如何??。
    --^endlineseparator      与 '--^machinereadable' ?志?合使用，
                            指定行?如何?尾。
    --^fieldseparator        与 '--^machinereadable' ?志?合使用，
                            指定?如何分隔字段。
    --^forcedetailedprogress 即使在?准?出被重定向的情况下也要?制?示
                            ???度。
== CMD_HELP_UPDATE ==
?注：

    '^update' 命令?下?所需的文件。

    ?命令采用??操作。

    使用 --^last' ???，无需指定路径。
    在??情况下，当前工作目?所属的工作区
    将更新。
    （切?，如果工作区??器先前指向某个
    ?更集或??，?指定??志可能?致
    工作区??器更改?分支配置。）


示例：

    cm ^update
    （更新当前工作区中的所有?。）

    cm ^update .
    （更新当前目?以及所有子?。）

    cm ^update .--^forced --^verbose
    （?制?索所有修?。）

    cm ^update --^last

    cm ^update .--^machinereadable --^startlineseparator=">"
    （更新当前目?，并以易于解析的?化格式
    打印?果，以指定的字符串作?行的
    ??。）

== CMD_DESCRIPTION_VERSION ==
?示当前的客?端版本号。

== CMD_USAGE_VERSION ==
用法：

    cm ^version

== CMD_HELP_VERSION ==

== CMD_DESCRIPTION_WHOAMI ==
?示当前的 Unity VCS 用?。

== CMD_USAGE_WHOAMI ==
用法：

    cm ^whoami

== CMD_HELP_WHOAMI ==

== CMD_USAGE_WKTREENODESTATUS ==
用法：

    cm ^wktreenodestatus 路径1, 路径2, ...

== CMD_DESCRIPTION_WORKSPACE ==
允?用?管理工作区。

== CMD_USAGE_WORKSPACE ==
用法：

    cm ^workspace | ^wk <命令> [??]

命令：

    ^list   | ^ls
    ^create | ^mk
    ^delete | ^rm
    ^move   | ^mv
    ^rename

    要?取有??条命令的更多信息，??行：
    cm ^workspace <命令> --^usage
    cm ^workspace <命令> --^help

== CMD_HELP_WORKSPACE ==
示例：

    cm ^workspace ^create myWorkspace 工作区路径
    cm ^workspace ^list
    cm ^workspace ^delete myWorkspace

== CMD_DESCRIPTION_WORKSPACE_CREATE ==
?建新的工作区。

== CMD_USAGE_WORKSPACE_CREATE ==
用法：

    cm ^workspace | ^wk [^create | ^mk] <工作区名称> <工作区路径> [<存???格>]
                      [--^selector[=<??器文件>]
    （?建新的工作区。）

    cm ^workspace | ^wk [^create | ^mk] <工作区名称> <工作区路径> --^dynamic --^tree=[<?>]
    （?建??工作区。此功能仍?于???段，
    ??用于 Windows。）

    工作区名称             新工作区的名称。
    工作区路径             新工作区的路径。
    存???格            使用指定的存???建新的工作区。
                        存???格：?看 'cm ^help ^objectspec'。

??：

    --^selector          ??新工作区的??器。
                        如果指定了??器文件，?会从指定文件
                        ?新工作区?置??器。
    --^dynamic           ?建??工作区。此功能仍?于
                        ???段，??用于 Windows。
                        指定此?志需要使用 --^tree 参数。
    --^tree              与 '--^dynamic' ?志?合使用，指定要
                        加???工作区的初始点。此??
                        可以是分支、?更集或???格。
                        工作区稍后将使用?格中的
                        存??。（使用 'cm ^help ^objectspec' 可?一?了解?格。）

== CMD_HELP_WORKSPACE_CREATE ==
?注：

    - 工作区是映射到本地文件系?的存??的??。
      工作区??器可以定?用于指定工作区内容的??。
      使用 'cm ^showselector' ?示工作区??器，或使用 'cm ^setselector' 修改
      工作区??器。
    - 如果存???格和 '--^selector' 均未指定，?工作区
      将自?配置?使用 client.conf 文件中配置的
      服?器的第一个存??（按字母?序）。
    - ??工作区是一???性功能（??用于 Windows），
      并需要?行 plasticfs.exe 程序。

示例：

    cm ^workspace ^create myworkspace c:\workspace
    cm ^wk ^mk myworkspace /home/john/plastic_view
    （分?在 Windows 和 Linux 中?建 'myworkspace' 工作区。）

    cm ^wk mywktest c:\wks\wktest --^selector=myselector.txt
    （使用 'myselector.txt' 文件中的??器?建 'mywktest' 工作区。）

    cm ^wk mywkprj c:\wks\wkprj myrep@^repserver:localhost:8084
    （使用所?存???建 'mywkprj' 工作区。）

    cm ^wk mywkprj c:\dynwks\mywkprj --^dynamic --^tree=^br:/main@myrep@localhost:8084
    （使用 'myrep@localhost:8084' 存???建??的 'mywkprj'
     工作区，并在首次装入?工作区?指向 '^br:/main'。）

== CMD_DESCRIPTION_WORKSPACE_DELETE ==
?除工作区。

== CMD_USAGE_WORKSPACE_DELETE ==
用法：

    cm ^workspace | ^wk ^delete | ^rm [<工作区路径> | <工作区?格>] [--^keepmetadata]

    工作区路径             要?除的工作区的路径。
    工作区?格              要?除的工作区的?格。（使用
                        'cm ^help ^objectspec' 可?一?了解?格。）

??：

    --^keepmetadata      不?除 .plastic 文件?中的元数据
                        文件。

== CMD_HELP_WORKSPACE_DELETE ==
?注：

    此命令将?除由路径或?格指定的工作区。
    如果未指定任何参数，?将采用当前工作区。

示例：

    cm ^workspace ^delete
    （?除当前工作区。）

    cm ^wk ^delete c:\workspace
    cm ^workspace rm /home/danipen/wks
    cm ^wk ^rm ^wk:MiWorkspace
    cm ^wk ^rm ^wk:MiWorkspace@DIGITALIS

== CMD_DESCRIPTION_WORKSPACE_LIST ==
列出工作区。

== CMD_USAGE_WORKSPACE_LIST ==
用法：

    cm ^workspace | ^wk [^list | ^ls] [--^format=<格式字符串>]

??：

    --^format            ?索特定格式的?出消息。?参?
                        “?注”以了解更多信息。

== CMD_HELP_WORKSPACE_LIST ==
?注：

    此命令接受格式字符串以?示?出。
    此命令的?出参数如下：

        {0} | {^wkname}          工作区名称。
        {1} | {^machine}         客?端机器名称。
        {2} | {^path}            工作区路径。
        {3} | {^wkid}            工作区唯一??符。
        {4} | {^wkspec}          使用以下格式的工作区?格：
                               'wkname@machine'。
        {^tab}                   插入一个制表符空格位。
        {^newline}               插入一个新行。

示例：

    cm ^wk
    （列出所有工作区。）

    cm ^workspace ^list --^format={0}#{3,40}
    cm ^workspace ^list --^format={^wkname}#{^wkid,40}
    （列出所有工作区，并在 40 个空格位中?示工作区名称、# 符号
    和工作区 GUID 字段（左??）。）

    cm ^wk --^format="路径 {2} 中的工作区 {0}"
    cm ^wk --^format="路径 {^path} 中的工作区 {^wkname}"
    （列出所有工作区并将?果?示?格式化的字符串。）

== CMD_DESCRIPTION_WORKSPACE_MOVE ==
移?工作区。

== CMD_USAGE_WORKSPACE_MOVE ==
用法：

    cm ^workspace | ^wk ^move | ^mv [<工作区?格>] <新路径>

??：

    工作区?格              要移?的工作区的?格。（使用
                        'cm ^help ^objectspec' 可?一?了解?格。）
    新路径            工作区将移?到此?。

== CMD_HELP_WORKSPACE_MOVE ==
?注：

此命令允?用?将工作区移?到磁?上的?一个位置。

示例：

    cm ^workspace ^move myWorkspace \new\workspaceDirectory
    （将 'myWorkspace' 移?到指定位置。）

    cm ^wk ^mv c:\users\maria\wkspaces\newlocation
    （将当前工作区移?到新位置。）

== CMD_DESCRIPTION_WORKSPACE_RENAME ==
重命名工作区。

== CMD_USAGE_WORKSPACE_RENAME ==
用法：

    cm ^workspace | ^wk ^rename [<工作区名称>] <新名称>

    工作区名称             要重命名的工作区。
    新名称            工作区的新名称。

== CMD_HELP_WORKSPACE_RENAME ==
?注：

    此命令将重命名工作区。
    如果未提供工作区名称，?将使用当前工作区。

示例：

    cm ^workspace ^rename mywk1 wk2
    （将工作区 'mywk1' 重命名? 'wk2'。）

    cm ^wk ^rename newname
    （将当前工作区重命名? 'newname'。）

== CMD_DESCRIPTION_WORKSPACESTATUS ==
?示工作区中的更改。

== CMD_USAGE_WORKSPACESTATUS ==
用法：

    cm ^status [<工作区路径>] [--^changelist[=<名称>] | --^changelists] [--^cutignored]
              [ --^header] [ --^noheader] [ --^nomergesinfo] [ --^head]
              [--^short] [--^symlink] [ --^dirwithchanges] [--^xml[=<?出文件>]]
              [--^encoding=<名称>] [ --^wrp |  --^wkrootrelativepaths]
              [--^fullpaths | --^fp] [<旧??>] [<搜索?型>[ ...]]
              [--^machinereadable [--^startlineseparator=分隔符]
                [--^endlineseparator=分隔符] [--^fieldseparator=分隔符]]

??：

    工作区路径               要作?搜索范?的工作区的
                          路径。
    --^changelist          ?示所?更改列表中的更改。
    --^changelists         ?示按客?端更改列表?行分?的更改。
    --^cutignored          跳?已忽略目?的内容。
                          需要 '--^ignored' 搜索?型。?参?
                          “搜索?型”部分以了解更多信息。
    --^header              ?打印工作区状?。
    --^noheader            ?打印修改后的?搜索?果。
    --^nomergesinfo        不打印更改的合并信息。
    --^head                打印分支上最后一个?更集的状?。
    --^short               ?列出包含更改的路径。
    --^symlink             将操作?用于符号?接而不是
                          目?。
     --^dirwithchanges     ?示含有更改的目?
                          （含有已添加、移?、?除的?）。
    --^xml                 以 XML 格式将?出打印到?准?出。
                          可以指定?出文件。
    --^pretty              Prints workspace changes in a nice table format.
    --^encoding            与 --^xml ???合使用，指定要在 XML ?出
                          中使用的??（例如：utf-8）。
                          ?参?位于以下网址的 MSDN 文档：
                          http://msdn.microsoft.com/en-us/library/system.text.encoding.aspx
                          以?看包含受支持??及其格式的表格
                          （位于?面末尾的“名称”列中）。
     --^wrp                打印工作区根目?的相?路径，而不是
                          当前目?的相?路径。
    --^fullpaths, --^fp     ?制打印??路径，覆盖所有其他
                          路径打印?置。
    --^machinereadable     以易于解析的格式?出?果。
    --^startlineseparator  与 '--^machinereadable' ?志?合使用，
                          指定行?如何??。
    --^endlineseparator    与 '--^machinereadable' ?志?合使用，
                          指定行?如何?尾。
    --^fieldseparator      与 '--^machinereadable' ?志?合使用，
                          指定?如何分隔字段。

旧??：

    --^cset              以旧格式打印工作区状?。
    --^compact           以旧格式打印工作区状?和
                        更改列表。
    --^noheaders         与 '--^compact' ?志?合使用?，不会
                        打印更改列表??。（不?用于
                        新更改列表格式。）

搜索?型：

    --^added                         打印已添加的?。
    --^checkout                      打印已?出的?。
    --^changed                       打印已更改的?。
    --^copied                        打印已?制的?。
    --^replaced                      打印已替?的?。
    --^deleted                       打印已?除的?。
    --^localdeleted                  打印已在本地?除的?。
    --^moved                         打印已移?的?。
    --^localmoved                    打印已在本地移?的?。
    --^percentofsimilarity=<?>   ?了将?个文件??同一个?而需要
                                    ?者之??到的相似程度百分比。用于
                                    搜索已在本地移?的?。默??
                                    ? 20%。
    --^txtsameext                    在搜索已移?的?期?，
                                    相似性内容匹配?程将
                                    ?考?具有相同?展名的
                                    文本文件。默?情况下，
                                    将?理任何文本文件。
    --^binanyext                     在搜索已移?的?期?，
                                    相似性内容匹配?程将
                                    考?任何二?制文件。默?情况下，
                                    只会?理具有相同?展名的
                                    二?制文件。
    --^private                       打印非受控?。
    --^ignored                       打印已忽略的?。
    --^hiddenchanged                 打印?藏的已更改?。（包括
                                    '--^changed'）
    --^cloaked                       打印掩蔽的?。
    --^controlledchanged             此?志替代以下??：
                                    '--^added'、'--^checkout'、'--^copied'、
                                     '--^replaced'、'--^deleted'、'--^moved'。
    --^all                           此?志替?以下参数：
                                    '--^controlledchanged'、'--^changed'、
                                    '--^localdeleted'、'--^localmoved'、'--^private'。

== CMD_HELP_WORKSPACESTATUS ==
?注：

    '^status' 命令将打印工作区中已加?的?更集，并在工作区
    内?取已更改的元素。

    此命令可用于?示工作区中的待定更改；
    可以使用命令参数来修改可以搜索的
    更改?型。默?情况下将?示所有更改，无?是
    受控更改?是本地更改。

    搜索已在本地移?的??会使用相似性参数百分比
    '--^percentofsimilarity' (-^p) 来?定?个元素是否?同一个?。
    默??? 20%，但可以?整。

    可以?示按客?端更改列表分?的工作区更改。
    '^default' 更改列表包括其他更改列表中
    未包含的更改。因此，默?更改列表将?示的更改
    取决于指定的搜索?型?志。

    要?示按更改列表分?的更改，?需要?示受控
    的更改（状?? '^added'、'^checkout'、'^copied'、
    、'^replaced'、'^deleted' 或 '^moved' 的?）。因此，当?示更改列表?，
    '--^controlledchanged' ??将自??用。

    XML ?出的默???? utf-8。

    默?情况下，此命令将打印当前目?的相?路径，
    除非指定了 '--^machinereadable' 或 '--^short' ?志。如果
    指定了其中任何一个?志，?命令将打印??路径。

    如果指定了 '--^xml' ?志，?将打印工作区根目?的
    相?路径（除非也指定了 '--^fp' ?志，那?将
    打印??路径）。

示例：

    cm ^status
    （打印工作?更集以及工作区中所有已更改的
    ??型，但已忽略的?除外。）

    cm ^status --^controlledchanged
    （打印工作?更集以及已?出、添加、?制、替?、
    ?除和移?的?。）

    cm ^status --^added
    （?打印工作?更集和工作区中添加的?。）

    cm ^status c:\workspaceLocation\code\client --^added
    （以??方式打印指定路径下的工作?更集和
    添加的?。）

    cm ^status --^changelists
    cm ^status --^changelist
    （?示按客?端更改列表分?的所有工作区更改。）

    cm ^status --^changelist=pending_to_review
    （?示名? 'pending_to_review' 的更改列表中的更改。）

    cm ^status --^changelist=default --^private
    （?示 'default' 更改列表中的更改，包括私有?以及
    具有受控更改的?（如果有）。）

    cm ^status --^short --^changelist=pending_to_review | cm ^checkin -
    （?入更改列表 'pending_to_review' 中的更改。）

    cm ^status C:\workspaceLocation --^xml=output.xml
    （?取 XML 格式的状?信息，并在文件 output.xml 中
    使用 utf-8。）

    cm ^status --^ignored
    （?示所有已忽略的?。）
    ?出：
    /main@myrepo@local (^cs:2 - ^head)
    ^Added
        状?     大小       上次修改??     路径

        ^Ignored    0 字?    19 秒前    out\app.exe
        ^Ignored               48 秒前    src
        ^Ignored    0 字?    48 秒前    src\version.c

    cm ^status --^ignored --^cutignored
    （?示父目?未被忽略的已忽略文件，并?示
    内容未被忽略的已忽略目?。）
    ?出：
    /main@myrepo@local (^cs:2 - ^head)
    ^Added
        状?     大小       上次修改??     路径

        ^Ignored    0 字?    19 秒前    out\app.exe
        ^Ignored               48 秒前    src

== CMD_DESCRIPTION_XLINK ==
?建、??或?示 Xlink 的??信息。

== CMD_USAGE_XLINK ==
用法：

    cm ^xlink [-^w] [-^rs] <Xlink_路径> / (<?更集?格> | <???格> | <分支?格)>
             [<?展??>[ ...]]
    （?建一个 Xlink。）

    cm ^xlink [-^rs] <Xlink_路径> /<相?路径> (<?更集?格> | <???格> | <分支?格>)
             [<?展??>[ ...]]
    （?建指向 /<相?路径>（而不是指向默?根目? /）的只?
    部分 Xlink。）

    cm ^xlink -^e <Xlink_路径> (<?更集?格> | <???格> | <分支?格>)
    （?? Xlink 以更改目??格。）

    cm ^xlink -^s|--^show <Xlink_路径>
    （?示 Xlink 信息，包括?展??。）

    cm ^xlink -^ar|--^addrules <Xlink_路径> <?展??>[ ...]
    （将?定的?展??添加到 Xlink。）

    cm ^xlink -^dr|--^deleterules <Xlink_路径> <?展??>[ ...]
    （从 Xlink 中?除?定的?展??。）

    Xlink_路径          ?是当前工作区中将装入（?建 Xlink ?）
                        或已?装入（?? Xlink ?）
                        已?接的存??的目?。
    ?更集?格            ?程存??中的完整目??更集
                        ?格。
                        此?格?定了已?接的存??的工作区中
                        加?了?个版本和分支。
                        （使用 'cm ^help ^objectspec' 可?一?了解?更集
                        ?格。）
    ???格              ?程存??中的完整???格。
                        （使用 'cm ^help ^objectspec' 可?一?了解??
                        ?格。）
    分支?格              ?程存??中的完整分支?格。
                        此?格使用指定的分支所指向的
                        当前?更集。（使用 'cm ^help ^objectspec' 可
                        可?一?了解分支?格。）
    -^e                  ???有的 Xlink 以更改目??更集
                        ?格。
    -^s | --^show         ?示有?所? Xlink 的信息。
    -^ar | --^addrules    将一个或多个?展??添加到所? Xlink。
    -^dr | --^deleterules 从所? Xlink 中?除一个或多个?展
                        ??。
    ?展??     指定一个或多个?展??。?个?展
                        ??都是一?分支/目?分支：
                        ^br:/main/fix-^br:/main/develop/fix

??：

    -^w                  表示 Xlink 是可写的。?意味着
                        可以通?分支自??展功能来修改
                        目?存??的内容。
    -^rs                 相?服?器。此??允??建独立于
                        存??服?器的相? Xlink。??，
                        在不同服?器的?制存??中
                        ?建的 Xlink 将被自???。

== CMD_HELP_XLINK ==
?注：

    此命令将?建指向?定?更集的 Xlink。默?情况下将?建一个
    只? Xlink。?意味着无法修改 Xlink 内部工作区中
    加?的内容。?了能?更改通? Xlink ?接的内容，
    ??建可写的 Xlink（使用 '-^w' ??）。

    在?? Xlink 的目??更集?，可以使用
    ?命令的?化?法。??，唯一需要的参数是
    新的目??更集。Xlink 的其余参数将不会
    被修改。

    分支自??展：

    在任何可写 Xlink ?接的存??（'-^w' ??）中?行更改?，
    需要在目?存??中?建一个新分支。新分支的
    名称基于??存??中定?的?出
    分支。?了?定要使用的分支的名称，遵循以下??：

    1) 系?会??目?存??中是否存在具有
       相同全名的分支：
         - 如果存在，?将此分支用作?出分支。
         - 如果不存在，?通?以下方式?建分支名称：
           - 通? Xlink ?接的目??更集的分支名称 + ?出分支
             的?短名称（最后一部分）。
           - 如果存在此分支，?将此分支用作?出分支。
           - 否?，将?建分支并将分支基??置?
             通? Xlink ?接的?更集。

    2) 在父存??的分支中?建 Xlink 的新版本，
        指向通? Xlink ?接的存??中的新?更集。

    最后，完整的 Xlink ??会与正?版本中的
    最新更改保持同?。

示例：

    cm ^xlink code\firstrepo / 1@first@localhost:8084
    （在当前工作区的文件? 'firstrepo' 中?建一个 Xlink，
    在其中将装入存?? 'first' 中的?更集 '1'。）

    cm ^xlink opengl\include /includes/opengl 1627@includes@localhost:8087
    （在当前工作区的目? 'opengl\include' 中?建只?
     部分 Xlink，在其中，存?? 'includes' 的?更集 '1627' 中的路径
     '/includes/opengl' 将作?根目?装入。?意味着 '/includes/opengl'
     中的任何内容都将装入到 'opengl\include' 中，
    而存??的其余部分将被忽略。）

    cm ^xlink -^w -^rs code\secondrepo / ^lb:LB001@second@localhost:8084
    （在当前工作区的文件? 'secondrepo' 中?建一个
     可写的相? Xlink，在其中将装入存?? 'second' 中的
    ?? 'LB001'。）

    cm ^xlink code\thirdrepo / 3@third@localhost:8087 ^br:/main-^br:/main/scm003
    （在当前工作区的文件? 'thirdrepo' 中?建一个 Xlink，
    在其中将装入存?? 'third' 中的?更集 '3'。）

    cm ^xlink -^e code\secondrepo ^br:/main/task1234@second@localhost:8084
    （通??接存?? 'second' 中的分支 'main/task1234'，
    ?? Xlink 'code\secondrepo' 以更改目?存??。）

    cm ^xlink --^show code\thirdrepo
    （?示 Xlink 'code\thirdrepo' 的信息，包括其?展??
     （如果存在））。

    cm ^xlink -^ar code\secondrepo ^br:/main-^br:/main/develop ^br:/main/fix-^br:/main/develop/fix
    （将?个?展??添加到 Xlink 'code\secondrepo'。）

    cm ^xlink -^dr code\secondrepo ^br:/main/fix-^br:/main/develop/fix
    （从 Xlink 'code\secondrepo' 中?除?展??）。

== CMD_USAGE_AUTOCOMPLETE ==
用法：

    cm ^autocomplete ^install
    （在 shell 中安装 'cm' 命令??功能。）

    cm ^autocomplete ^uninstall
    （从 shell 中卸? 'cm' 命令??功能。）

    cm ^autocomplete --^line <shell_行> --^position <光?位置>
    （返回自???建?，以便将“shell_行”插入
     “光?位置”。此命令并非供最?用?使用，但?里??
     此命令是考?到?可能需要?展?所? shell 的
    自???支持。）

    shell_行      当?求自????由用?
                    写入 shell 的行。
                    在 Bash 中，此?位于 COMP_LINE ?境?量中。
                    在 PowerShell 中，?位于 $wordToComplete ?量中。
    光?位置 ?求自????的
                    光?位置。
                    在 Bash 中，此?位于 COMP_POINT ?境?量中。
                    在 PowerShell 中，?位于 $cursorPosition ?量中。

== CMD_DESCRIPTION_CONFIGURECLIENT ==
Configures the Unity VCS client for the current machine user to work with a default server.

== CMD_USAGE_CONFIGURECLIENT ==
用法：
    cm ^configure [--^language=<language> --^workingmode=<mode> [AuthParameters] 
                 --^server=<server> [--^port=<port>]] [--^clientconf=<clientconfpath>]

    --^language          可用?言：
                        en (English)
                        es (Spanish)

    --^workingmode       可用的用?/安全工作模式：
                        NameWorkingMode (Name)
                        NameIDWorkingMode (Name + ID)
                        LDAPWorkingMode (LDAP)
                        ADWorkingMode (Active Directory)
                        UPWorkingMode (User and password)
                        SSOWorkingMode (Single Sign On)

    AuthParameters      身???参数（??用于 ^LDAPWorkingMode 和 ^UPWorkingMode）：
                        --^user=<user>
                        --^password=<password>

                        Single Sign On parameters (only for ^SSOWorkingMode):
                        --^user=<user>
                        --^token=<token>

    --^server            Unity VCS server IP / address

    --^port              Unity VCS server port
                        (port optional for Cloud servers)

    --^clientconf        用于?建配置文件的文件路径（可?）
                        参数可以是完整路径、文件名或目?。

                        示例：

                        --^clientconf=c:/path/to/myclient.conf
                        （指定的路径将用于?建客?端配置文件）

                        --^clientconf=myclient.conf
                        （将使用默?配置目?中的文件 myclient.conf）

                        --^clientconf=c:/exisitingDirectory
                        （将使用指定目?中的默?文件名 client.conf）

== CMD_HELP_CONFIGURECLIENT ==
?注：

    The cm ^configure command cannot be used on Cloud Edition or DVCS Edition of Unity VCS.
    Use 'plastic --configure' instead.

示例：

    cm ^configure 
    (runs the interactive Unity VCS client configuration command)

    cm ^configure --^language=^en --^workingmode=^LDAPWorkingMode --^user=^jack --^password=^01234 \
                 --^server=^plastic.mymachine.com --^port=^8084
    (configures the Unity VCS client with the specified parameters and creates the 'client.conf'
    configuration file in the default directory).
    （Cloud 服?器的可?端口）

    cm ^configure --^language=^en --^workingmode=^NameWorkingMode --^server=^plastic.mymachine.com \
                 --^port=^8084 --^clientconf=^clientconf_exp.conf
    (configures the Unity VCS client with the specified parameters and creates the 'client.conf'
    configuration file in the specified path).
    （Cloud 服?器的可?端口）
